---
phase: 23-bulk-operations-ux
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/Deskbridge/ViewModels/BulkEditViewModel.cs
  - src/Deskbridge/Dialogs/BulkEditDialog.xaml
  - src/Deskbridge/Dialogs/BulkEditDialog.xaml.cs
  - src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml
  - src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml.cs
  - tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs
autonomous: true
requirements: [BULK-03, BULK-01]

must_haves:
  truths:
    - "BulkEditViewModel pre-fills shared fields and shows 'Multiple values' on divergent fields"
    - "ApplyToModels writes ONLY checked fields to all selected models; Name and password are never touched"
    - "CanApply is false until at least one field is enabled"
    - "BulkEditDialog renders a 3-column (Auto/72/*) grid with per-field enable checkboxes and validates port 1-65535 / non-empty hostname before applying"
    - "BulkConnectConfirmDialog renders the GDI Warning InfoBar with the canonical 'may degrade performance' copy"
  artifacts:
    - path: "src/Deskbridge/ViewModels/BulkEditViewModel.cs"
      provides: "Field-diff + per-field-enable + ApplyToModels + Validate (BULK-03)"
      contains: "ApplyToModels"
    - path: "src/Deskbridge/Dialogs/BulkEditDialog.xaml"
      provides: "Bulk edit modal form (DialogMaxWidth 520)"
      contains: "DialogMaxWidth=\"520\""
    - path: "src/Deskbridge/Dialogs/BulkEditDialog.xaml.cs"
      provides: "OnButtonClick validation gate + base(host) ctor + Enter-swallow"
      contains: "OnButtonClick"
    - path: "src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml"
      provides: "GDI confirm modal (DialogMaxWidth 420) with Warning InfoBar (BULK-01)"
      contains: "DialogMaxWidth=\"420\""
    - path: "src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml.cs"
      provides: "base(host) ctor + N/threshold params"
      contains: "base(host)"
  key_links:
    - from: "src/Deskbridge/Dialogs/BulkEditDialog.xaml"
      to: "BulkEditViewModel.CanApply"
      via: "IsPrimaryButtonEnabled binding"
      pattern: "IsPrimaryButtonEnabled=\"\\{Binding CanApply\\}\""
    - from: "src/Deskbridge/ViewModels/BulkEditViewModel.cs"
      to: "ConnectionModel (selected)"
      via: "per-field IsShared diff over the selection"
      pattern: "Distinct"
---

<objective>
Build the new, self-contained UI tier for Phase 23: the dependency-light BulkEditViewModel (the only
genuinely new logic in the phase) and the two new ContentDialogs (BulkEditDialog for BULK-03, and
BulkConnectConfirmDialog with the Warning InfoBar for BULK-01's threshold gate). This plan creates types
only — no tree-VM wiring, no DI registration, no context-menu changes (that is plan 23-03). It also makes
the BULK-03 ViewModel tests from 23-01 green (RED→GREEN).

Purpose: Isolate the new/novel logic and its tests into one plan so the diffing/apply contract is proven
before it is wired into the large ConnectionTreeViewModel singleton.
Output: BulkEditViewModel.cs, BulkEditDialog (xaml+cs), BulkConnectConfirmDialog (xaml+cs); BulkEditViewModelTests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bulk-operations-ux/23-UI-SPEC.md
@.planning/phases/23-bulk-operations-ux/23-PATTERNS.md
@.planning/phases/23-bulk-operations-ux/23-RESEARCH.md

<interfaces>
<!-- Verified in 23-RESEARCH.md / 23-PATTERNS.md. Use directly; no codebase exploration needed for contracts. -->

ConnectionModel editable fields (VERIFIED): Hostname (string), Port (int), CredentialMode (enum),
Username (string), Domain (string), GroupId (Guid?), Name (string — EXCLUDED), plus Id (Guid).
Password is NOT a property written here and is never in scope.

ConnectionEditorViewModel.cs / GroupEditorViewModel.cs — copy [ObservableProperty] partial-property +
[RelayCommand] conventions (CommunityToolkit.Mvvm 8.4.*, C# 14, no <LangVersion>preview>).

Dialog ctor + host pattern (analog): ConnectionEditorDialog.xaml.cs
  - ctor takes the ContentDialogHost and calls : base(host)
  - override OnButtonClick(ContentDialogButton button, ...): on Primary, validate; on failure `return`
    WITHOUT calling base.OnButtonClick to keep dialog open (ConnectionEditorDialog.xaml.cs:35-71)
  - Dialog_PreviewKeyDown swallows Enter in text inputs (ConnectionEditorDialog.xaml.cs:26-33)

GroupEditorDialog.xaml — scale analog for a smaller single-purpose ui:ContentDialog.

Port validation reference: SaveConnectionFromQuickEdit port range check (ConnectionTreeViewModel.cs:579) — 1..65535.

ComboBox glyph trap: ConnectionEditorDialog.xaml uses an explicit ItemTemplate to avoid the "- - -"
SelectionBoxItem glyph — copy that ItemTemplate approach for Credential mode and Group ComboBoxes.

Reusable styles/tokens (do NOT invent): SectionLabelStyle, FieldLabelCompactStyle, BodyStyle,
ErrorTextStyle, DialogContentMargin, FormFieldSpacing, CardContainerStyle. All colours via DynamicResource.
Warning brush: DeskbridgeWarningBrush. Accent: SystemAccentColorPrimaryBrush (WPF-UI Primary button appearance).

Canonical GDI warning phrasing (mirror, TabHostManager.cs:373-378): "...performance may degrade...".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BulkEditViewModel — field diff, per-field enable, ApplyToModels, Validate (BULK-03)</name>
  <files>src/Deskbridge/ViewModels/BulkEditViewModel.cs, tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs</files>
  <read_first>
    - src/Deskbridge/ViewModels/ConnectionEditorViewModel.cs (analog: [ObservableProperty]/[RelayCommand] conventions)
    - src/Deskbridge/ViewModels/GroupEditorViewModel.cs (analog: small single-purpose VM)
    - tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs (the failing/skipped contract from 23-01 — un-skip and make pass)
    - .planning/phases/23-bulk-operations-ux/23-RESEARCH.md (Pitfall 2 divergent/shared semantics; Code Examples)
    - .claude/skills/deskbridge-design/SKILL.md (token/spacing authority; no new design language)
  </read_first>
  <behavior>
    - Shared field (all selected models equal): pre-fill the value; enable checkbox unchecked.
    - Divergent field (>1 distinct value): IsShared=false; value blank; placeholder "Multiple values"; checkbox unchecked.
    - CanApply == false until ≥1 IsXxxEnabled is true; flips true when any enable flag set.
    - ApplyToModels(): for each selected ConnectionModel, overwrite a field ONLY if its IsXxxEnabled is true; otherwise leave unchanged. Never write Name. Never write any password/credential secret.
    - Group field (IsGroupEnabled): sets model.GroupId directly on connections; no cycle check (connections have no descendants).
    - Validate(): returns false (no throw) if Port enabled and value not in 1..65535, or Hostname enabled and empty/whitespace.
  </behavior>
  <action>
Create `src/Deskbridge/ViewModels/BulkEditViewModel.cs`, a thin DEPENDENCY-LIGHT VM (per
23-VALIDATION.md highest-value testability note). Ctor signature: `BulkEditViewModel(IReadOnlyList<ConnectionModel> selected)`.
No services injected — diffing/apply tests need zero mocks.

Editable set (Name EXCLUDED): Hostname, Port, CredentialMode, Username, Domain, GroupId. For EACH:
  - an `[ObservableProperty]` backing value (Hostname:string, Port:string or int per existing editor's port field —
    match ConnectionEditorViewModel's Port representation so the same 1..65535 check applies; CredentialMode:enum,
    Username/Domain:string, SelectedGroupId:Guid?),
  - an `[ObservableProperty] bool IsXxxEnabled`,
  - a `bool IsXxxShared` computed in ctor as `selected.Select(m => m.Field).Distinct().Count() == 1`.
Ctor: for each field, if shared → set the property to the shared value (checkbox stays unchecked); if divergent
→ leave blank and expose `Multiple values` via the dialog PlaceholderText (the VM exposes IsXxxShared so the
view binds the placeholder). Group ComboBox source: expose the available groups list (mirror how the editor lists
groups) OR accept the group list as a second ctor arg if needed for display — keep it dependency-light (a
plain `IReadOnlyList<(Guid Id, string Name)>` arg is acceptable; document the choice).

`public bool CanApply` (or [ObservableProperty]) = IsHostnameEnabled || IsPortEnabled || IsCredentialModeEnabled
|| IsUsernameEnabled || IsDomainEnabled || IsGroupEnabled. Raise PropertyChanged for CanApply whenever any
enable flag changes (partial OnXxxEnabledChanged hooks from the source generator).

`public List<ConnectionModel> ApplyToModels()` — return each selected model (clone or mutate-and-return per
the existing edit pattern; match how DeleteSelected/edit constructs the saved list) with ONLY checked fields
overwritten. Group: set GroupId. NEVER set Name. NEVER set any password.

`public bool Validate()` — pure, no throw: port range 1..65535 when IsPortEnabled (reuse the parse+range
shape from SaveConnectionFromQuickEdit:579); non-empty Hostname when IsHostnameEnabled.

Then open `tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs` and remove the `Skip=` from each
method, replacing the `// TODO 23-02:` blocks with the real Arrange/Act/Assert so all BULK-03 VM tests pass.
  </action>
  <verify>
    <automated>dotnet build src/Deskbridge/Deskbridge.csproj</automated>
    <automated>dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~BulkEditViewModel"</automated>
  </verify>
  <acceptance_criteria>
    - BulkEditViewModel.cs contains `public List<ConnectionModel> ApplyToModels`
    - BulkEditViewModel.cs contains `public bool Validate`
    - BulkEditViewModel.cs contains `CanApply`
    - BulkEditViewModel.cs contains `IsHostnameEnabled` and `IsPortEnabled` and `IsGroupEnabled`
    - BulkEditViewModel.cs does NOT contain `Name =` write inside ApplyToModels (Name excluded) and contains no `Password`
    - BulkEditViewModelTests.cs does NOT contain `Skip =`
    - `dotnet test ... --filter "FullyQualifiedName~BulkEditViewModel"` exits 0
  </acceptance_criteria>
  <done>BulkEditViewModel implements the diff/enable/apply/validate contract; all BULK-03 VM tests are un-skipped and green; Name and password are provably never written.</done>
</task>

<task type="auto">
  <name>Task 2: BulkEditDialog + BulkConnectConfirmDialog (ContentDialogs)</name>
  <files>src/Deskbridge/Dialogs/BulkEditDialog.xaml, src/Deskbridge/Dialogs/BulkEditDialog.xaml.cs, src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml, src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml.cs</files>
  <read_first>
    - src/Deskbridge/Dialogs/ConnectionEditorDialog.xaml (analog: ui:ContentDialog form, ComboBox ItemTemplate to avoid "- - -" glyph)
    - src/Deskbridge/Dialogs/ConnectionEditorDialog.xaml.cs (analog: base(host) ctor, OnButtonClick gate, PreviewKeyDown Enter-swallow)
    - src/Deskbridge/Dialogs/GroupEditorDialog.xaml (analog: smaller single-purpose dialog scale)
    - docs/WPF-UI-PITFALLS.md (ContentDialog hosting via RootContentDialog; SymbolIcon; Color vs Brush keys)
    - .claude/skills/deskbridge-design/README.md (spacing grid, component specs, tokens)
    - .planning/phases/23-bulk-operations-ux/23-UI-SPEC.md (Copywriting Contract + Component Inventory — verbatim copy/icons/widths)
  </read_first>
  <action>
Create BOTH dialogs as `ui:ContentDialog` subclasses, each ctor taking the ContentDialogHost and calling
`: base(host)` (mirror ConnectionEditorDialog). Register nothing here (DI is plan 23-03).

(A) src/Deskbridge/Dialogs/BulkEditDialog.xaml + .xaml.cs — BULK-03:
  - `DialogMaxWidth="520"`, `Title="Edit {N} connections"` (set from ctor/VM; e.g. `Edit 4 connections`),
    `PrimaryButtonText="Apply"`, `CloseButtonText="Cancel"`,
    `IsPrimaryButtonEnabled="{Binding CanApply}"`. BasedOn the implicit ContentDialog style.
  - Body: section label `FIELDS TO EDIT` (SectionLabelStyle). One row per editable field in a `Grid` with
    3 columns: `Auto` (CheckBox) / `72` (label, FieldLabelCompactStyle) / `*` (input). Rows for:
    Hostname (ui:TextBox), Port (ui:TextBox), Credential mode (ComboBox + explicit ItemTemplate),
    Username (ui:TextBox), Domain (ui:TextBox), Group (ComboBox + explicit ItemTemplate). NO Name row.
  - Per-row CheckBox: `IsChecked` two-way to the VM `IsXxxEnabled`; tooltip
    `Apply this field to all selected connections`. Each input `IsEnabled` bound to the same flag.
    Divergent fields: `PlaceholderText="Multiple values"` (bind/trigger off IsXxxShared, tertiary text colour).
  - Error surface: `ui:InfoBar Severity="Error"` title `Couldn't apply changes`,
    message `{n} of {N} connections could not be updated. No changes were saved.` (collapsed by default;
    shown by the tree VM on SaveBatch failure in 23-03 — expose an InfoBar the VM/dialog can toggle).
  - Use spacing tokens: DialogContentMargin, FormFieldSpacing (8px between rows). All colours DynamicResource.
  - Code-behind: ctor `(ContentDialogHost host, BulkEditViewModel vm) : base(host) { DataContext = vm; }`;
    override `OnButtonClick` — on Primary call `vm.Validate()`; if false, show the validation message and
    `return` WITHOUT `base.OnButtonClick` (keep open); else `base.OnButtonClick`. Add `Dialog_PreviewKeyDown`
    Enter-swallow on text inputs (copy ConnectionEditorDialog).

(B) src/Deskbridge/Dialogs/BulkConnectConfirmDialog.xaml + .xaml.cs — BULK-01 threshold gate:
  - `DialogMaxWidth="420"`, `Title="Connect all connections?"`, `PrimaryButtonText="Connect All"`,
    `PrimaryButtonAppearance="Primary"` (accent — NOT Danger; these ops are non-destructive),
    `DefaultButton="Primary"`, `CloseButtonText="Cancel"`.
  - Body: sentence `This will open {N} sessions. {threshold}+ active sessions may degrade performance. Continue?`
    (mirror the shipped "may degrade performance" phrasing) + `ui:InfoBar Severity="Warning"` with the
    `Warning24` SymbolIcon. {N} and {threshold} injected via ctor params.
  - Code-behind: ctor `(ContentDialogHost host, int sessionCount, int threshold) : base(host)` that formats
    Title/body text; no validation gate. No new accent usage beyond the built-in Primary appearance.
  </action>
  <verify>
    <automated>dotnet build src/Deskbridge/Deskbridge.csproj</automated>
  </verify>
  <acceptance_criteria>
    - BulkEditDialog.xaml contains `DialogMaxWidth="520"`
    - BulkEditDialog.xaml contains `PrimaryButtonText="Apply"` and `IsPrimaryButtonEnabled="{Binding CanApply}"`
    - BulkEditDialog.xaml contains `FIELDS TO EDIT`
    - BulkEditDialog.xaml contains `Multiple values`
    - BulkEditDialog.xaml does NOT contain a Name TextBox row (grep: no `Header="Name"` / `Label.*Name` input row)
    - BulkEditDialog.xaml.cs contains `OnButtonClick` and `base(host)`
    - BulkConnectConfirmDialog.xaml contains `DialogMaxWidth="420"`
    - BulkConnectConfirmDialog.xaml contains `PrimaryButtonAppearance="Primary"` and `Warning24`
    - BulkConnectConfirmDialog.xaml contains `may degrade performance`
    - BulkConnectConfirmDialog.xaml.cs contains `base(host)`
    - `dotnet build src/Deskbridge/Deskbridge.csproj` exits 0
  </acceptance_criteria>
  <done>Both dialogs compile, match the UI-SPEC widths/copy/icons verbatim, host the required InfoBars, use only existing tokens/styles, and follow the base(host) + OnButtonClick gate patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (dialog fields) → ConnectionModel | Hostname/Port/Username/Domain/Group typed in BulkEditDialog become persisted model fields (persistence happens in 23-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Tampering / Invalid Input | BulkEditDialog Port/Hostname | mitigate | V5 input validation: BulkEditViewModel.Validate() rejects Port outside 1..65535 and empty Hostname for ENABLED fields; OnButtonClick blocks Apply (returns without base.OnButtonClick) until valid. |
| T-23-04 | Information Disclosure | BulkEditViewModel.ApplyToModels | mitigate | Editable set is non-secret only (Hostname/Port/Mode/Username/Domain/Group). ApplyToModels never reads or writes any password/credential field; Name excluded. No credential auto-sync (documented decision — passwords out of scope). |
| T-23-05 | Repudiation/Logging leak | (deferred to 23-03 SaveBatch path) | accept | This plan persists nothing; logging of IDs/counts (not values) is enforced in 23-03 where SaveBatch is called. |
</threat_model>

<verification>
- `dotnet build src/Deskbridge/Deskbridge.csproj` exits 0
- `dotnet test ... --filter "FullyQualifiedName~BulkEditViewModel"` exits 0 (all un-skipped)
- BulkEditViewModel never writes Name or any password (grep + test ApplyToModels_NeverModifiesName / _NeverModifiesPassword green)
- Both dialogs honor UI-SPEC widths (520 / 420), copy strings, and icons verbatim
</verification>

<success_criteria>
- BulkEditViewModel implements shared/divergent diff, per-field enable, CanApply, ApplyToModels (Name/password excluded), Validate (port/hostname)
- All BULK-03 ViewModel tests green
- BulkEditDialog (520) and BulkConnectConfirmDialog (420) compile, match UI-SPEC verbatim, use only existing tokens/styles
</success_criteria>

<output>
After completion, create `.planning/phases/23-bulk-operations-ux/23-02-SUMMARY.md`
</output>
