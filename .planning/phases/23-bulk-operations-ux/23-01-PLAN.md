---
phase: 23-bulk-operations-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs
  - tests/Deskbridge.Tests/Services/BulkEditPersistenceTests.cs
  - tests/Deskbridge.Tests/ViewModels/ConnectAllTests.cs
  - tests/Deskbridge.Tests/ViewModels/DisconnectAllTests.cs
autonomous: true
requirements: [BULK-01, BULK-02, BULK-03]

must_haves:
  truths:
    - "All four bulk-ops test files exist and the solution still compiles"
    - "Each behavior from 23-VALIDATION.md Per-Task Verification Map has a named test method (skipped, pinning behavior)"
    - "The quick filter command resolves and runs the new test classes"
  artifacts:
    - path: "tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs"
      provides: "BULK-03 VM field-diff / per-field-enable / ApplyToModels / validation / Name-excluded test scaffold"
      contains: "class BulkEditViewModelTests"
    - path: "tests/Deskbridge.Tests/Services/BulkEditPersistenceTests.cs"
      provides: "BULK-03 SaveBatch round-trip / single-atomic-write test scaffold"
      contains: "class BulkEditPersistenceTests"
    - path: "tests/Deskbridge.Tests/ViewModels/ConnectAllTests.cs"
      provides: "BULK-01 count/threshold/confirm/publish/skip-open test scaffold"
      contains: "class ConnectAllTests"
    - path: "tests/Deskbridge.Tests/ViewModels/DisconnectAllTests.cs"
      provides: "BULK-02 descendant-close / enable-state test scaffold"
      contains: "class DisconnectAllTests"
  key_links:
    - from: "tests/Deskbridge.Tests/Services/BulkEditPersistenceTests.cs"
      to: "JsonConnectionStore (temp-file)"
      via: "BulkDeleteTests.cs setup pattern"
      pattern: "JsonConnectionStore"
---

<objective>
Create the Wave 0 Nyquist test scaffold for Phase 23 (Connect All / Disconnect All / Bulk Edit). This plan writes the four test files named in 23-VALIDATION.md with one named, behavior-pinning test method per row of the Per-Task Verification Map. Tests are authored as `[Fact(Skip = "...")]` or `[Fact]` against not-yet-existing production members where compilation would otherwise break — the goal is a compiling test project with the behavior contract locked in, ready for plans 02 and 03 to make green.

Purpose: Establishes feedback sampling (< 5s quick filter) before any production code is written, so plans 02/03 can run RED→GREEN.
Output: Four new test files; solution still builds; quick filter command discovers the new classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bulk-operations-ux/23-VALIDATION.md
@.planning/phases/23-bulk-operations-ux/23-RESEARCH.md
@.planning/phases/23-bulk-operations-ux/23-PATTERNS.md

<interfaces>
<!-- Verified in 23-RESEARCH.md / 23-PATTERNS.md against the real src/Deskbridge/... paths. -->

Test framework: xUnit.v3 3.2.* + FluentAssertions 8.9.* + NSubstitute 5.3.* (already configured;
no install). Microsoft.Testing.Platform — no config file. No XML fixtures needed for this phase;
construct ConnectionModel/ConnectionGroup in-memory against a temp-file JsonConnectionStore.

Store-test analog (copy temp-file setup + reload assertion):
  tests/Deskbridge.Tests/Services/BulkDeleteTests.cs:14-20 (temp store), :126 (SaveBatch round-trip + reload)

VM-with-substitutes analog (NSubstitute precedent for ConnectionTreeViewModel):
  tests/Deskbridge.Tests/ViewModels/ConnectionTreeStateTrackingTests.cs
  tests/Deskbridge.Tests/ViewModels/SwitchToExistingTabTests.cs (SwitchTo assertion shape)

Persistence contract (Deskbridge.Core):
  IConnectionStore.SaveBatch(IReadOnlyList<ConnectionModel> connections, IReadOnlyList<ConnectionGroup> groups)
  IConnectionStore.DeleteBatch(...)  — symmetric; already atomic via .tmp rename

Settings shape (Deskbridge.Core/Settings/AppSettings.cs — VERIFIED in RESEARCH):
  record BulkOperationsRecord(bool ConfirmBeforeBulkOperations, int GdiWarningThreshold)
  read via IWindowStateService.LoadAsync() -> settings.BulkOperations  (same shape as TabHostManager.cs:73-75)

Tab manager (Deskbridge.Core):
  ITabHostManager.ActiveCount  (int)
  ITabHostManager.TryGetExistingTab(Guid id, out _)  -> bool
  ITabHostManager.SwitchTo(Guid id)
  ITabHostManager.CloseTabAsync(Guid id)  -> Task

ConnectionModel editable fields (VERIFIED in PATTERNS.md): Hostname, Port, CredentialMode, Username, Domain, GroupId, Name.
  Bulk-edit editable set = Hostname, Port, CredentialMode, Username, Domain, GroupId. Name is EXCLUDED. Password never in scope.

Group recursive count: GroupTreeItemViewModel.ConnectionCount (recursive).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold BULK-03 test files (VM diff + persistence)</name>
  <files>tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs, tests/Deskbridge.Tests/Services/BulkEditPersistenceTests.cs</files>
  <read_first>
    - tests/Deskbridge.Tests/Services/BulkDeleteTests.cs (analog: temp-file JsonConnectionStore setup + SaveBatch reload assertion at :14-20, :126)
    - tests/Deskbridge.Tests/ViewModels/ConnectionTreeStateTrackingTests.cs (analog: VM-with-substitutes construction precedent)
    - .planning/phases/23-bulk-operations-ux/23-VALIDATION.md (Per-Task Verification Map — BULK-03 rows)
    - .planning/phases/23-bulk-operations-ux/23-PATTERNS.md (Test files table — what to copy from each analog)
  </read_first>
  <action>
Create two test files. Both must COMPILE against the test project today. Where they would reference
not-yet-existing production types (BulkEditViewModel), declare the test methods as
`[Fact(Skip = "Wave 1: implemented by 23-02 (BulkEditViewModel)")]` with the intended Arrange/Act/Assert
written out in the body but guarded so the file compiles (e.g. keep bodies that only use existing types,
or wrap planned VM usage in the skipped method body using `var sut = ...` only if the type exists — if it
does not yet exist, leave a `// TODO 23-02:` commented Act/Assert and a trivial `Assert.True(true)` so the
skipped method still compiles). Prefer real compiling bodies for anything that uses only existing types.

File 1 — tests/Deskbridge.Tests/ViewModels/BulkEditViewModelTests.cs, `public class BulkEditViewModelTests`.
One named [Fact(Skip=...)] per BULK-03 VM row of the Verification Map:
  - SharedField_PreFillsSharedValue_CheckboxUnchecked
  - DivergentField_IsSharedFalse_ShowsMultipleValuesPlaceholder
  - CanApply_FalseUntilAtLeastOneFieldChecked_TrueAfter
  - ApplyToModels_WritesOnlyCheckedFields_LeavesUncheckedDivergentUntouched
  - ApplyToModels_NeverModifiesName
  - ApplyToModels_NeverModifiesPassword
  - Validate_RejectsPortOutOf1To65535_WhenPortEnabled
  - Validate_RejectsEmptyHostname_WhenHostnameEnabled
  - ApplyToModels_GroupField_SetsGroupIdOnConnections
Each method's body documents the in-memory ConnectionModel setup (2+ models with one shared field, one
divergent field) in a `// TODO 23-02:` block plus `Assert.True(true)`.

File 2 — tests/Deskbridge.Tests/Services/BulkEditPersistenceTests.cs, `public class BulkEditPersistenceTests`.
COMPILE WITH REAL BODIES (uses only existing IConnectionStore/JsonConnectionStore/SaveBatch — no Skip needed):
  - SaveBatch_PersistsEditedConnections_SurvivesStoreReload  — copy BulkDeleteTests temp-store setup,
    seed N connections, mutate 2 fields on a subset, call store.SaveBatch(edited, Array.Empty<ConnectionGroup>()),
    new JsonConnectionStore over same temp path, reload, assert mutated values present and others unchanged.
  - SaveBatch_SingleWrite_NotPerItemSave  — assert the round-trip via one SaveBatch call (document IMP-04
    rationale in a comment; mirror BulkDeleteTests:126 reload assertion).
Use the same temp-file dispose/cleanup pattern as BulkDeleteTests (IDisposable or try/finally File.Delete).
  </action>
  <verify>
    <automated>dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj</automated>
    <automated>dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~BulkEditViewModel|FullyQualifiedName~BulkEditPersistence"</automated>
  </verify>
  <acceptance_criteria>
    - BulkEditViewModelTests.cs contains `class BulkEditViewModelTests`
    - BulkEditViewModelTests.cs contains `ApplyToModels_NeverModifiesName`
    - BulkEditViewModelTests.cs contains `Validate_RejectsPortOutOf1To65535_WhenPortEnabled`
    - BulkEditPersistenceTests.cs contains `class BulkEditPersistenceTests`
    - BulkEditPersistenceTests.cs contains `SaveBatch`
    - BulkEditPersistenceTests.cs contains `JsonConnectionStore`
    - `dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>Both BULK-03 test files exist, the test project compiles, BulkEditPersistenceTests pass (real bodies), BulkEditViewModelTests are present and skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold BULK-01 / BULK-02 test files (ConnectAll + DisconnectAll)</name>
  <files>tests/Deskbridge.Tests/ViewModels/ConnectAllTests.cs, tests/Deskbridge.Tests/ViewModels/DisconnectAllTests.cs</files>
  <read_first>
    - tests/Deskbridge.Tests/ViewModels/ConnectionTreeStateTrackingTests.cs (analog: ConnectionTreeViewModel with NSubstitute deps)
    - tests/Deskbridge.Tests/ViewModels/SwitchToExistingTabTests.cs (analog: SwitchTo / TryGetExistingTab assertion shape)
    - .planning/phases/23-bulk-operations-ux/23-VALIDATION.md (Per-Task Verification Map — BULK-01 and BULK-02 rows)
    - .planning/phases/23-bulk-operations-ux/23-RESEARCH.md (Pitfall 1 threshold boundary; Code Examples — ConnectAll shape)
  </read_first>
  <action>
Create two test files. Construct ConnectionTreeViewModel with NSubstitute substitutes for
ITabHostManager, IEventBus, IWindowStateService, IConnectionStore (and other existing ctor deps —
copy the substitute set from ConnectionTreeStateTrackingTests; the IWindowStateService param is NEW for
this phase, add a substitute for it). Because ConnectAllCommand/DisconnectAllCommand/GroupHasActiveSessions
do not exist yet, mark every method `[Fact(Skip = "Wave 2: implemented by 23-03 (ConnectionTreeViewModel commands)")]`
with intended Arrange/Act/Assert in a `// TODO 23-03:` block plus `Assert.True(true)` so files compile.

File 1 — tests/Deskbridge.Tests/ViewModels/ConnectAllTests.cs, `public class ConnectAllTests`:
  - ProjectedCount_EqualsActiveCountPlusGroupConnectionCount
  - Confirm_ShownOnly_WhenOverThreshold_And_ConfirmBeforeBulkOperationsTrue
  - AtOrBelowThreshold_ConnectsImmediately_NoDialog
  - ConfirmBeforeBulkOperationsFalse_ConnectsImmediately_NoDialog
  - ThresholdBoundary_EqualThreshold_DoesNotWarn  (== threshold: no warn)
  - ThresholdBoundary_ThresholdPlusOne_Warns      (threshold+1: warn)
  - PublishesConnectionRequestedEvent_PerDescendant
  - SkipsAlreadyOpenTabs_CallsSwitchTo
Document the substitute wiring (settings.BulkOperations with a chosen GdiWarningThreshold; tabHost.ActiveCount;
tabHost.TryGetExistingTab returns true for one descendant) in the TODO block.

File 2 — tests/Deskbridge.Tests/ViewModels/DisconnectAllTests.cs, `public class DisconnectAllTests`:
  - CallsCloseTabAsync_ForEveryActiveDescendant
  - DoesNotCloseTabAsync_ForInactiveDescendants
  - GroupHasActiveSessions_TrueWhenAtLeastOneActive_FalseWhenNone
  </action>
  <verify>
    <automated>dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj</automated>
    <automated>dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~ConnectAll|FullyQualifiedName~DisconnectAll"</automated>
  </verify>
  <acceptance_criteria>
    - ConnectAllTests.cs contains `class ConnectAllTests`
    - ConnectAllTests.cs contains `ThresholdBoundary_EqualThreshold_DoesNotWarn`
    - ConnectAllTests.cs contains `SkipsAlreadyOpenTabs_CallsSwitchTo`
    - DisconnectAllTests.cs contains `class DisconnectAllTests`
    - DisconnectAllTests.cs contains `GroupHasActiveSessions_TrueWhenAtLeastOneActive_FalseWhenNone`
    - `dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj` exits 0
  </acceptance_criteria>
  <done>Both files exist, compile, and the quick filter discovers ConnectAllTests + DisconnectAllTests (all skipped pending Wave 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only plan. No runtime trust boundary is crossed; tests run in-process against temp files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | BulkEditPersistenceTests temp store | accept | Temp store seeded with non-secret synthetic ConnectionModel data only; no real passwords/credentials written. Test cleans up temp files. |
| T-23-02 | Tampering | Test fixtures | accept | In-memory models only; no shared mutable state across test classes (each uses its own temp path). |
</threat_model>

<verification>
- `dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj` exits 0
- `dotnet test ... --filter "FullyQualifiedName~BulkEdit|FullyQualifiedName~ConnectAll|FullyQualifiedName~DisconnectAll"` runs (BulkEditPersistence green; others skipped)
- All four test files present in the listed paths
</verification>

<success_criteria>
- Four test files created matching 23-VALIDATION.md Wave 0 Requirements
- Test project compiles; quick filter command discovers all four classes
- BulkEditPersistenceTests pass with real bodies; VM/command tests are skipped placeholders with documented intent
</success_criteria>

<output>
After completion, create `.planning/phases/23-bulk-operations-ux/23-01-SUMMARY.md`
</output>
