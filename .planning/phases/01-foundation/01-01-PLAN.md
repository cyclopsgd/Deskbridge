---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Deskbridge.sln
  - Directory.Build.props
  - Directory.Packages.props
  - .editorconfig
  - .gitignore
  - LICENSE
  - src/Deskbridge/Deskbridge.csproj
  - src/Deskbridge/Program.cs
  - src/Deskbridge/App.xaml
  - src/Deskbridge/App.xaml.cs
  - src/Deskbridge/MainWindow.xaml
  - src/Deskbridge/MainWindow.xaml.cs
  - src/Deskbridge/GlobalUsings.cs
  - src/Deskbridge/app.manifest
  - src/Deskbridge.Core/Deskbridge.Core.csproj
  - src/Deskbridge.Core/GlobalUsings.cs
  - src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj
  - src/Deskbridge.Protocols.Rdp/GlobalUsings.cs
  - src/Deskbridge.Protocols.Rdp/Interop/MSTSCLib.dll
  - src/Deskbridge.Protocols.Rdp/Interop/AxMSTSCLib.dll
  - tests/Deskbridge.Tests/Deskbridge.Tests.csproj
  - tests/Deskbridge.Tests/GlobalUsings.cs
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
  - PROJ-05
  - CORE-01

must_haves:
  truths:
    - "dotnet build Deskbridge.sln succeeds with zero errors"
    - "dotnet test discovers the test project (even if no tests yet)"
    - "Running the app shows an empty dark-themed WPF window via Velopack entry point"
    - "All NuGet packages restore from a single Directory.Packages.props"
    - "Interop DLLs exist at src/Deskbridge.Protocols.Rdp/Interop/"
  artifacts:
    - path: "Deskbridge.sln"
      provides: "Solution file referencing all 4 projects"
      contains: "Deskbridge.Core"
    - path: "Directory.Build.props"
      provides: "Shared build config"
      contains: "net10.0-windows"
    - path: "Directory.Packages.props"
      provides: "Central Package Management"
      contains: "ManagePackageVersionsCentrally"
    - path: "src/Deskbridge/Program.cs"
      provides: "Velopack custom Main entry point"
      contains: "VelopackApp.Build().Run()"
    - path: "src/Deskbridge/App.xaml"
      provides: "WPF-UI dark theme bootstrap"
      contains: "ThemesDictionary"
    - path: "src/Deskbridge/app.manifest"
      provides: "Windows 10/11 compat + PerMonitorV2 DPI"
      contains: "PerMonitorV2"
    - path: ".editorconfig"
      provides: "Code style enforcement"
      contains: "CS8602"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
  key_links:
    - from: "Directory.Build.props"
      to: "all .csproj files"
      via: "MSBuild import"
      pattern: "net10\\.0-windows"
    - from: "Directory.Packages.props"
      to: "all .csproj PackageReferences"
      via: "Central Package Management"
      pattern: "ManagePackageVersionsCentrally"
    - from: "src/Deskbridge/Deskbridge.csproj"
      to: "src/Deskbridge/App.xaml"
      via: "Page build action (not ApplicationDefinition)"
      pattern: "ApplicationDefinition Remove"
---

<objective>
Scaffold the entire Deskbridge solution with all four projects, shared build configuration, Central Package Management, code style enforcement, Velopack entry point, WPF-UI dark theme bootstrap, app manifest, interop DLL positioning, and a minimal DI composition root that launches an empty FluentWindow.

Purpose: Establish the compilable foundation that every subsequent phase builds on. Nothing else can start until the solution structure, build config, and entry point are correct.
Output: A building solution with Deskbridge, Deskbridge.Core, Deskbridge.Protocols.Rdp, and Deskbridge.Tests projects, all config files, and a launchable dark-themed WPF window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@REFERENCE.md
@DESIGN.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create solution structure, build config, and all project files</name>
  <files>
    Deskbridge.sln
    Directory.Build.props
    Directory.Packages.props
    .editorconfig
    .gitignore
    LICENSE
    src/Deskbridge/Deskbridge.csproj
    src/Deskbridge/GlobalUsings.cs
    src/Deskbridge/app.manifest
    src/Deskbridge.Core/Deskbridge.Core.csproj
    src/Deskbridge.Core/GlobalUsings.cs
    src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj
    src/Deskbridge.Protocols.Rdp/GlobalUsings.cs
    src/Deskbridge.Protocols.Rdp/Interop/MSTSCLib.dll
    src/Deskbridge.Protocols.Rdp/Interop/AxMSTSCLib.dll
    tests/Deskbridge.Tests/Deskbridge.Tests.csproj
    tests/Deskbridge.Tests/GlobalUsings.cs
  </files>
  <read_first>
    REFERENCE.md (project structure, Directory.Build.props template, app.manifest template, .gitignore template)
    .planning/phases/01-foundation/01-RESEARCH.md (Central Package Management format, .editorconfig content, GlobalUsings.cs contents, version pins)
    .planning/phases/01-foundation/01-CONTEXT.md (locked decisions D-01 through D-08)
  </read_first>
  <action>
    Create the full solution structure using dotnet CLI and manual file creation:

    1. **Create projects via dotnet CLI:**
       - `dotnet new sln -n Deskbridge` at repo root
       - `dotnet new wpf -n Deskbridge -o src/Deskbridge -f net10.0-windows` (WPF app)
       - `dotnet new classlib -n Deskbridge.Core -o src/Deskbridge.Core -f net10.0-windows` (class library)
       - `dotnet new classlib -n Deskbridge.Protocols.Rdp -o src/Deskbridge.Protocols.Rdp -f net10.0-windows` (class library)
       - `dotnet new xunit -n Deskbridge.Tests -o tests/Deskbridge.Tests -f net10.0-windows` (test project -- will replace csproj contents)
       - Add all projects to solution: `dotnet sln add src/Deskbridge/ src/Deskbridge.Core/ src/Deskbridge.Protocols.Rdp/ tests/Deskbridge.Tests/`

    2. **Directory.Build.props** (repo root) -- per D-04, D-08, and REFERENCE.md:
       ```xml
       <Project>
         <PropertyGroup>
           <TargetFramework>net10.0-windows</TargetFramework>
           <UseWPF>true</UseWPF>
           <Nullable>enable</Nullable>
           <ImplicitUsings>enable</ImplicitUsings>
           <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
           <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
           <PackageLicenseExpression>MIT</PackageLicenseExpression>
         </PropertyGroup>
       </Project>
       ```

    3. **Directory.Packages.props** (repo root) -- per D-06, D-07:
       ```xml
       <Project>
         <PropertyGroup>
           <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
         </PropertyGroup>
         <ItemGroup>
           <!-- UI -->
           <PackageVersion Include="WPF-UI" Version="4.2.*" />
           <!-- MVVM -->
           <PackageVersion Include="CommunityToolkit.Mvvm" Version="8.4.*" />
           <!-- DI -->
           <PackageVersion Include="Microsoft.Extensions.DependencyInjection" Version="10.0.*" />
           <!-- Auto-update -->
           <PackageVersion Include="Velopack" Version="0.0.1298" />
           <!-- Logging -->
           <PackageVersion Include="Serilog" Version="4.3.*" />
           <PackageVersion Include="Serilog.Sinks.File" Version="7.0.*" />
           <!-- Testing -->
           <PackageVersion Include="xunit.v3" Version="3.2.*" />
           <PackageVersion Include="FluentAssertions" Version="8.9.*" />
           <PackageVersion Include="NSubstitute" Version="5.3.*" />
           <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.*" />
         </ItemGroup>
       </Project>
       ```

    4. **Deskbridge.csproj** (src/Deskbridge/) -- Velopack custom Main, WPF-UI, disable .NET fluent theme:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <OutputType>WinExe</OutputType>
           <StartupObject>Deskbridge.Program</StartupObject>
         </PropertyGroup>
         <ItemGroup>
           <ApplicationDefinition Remove="App.xaml" />
           <Page Include="App.xaml" />
         </ItemGroup>
         <ItemGroup>
           <RuntimeHostConfigurationOption Include="Switch.System.Windows.Appearance.DisableFluentThemeWindowBackdrop" Value="true" />
         </ItemGroup>
         <ItemGroup>
           <PackageReference Include="WPF-UI" />
           <PackageReference Include="CommunityToolkit.Mvvm" />
           <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
           <PackageReference Include="Velopack" />
           <PackageReference Include="Serilog" />
           <PackageReference Include="Serilog.Sinks.File" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="..\Deskbridge.Core\Deskbridge.Core.csproj" />
         </ItemGroup>
       </Project>
       ```

    5. **Deskbridge.Core.csproj** (src/Deskbridge.Core/) -- minimal class library:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <ItemGroup>
           <PackageReference Include="CommunityToolkit.Mvvm" />
         </ItemGroup>
       </Project>
       ```

    6. **Deskbridge.Protocols.Rdp.csproj** (src/Deskbridge.Protocols.Rdp/) -- UseWindowsForms HERE ONLY, interop DLL references:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <UseWindowsForms>true</UseWindowsForms>
         </PropertyGroup>
         <ItemGroup>
           <Reference Include="AxMSTSCLib">
             <HintPath>Interop\AxMSTSCLib.dll</HintPath>
           </Reference>
           <Reference Include="MSTSCLib">
             <HintPath>Interop\MSTSCLib.dll</HintPath>
           </Reference>
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="..\Deskbridge.Core\Deskbridge.Core.csproj" />
         </ItemGroup>
       </Project>
       ```

    7. **Deskbridge.Tests.csproj** (tests/Deskbridge.Tests/) -- per D-01:
       ```xml
       <Project Sdk="Microsoft.NET.Sdk">
         <PropertyGroup>
           <IsPackable>false</IsPackable>
           <IsTestProject>true</IsTestProject>
         </PropertyGroup>
         <ItemGroup>
           <PackageReference Include="xunit.v3" />
           <PackageReference Include="FluentAssertions" />
           <PackageReference Include="NSubstitute" />
           <PackageReference Include="Microsoft.NET.Test.Sdk" />
         </ItemGroup>
         <ItemGroup>
           <ProjectReference Include="..\..\src\Deskbridge\Deskbridge.csproj" />
           <ProjectReference Include="..\..\src\Deskbridge.Core\Deskbridge.Core.csproj" />
         </ItemGroup>
       </Project>
       ```

    8. **Move interop DLLs** from repo root to `src/Deskbridge.Protocols.Rdp/Interop/`:
       - `mkdir -p src/Deskbridge.Protocols.Rdp/Interop/`
       - `cp MSTSCLib.dll src/Deskbridge.Protocols.Rdp/Interop/MSTSCLib.dll`
       - `cp AxMSTSCLib.dll src/Deskbridge.Protocols.Rdp/Interop/AxMSTSCLib.dll`
       - Keep originals at root for now (remove in a later cleanup)

    9. **app.manifest** (src/Deskbridge/) -- exact content from REFERENCE.md:
       ```xml
       <?xml version="1.0" encoding="utf-8"?>
       <assembly manifestVersion="1.0" xmlns="urn:schemas-microsoft-com:asm.v1">
         <compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
           <application>
             <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}" />
           </application>
         </compatibility>
         <application xmlns="urn:schemas-microsoft-com:asm.v3">
           <windowsSettings>
             <dpiAwareness xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">PerMonitorV2</dpiAwareness>
             <dpiAware xmlns="http://schemas.microsoft.com/SMI/2005/WindowsSettings">true/pm</dpiAware>
           </windowsSettings>
         </application>
       </assembly>
       ```

    10. **.editorconfig** (repo root) -- full content from RESEARCH.md Code Examples section. Copy the entire .editorconfig block from that section verbatim, including: root=true, charset utf-8, indent rules, C# conventions, nullable warnings as errors (CS8600-CS8625), CA rules, naming conventions for private fields with _ prefix, interfaces with I prefix, async methods with Async suffix.

    11. **.gitignore** (repo root) -- replace existing with REFERENCE.md template PLUS standard .NET entries:
        ```
        bin/
        obj/
        *.user
        .vs/
        *.suo
        *.DotSettings.user
        .gsd/
        ```
        NOTE: Do NOT include `.planning/` in .gitignore -- planning artifacts are tracked.

    12. **LICENSE** (repo root) -- MIT license per D-08, with copyright "2026 Deskbridge Contributors".

    13. **GlobalUsings.cs** per project -- per D-05 and RESEARCH.md Code Examples:

        **src/Deskbridge/GlobalUsings.cs:**
        ```csharp
        global using System.Windows;
        global using System.Windows.Controls;
        global using CommunityToolkit.Mvvm.ComponentModel;
        global using CommunityToolkit.Mvvm.Input;
        global using Microsoft.Extensions.DependencyInjection;
        global using Deskbridge.Core.Interfaces;
        global using Deskbridge.Core.Models;
        global using Deskbridge.Core.Events;
        ```

        **src/Deskbridge.Core/GlobalUsings.cs:**
        ```csharp
        global using System.Collections.ObjectModel;
        global using CommunityToolkit.Mvvm.ComponentModel;
        global using CommunityToolkit.Mvvm.Messaging;
        ```

        **src/Deskbridge.Protocols.Rdp/GlobalUsings.cs:**
        ```csharp
        global using System.Windows.Forms.Integration;
        global using Deskbridge.Core.Interfaces;
        global using Deskbridge.Core.Models;
        ```

        **tests/Deskbridge.Tests/GlobalUsings.cs:**
        ```csharp
        global using Xunit;
        global using FluentAssertions;
        global using NSubstitute;
        global using Deskbridge.Core.Interfaces;
        global using Deskbridge.Core.Models;
        global using Deskbridge.Core.Events;
        global using Deskbridge.Core.Services;
        global using Deskbridge.Core.Pipeline;
        ```

    14. **Delete auto-generated template files** from dotnet new:
        - Delete any auto-generated Class1.cs files in Core and Protocols.Rdp
        - Delete auto-generated UnitTest1.cs in Tests
        - Clean up any duplicate AssemblyInfo or Program.cs generated by templates
  </action>
  <verify>
    <automated>dotnet restore Deskbridge.sln && echo "RESTORE OK"</automated>
  </verify>
  <acceptance_criteria>
    - Deskbridge.sln contains 4 project entries: Deskbridge, Deskbridge.Core, Deskbridge.Protocols.Rdp, Deskbridge.Tests
    - Directory.Build.props contains `<TargetFramework>net10.0-windows</TargetFramework>` and `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>`
    - Directory.Packages.props contains `<ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>` and `<PackageVersion Include="WPF-UI" Version="4.2.*" />`
    - src/Deskbridge/Deskbridge.csproj contains `<ApplicationDefinition Remove="App.xaml" />` and `<Page Include="App.xaml" />`
    - src/Deskbridge/Deskbridge.csproj contains `<StartupObject>Deskbridge.Program</StartupObject>`
    - src/Deskbridge/Deskbridge.csproj contains `DisableFluentThemeWindowBackdrop` RuntimeHostConfigurationOption
    - src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj contains `<UseWindowsForms>true</UseWindowsForms>`
    - Directory.Build.props does NOT contain `UseWindowsForms`
    - src/Deskbridge.Protocols.Rdp/Interop/MSTSCLib.dll exists
    - src/Deskbridge.Protocols.Rdp/Interop/AxMSTSCLib.dll exists
    - src/Deskbridge/app.manifest contains `PerMonitorV2`
    - .editorconfig contains `dotnet_diagnostic.CS8602.severity = error`
    - LICENSE file exists and contains `MIT License`
    - tests/Deskbridge.Tests/Deskbridge.Tests.csproj contains `<PackageReference Include="xunit.v3" />`
    - tests/Deskbridge.Tests/Deskbridge.Tests.csproj contains `<PackageReference Include="FluentAssertions" />`
    - tests/Deskbridge.Tests/Deskbridge.Tests.csproj contains `<PackageReference Include="NSubstitute" />`
    - No .csproj file other than Deskbridge.Protocols.Rdp.csproj contains `UseWindowsForms`
    - `dotnet restore Deskbridge.sln` succeeds
  </acceptance_criteria>
  <done>All 4 projects created with correct references, Central Package Management configured, code style enforcement active, interop DLLs positioned, app manifest present, LICENSE file present, and dotnet restore succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Velopack entry point, WPF-UI dark theme bootstrap, MainWindow, and DI skeleton</name>
  <files>
    src/Deskbridge/Program.cs
    src/Deskbridge/App.xaml
    src/Deskbridge/App.xaml.cs
    src/Deskbridge/MainWindow.xaml
    src/Deskbridge/MainWindow.xaml.cs
    src/Deskbridge/ViewModels/MainWindowViewModel.cs
  </files>
  <read_first>
    REFERENCE.md (Velopack entry point code, DI registrations in App.xaml.cs)
    DESIGN.md (App.xaml dark theme setup with ThemesDictionary + ControlsDictionary, FluentWindow pattern, ApplicationThemeManager.Apply call)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Velopack Custom Main with WPF-UI Dark Theme, anti-patterns to avoid)
    src/Deskbridge/Deskbridge.csproj (verify build action config from Task 1)
  </read_first>
  <action>
    Create the application entry point and minimal UI that proves the scaffold works:

    1. **Program.cs** (src/Deskbridge/) -- Velopack entry point per REFERENCE.md:
       ```csharp
       namespace Deskbridge;

       public static class Program
       {
           [STAThread]
           static void Main(string[] args)
           {
               VelopackApp.Build().Run();
               var app = new App();
               app.InitializeComponent();
               app.Run();
           }
       }
       ```
       CRITICAL: Main MUST be synchronous with [STAThread]. NEVER use async Main -- it silently changes thread to MTA and crashes ActiveX controls. MUST call app.InitializeComponent() after new App() and before app.Run() -- without this, WPF-UI theme resources fail to load silently.

    2. **App.xaml** (src/Deskbridge/) -- WPF-UI dark theme bootstrap per DESIGN.md section 1. Note: do NOT set StartupUri since we launch MainWindow from code:
       ```xml
       <Application x:Class="Deskbridge.App"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
           <Application.Resources>
               <ResourceDictionary>
                   <ResourceDictionary.MergedDictionaries>
                       <ui:ThemesDictionary Theme="Dark" />
                       <ui:ControlsDictionary />
                   </ResourceDictionary.MergedDictionaries>
               </ResourceDictionary>
           </Application.Resources>
       </Application>
       ```

    3. **App.xaml.cs** (src/Deskbridge/) -- composition root with DI container, belt-and-suspenders theme application per DESIGN.md:
       ```csharp
       using Wpf.Ui.Appearance;
       using Wpf.Ui.Controls;

       namespace Deskbridge;

       public partial class App : Application
       {
           private ServiceProvider? _serviceProvider;

           protected override void OnStartup(StartupEventArgs e)
           {
               base.OnStartup(e);

               ApplicationThemeManager.Apply(
                   ApplicationTheme.Dark,
                   WindowBackdropType.Mica,
                   updateAccent: true
               );

               var services = new ServiceCollection();
               ConfigureServices(services);
               _serviceProvider = services.BuildServiceProvider();

               var mainWindow = _serviceProvider.GetRequiredService<MainWindow>();
               mainWindow.Show();
           }

           private static void ConfigureServices(IServiceCollection services)
           {
               // Views
               services.AddTransient<MainWindow>();
               services.AddTransient<MainWindowViewModel>();

               // Core services will be registered in Plan 02
           }

           protected override void OnExit(ExitEventArgs e)
           {
               _serviceProvider?.Dispose();
               base.OnExit(e);
           }
       }
       ```

    4. **MainWindowViewModel.cs** (src/Deskbridge/ViewModels/) -- minimal ViewModel skeleton:
       ```csharp
       namespace Deskbridge.ViewModels;

       public partial class MainWindowViewModel : ObservableObject
       {
           [ObservableProperty]
           private partial string _title { get; set; } = "Deskbridge";
       }
       ```
       Note: Uses C# 14 partial property syntax with CommunityToolkit.Mvvm 8.4.2 source generators.

    5. **MainWindow.xaml** (src/Deskbridge/) -- FluentWindow with dark theme per DESIGN.md. This is a minimal shell that Phase 2 will expand:
       ```xml
       <ui:FluentWindow x:Class="Deskbridge.MainWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
           Title="{Binding Title}"
           Width="1100" Height="700"
           ExtendsContentIntoTitleBar="True"
           WindowBackdropType="Mica"
           WindowCornerPreference="Round"
           WindowStartupLocation="CenterScreen">
           <Grid>
               <Grid.RowDefinitions>
                   <RowDefinition Height="Auto" />
                   <RowDefinition Height="*" />
               </Grid.RowDefinitions>
               <ui:TitleBar Grid.Row="0" Title="Deskbridge" Height="32" />
               <TextBlock Grid.Row="1"
                   Text="Deskbridge Foundation"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   FontSize="20" />
           </Grid>
       </ui:FluentWindow>
       ```

    6. **MainWindow.xaml.cs** (src/Deskbridge/) -- FluentWindow code-behind:
       ```csharp
       using Wpf.Ui.Controls;

       namespace Deskbridge;

       public partial class MainWindow : FluentWindow
       {
           public MainWindow(MainWindowViewModel viewModel)
           {
               DataContext = viewModel;
               InitializeComponent();
           }
       }
       ```
       CRITICAL: The code-behind class MUST inherit from `FluentWindow`, not `Window`.

    After creating all files, run `dotnet build Deskbridge.sln` and fix any compilation errors. Common issues to check: namespace mismatches, missing using statements, duplicate Program.cs from template generation.
  </action>
  <verify>
    <automated>dotnet build Deskbridge.sln --no-restore && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - src/Deskbridge/Program.cs contains `[STAThread]` and `VelopackApp.Build().Run()` and `app.InitializeComponent()`
    - src/Deskbridge/Program.cs does NOT contain `async`
    - src/Deskbridge/App.xaml contains `<ui:ThemesDictionary Theme="Dark" />` and `<ui:ControlsDictionary />`
    - src/Deskbridge/App.xaml does NOT contain `StartupUri`
    - src/Deskbridge/App.xaml.cs contains `ApplicationThemeManager.Apply(` and `ApplicationTheme.Dark`
    - src/Deskbridge/App.xaml.cs contains `services.AddTransient<MainWindow>()`
    - src/Deskbridge/MainWindow.xaml contains `ui:FluentWindow` and `ExtendsContentIntoTitleBar="True"` and `WindowBackdropType="Mica"`
    - src/Deskbridge/MainWindow.xaml.cs inherits from `FluentWindow` (contains `: FluentWindow`)
    - src/Deskbridge/ViewModels/MainWindowViewModel.cs contains `ObservableObject` and `[ObservableProperty]`
    - `dotnet build Deskbridge.sln` succeeds with zero errors
  </acceptance_criteria>
  <done>The solution builds cleanly. Program.cs uses Velopack custom Main with STAThread. App.xaml loads WPF-UI dark theme resources. App.xaml.cs applies dark theme via ApplicationThemeManager and creates DI container. MainWindow is a FluentWindow with Mica backdrop and 32px title bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NuGet package restore | External packages fetched from nuget.org |
| Interop DLLs | Pre-built COM interop assemblies included in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Directory.Packages.props | mitigate | Pin all package versions with major.minor range or exact pin. No wildcard `*` without major.minor prefix. Velopack pinned exactly to `0.0.1298`. |
| T-01-02 | Tampering | Interop DLLs | accept | MSTSCLib.dll and AxMSTSCLib.dll are pre-generated via aximp.exe from system-installed RDP ActiveX. Binary files tracked in git. Risk is low -- these are local COM wrappers, not external downloads. |
| T-01-03 | Information Disclosure | .gitignore | mitigate | Ensure .gitignore excludes .vs/, *.user, *.suo, and any credential files. Do NOT exclude .planning/ (needed for workflow). |
| T-01-04 | Elevation of Privilege | app.manifest | mitigate | Manifest declares standard user execution (no requestedExecutionLevel=requireAdministrator). DPI awareness set to PerMonitorV2 for correct rendering. |
</threat_model>

<verification>
1. `dotnet restore Deskbridge.sln` -- all packages restore
2. `dotnet build Deskbridge.sln` -- zero errors, zero warnings (TreatWarningsAsErrors is on)
3. Solution has 4 projects: `dotnet sln list` shows Deskbridge, Deskbridge.Core, Deskbridge.Protocols.Rdp, Deskbridge.Tests
4. Interop DLLs exist: `ls src/Deskbridge.Protocols.Rdp/Interop/` shows MSTSCLib.dll and AxMSTSCLib.dll
5. `grep -r "UseWindowsForms" --include="*.props"` returns nothing (not in Directory.Build.props)
</verification>

<success_criteria>
- `dotnet build Deskbridge.sln` succeeds with zero errors
- All 4 projects reference each other correctly (Deskbridge -> Core, Protocols.Rdp -> Core, Tests -> Deskbridge + Core)
- Central Package Management active (no Version attributes in .csproj PackageReferences)
- Interop DLLs in correct location
- App manifest declares Windows 10/11 + PerMonitorV2
- Velopack entry point compiles (custom Main with STAThread)
- WPF-UI dark theme configured in App.xaml + App.xaml.cs
- .editorconfig enforces nullable warnings as errors
- LICENSE file at repo root
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
