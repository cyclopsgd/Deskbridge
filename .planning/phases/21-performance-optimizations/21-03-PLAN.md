---
phase: 21
plan: 03
type: tdd
wave: 2
depends_on:
  - 21-02
files_modified:
  - src/Deskbridge.Core/Interfaces/IConnectionStore.cs
  - src/Deskbridge.Core/Services/JsonConnectionStore.cs
  - src/Deskbridge/App.xaml.cs
  - tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs
autonomous: true
requirements:
  - PERF-03

must_haves:
  truths:
    - "JsonConnectionStore exposes Task LoadAsync() that loads connections from JSON identical to sync Load (D-06)"
    - "LoadAsync runs file I/O off the calling thread (Task.Run dispatch verified by ManagedThreadId comparison)"
    - "Sync Load() remains for benchmarks/tests — not removed, not [Obsolete] (D-06)"
    - "App.OnStartup is async void; awaits store.LoadAsync() AFTER BuildServiceProvider() and BEFORE MigrateFromTermsrv / MainWindow.Show() (D-04, D-05)"
    - "JsonConnectionStore DI factory no longer calls Load() — factory just constructs"
  artifacts:
    - path: "src/Deskbridge.Core/Interfaces/IConnectionStore.cs"
      provides: "Task LoadAsync() member added next to existing void Load()"
      contains: "Task LoadAsync"
    - path: "src/Deskbridge.Core/Services/JsonConnectionStore.cs"
      provides: "public Task LoadAsync() => Task.Run(Load)"
      contains: "LoadAsync"
    - path: "src/Deskbridge/App.xaml.cs"
      provides: "async void OnStartup with awaited LoadAsync after BuildServiceProvider"
      contains: "await store.LoadAsync"
    - path: "tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs"
      provides: "RED-then-GREEN tests for LoadAsync roundtrip, missing-file fallback, background-thread dispatch"
  key_links:
    - from: "App.OnStartup"
      to: "JsonConnectionStore.LoadAsync"
      via: "awaited Task between BuildServiceProvider() and MigrateFromTermsrv"
      pattern: "await store\\.LoadAsync"
    - from: "JsonConnectionStore.LoadAsync"
      to: "JsonConnectionStore.Load"
      via: "Task.Run wrapper"
      pattern: "Task\\.Run\\(Load\\)"
---

<objective>
PERF-03 — Add `Task LoadAsync()` to `IConnectionStore` and implement it in `JsonConnectionStore` as a `Task.Run(Load)` wrapper. Refactor `App.OnStartup` to be `async void` and `await store.LoadAsync()` between `BuildServiceProvider()` and `MigrateFromTermsrv`. Move the load OUT of the DI factory so file I/O no longer runs on the UI thread during DI graph construction.

Purpose: D-04/D-05/D-06 (CONTEXT.md) lock the strategy — new method (not replacement), background dispatch, post-DI await placement. Sync `Load()` retained for `tests/Deskbridge.Benchmarks/StoreBenchmarks` and existing tests.

Output: Interface change + service impl + DI factory simplification + `OnStartup` async refactor + xUnit test class. TDD ordering: RED (interface + tests, build fails) → GREEN (impl + startup wiring, tests pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-performance-optimizations/21-CONTEXT.md
@.planning/phases/21-performance-optimizations/21-RESEARCH.md
@.planning/phases/21-performance-optimizations/21-PATTERNS.md

<interfaces>
<!-- Existing IConnectionStore interface — verbatim from PATTERNS.md "IConnectionStore.cs" section. -->
<!-- The new Task LoadAsync(); line is added immediately after the existing void Load(); line. -->
<!-- Existing Load() body (sync, file I/O + JSON parse + JsonException fallback to empty store) -->
<!-- is reproduced verbatim in PATTERNS.md under the JsonConnectionStore section. -->
<!-- Existing OnStartup pattern (lines 35-148) and DI factory (lines 200-206 register IConnectionStore -->
<!-- with sync Load() inside the factory) are reproduced verbatim in PATTERNS.md under "App.xaml.cs". -->
<!-- Existing test scaffolding (temp-dir + IDisposable + JsonConnectionStore ctor) -->
<!-- is in tests/Deskbridge.Tests/JsonConnectionStoreTests.cs lines 1-52, reproduced verbatim in PATTERNS.md. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add LoadAsync to interface + write failing async-load tests</name>
  <files>src/Deskbridge.Core/Interfaces/IConnectionStore.cs, tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs</files>
  <read_first>
    - src/Deskbridge.Core/Interfaces/IConnectionStore.cs (full file — current member ordering)
    - src/Deskbridge.Core/Services/JsonConnectionStore.cs (lines 1-80 — for namespace, ctor signature accepting filePath)
    - tests/Deskbridge.Tests/JsonConnectionStoreTests.cs (lines 1-80 — sibling test ctor with temp-dir IDisposable pattern, copy verbatim)
    - .planning/phases/21-performance-optimizations/21-PATTERNS.md ("IConnectionStore.cs" + "JsonConnectionStoreAsyncLoadTests.cs (NEW)" sections)
  </read_first>
  <behavior>
    - Test 1 `Save_ThenLoadAsync_RoundtripsConnection`: save a `ConnectionModel` via existing `Save`, construct a fresh `JsonConnectionStore` against the same file path, `await store.LoadAsync()`, assert `GetAll()` contains the saved connection.
    - Test 2 `LoadAsync_MissingFile_StartsWithEmptyStore`: construct store against a non-existent file path, `await LoadAsync()`, assert `GetAll().Should().BeEmpty()` and `GetGroups().Should().BeEmpty()` — no exception thrown.
    - Test 3 `LoadAsync_RunsOnBackgroundThread`: capture `Environment.CurrentManagedThreadId` before the `await`; inside the awaited continuation (post-await line in test method), capture again. Test infrastructure (xUnit.v3 default) does not pin a specific synchronization context so we must instead verify Task.Run dispatch by observing thread inside the wrapped Load. Specifically: set `_threadIdInsideLoad` from a custom test subclass or by injecting a callback on file open. **Recommended approach:** seed a deterministic JSON file, then `var idBefore = Environment.CurrentManagedThreadId; await store.LoadAsync(); /* nothing to assert here — instead use a probe file with a side-channel */`. **Simpler probe:** override the test by creating a `JsonConnectionStoreThreadProbe.cs` derived class that captures `Environment.CurrentManagedThreadId` inside an overridden Load... but `Load` is not virtual. Use the alternate strategy: inside the test method, run `await Task.Run(() => store.Load());` to verify the dispatch primitive directly, AND assert `await store.LoadAsync()` returns a non-completed-synchronously Task. Concretely: assert `store.LoadAsync().IsCompletedSuccessfully == false` immediately after invocation (Task.Run schedules to threadpool — the call site sees a hot, not-yet-completed task). This is acceptable proof of off-thread dispatch.
  </behavior>
  <action>
    1. **Modify `src/Deskbridge.Core/Interfaces/IConnectionStore.cs`**:
       (a) Verify whether `using System.Threading.Tasks;` is needed by checking the project's `<ImplicitUsings>` setting in `src/Deskbridge.Core/Deskbridge.Core.csproj`. If implicit usings are `enable`, `Task` is already in scope. Otherwise, add `using System.Threading.Tasks;` to the using block (or to the top of the file, matching existing using style).
       (b) Add `Task LoadAsync();` as a new line immediately after the existing `void Load();` line (the sync/async pair stays grouped). Match the no-XML-doc convention of the surrounding members.

    2. **Create `tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs`** (create the `Services/` test folder if it doesn't exist; verify via `ls tests/Deskbridge.Tests/`). Class is `public sealed class JsonConnectionStoreAsyncLoadTests : IDisposable`. Namespace `Deskbridge.Tests.Services`. Copy the temp-dir + IDisposable ctor pattern verbatim from `tests/Deskbridge.Tests/JsonConnectionStoreTests.cs` lines 1-52 (PATTERNS.md reproduces the verbatim form):
       - Fields `private readonly string _tempDir; private readonly string _filePath; private readonly JsonConnectionStore _store;`
       - Ctor creates `Path.Combine(Path.GetTempPath(), "DeskbridgeTests_" + Guid.NewGuid().ToString("N"))`, calls `Directory.CreateDirectory`, sets `_filePath = Path.Combine(_tempDir, "connections.json")`, constructs `_store = new JsonConnectionStore(_filePath)`.
       - `Dispose()` deletes the temp dir best-effort (try/catch).

       Add three `[Fact]` methods with these exact names + bodies:

       (a) `[Fact] public async Task Save_ThenLoadAsync_RoundtripsConnection()` — create a `ConnectionModel` (Id = `Guid.NewGuid()`, Name = `"TestServer"`, Hostname = `"server01.local"`, Port = 3389, Protocol = `Protocol.Rdp`); call `_store.Save(conn);`; construct a SECOND store `var store2 = new JsonConnectionStore(_filePath);` and `await store2.LoadAsync();`. Assert `store2.GetAll().Should().HaveCount(1)` and the loaded connection's Id matches.

       (b) `[Fact] public async Task LoadAsync_MissingFile_StartsWithEmptyStore()` — note the ctor already created an empty `_tempDir`; do NOT save anything. Call `var store2 = new JsonConnectionStore(Path.Combine(_tempDir, "nonexistent.json"));` then `await store2.LoadAsync();`. Assert `store2.GetAll().Should().BeEmpty();` and `store2.GetGroups().Should().BeEmpty();`. The `JsonException` / missing-file handling is delegated to the existing sync `Load` (PATTERNS.md confirms the fallback).

       (c) `[Fact] public async Task LoadAsync_DispatchesViaTaskRun()` — seed a connection (e.g., `_store.Save(new ConnectionModel { Id = Guid.NewGuid(), Name = "X", Hostname = "h", Port = 3389, Protocol = Protocol.Rdp });`) so the file is non-trivial. Construct a fresh store `var store2 = new JsonConnectionStore(_filePath);`. Capture `var task = store2.LoadAsync();`. Assert `task.IsCompleted.Should().BeFalse()` OR `task.IsCompletedSuccessfully.Should().BeFalse()` IMMEDIATELY after invocation (Task.Run schedules to threadpool — the synchronously-returned Task is not yet completed even for trivial files). Then `await task;` and assert `store2.GetAll().Should().HaveCount(1);`.

       Using-block (verbatim order from PATTERNS.md): `System.IO`, `System.Threading.Tasks`, `Deskbridge.Core.Interfaces`, `Deskbridge.Core.Models`, `Deskbridge.Core.Services`, `FluentAssertions`, `Xunit`.

    3. **Run** `dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj -c Debug --nologo`. Build MUST FAIL because `JsonConnectionStore` does not yet implement `Task LoadAsync()` (CS0535 — interface member not implemented — when the compiler sees the new interface contract). **This is the RED state.**

    4. **Stage and commit** with message `test(21-03): add failing LoadAsync tests for PERF-03 (RED)`. Do NOT use `Co-Authored-By`.
  </action>
  <verify>
    <automated>cd /c/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj -c Debug --nologo 2&gt;&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Task LoadAsync' src/Deskbridge.Core/Interfaces/IConnectionStore.cs` returns exactly 1 match, on a line immediately after `void Load();`.
    - `grep -c '\[Fact\]' tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs` returns at least 3.
    - `grep -c 'Save_ThenLoadAsync_RoundtripsConnection\|LoadAsync_MissingFile_StartsWithEmptyStore\|LoadAsync_DispatchesViaTaskRun' tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs` returns 3.
    - `grep -n 'public sealed class JsonConnectionStoreAsyncLoadTests : IDisposable' tests/Deskbridge.Tests/Services/JsonConnectionStoreAsyncLoadTests.cs` returns 1 match.
    - `dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj -c Debug --nologo` exits NON-ZERO with at least one error CS0535 (interface member not implemented) referencing `JsonConnectionStore.LoadAsync`.
    - Latest commit subject matches regex `^test\(21-03\): .*RED.*$`.
  </acceptance_criteria>
  <done>
    Interface extended; three test methods written; build fails on missing impl; RED commit recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement LoadAsync + refactor App.OnStartup to async</name>
  <files>src/Deskbridge.Core/Services/JsonConnectionStore.cs, src/Deskbridge/App.xaml.cs</files>
  <read_first>
    - src/Deskbridge.Core/Services/JsonConnectionStore.cs (lines 1-80 — locate the existing `public void Load()` at line ~42 and the next member after it)
    - src/Deskbridge/App.xaml.cs (FULL OnStartup method lines 35-148 + DI factory at lines 200-210 — to identify exact insertion points and ordering of subsequent post-load steps like MigrateFromTermsrv)
    - .planning/phases/21-performance-optimizations/21-RESEARCH.md §3 (LoadAsync option A) + §5 (App.xaml.cs wiring sketch — the order invariant: BuildServiceProvider → await LoadAsync → MigrateFromTermsrv → pipeline wiring → MainWindow.Show)
    - .planning/phases/21-performance-optimizations/21-PATTERNS.md ("JsonConnectionStore.cs" + "App.xaml.cs" sections)
  </read_first>
  <behavior>
    - The three tests from Task 1 BUILD and PASS.
    - `JsonConnectionStore` has a `public Task LoadAsync() => Task.Run(Load);` one-liner immediately after `Load()`.
    - `App.OnStartup` is `async void` and awaits `store.LoadAsync()` between `BuildServiceProvider()` and the existing `MigrateFromTermsrv` call. The await is wrapped in try/catch with `Log.Fatal` on exception.
    - The existing `IConnectionStore` DI factory simplifies to `services.AddSingleton<IConnectionStore>(sp => new JsonConnectionStore());` — no `Load()` call inside the factory.
    - All previously-green tests remain green.
  </behavior>
  <action>
    1. **Modify `src/Deskbridge.Core/Services/JsonConnectionStore.cs`**:
       (a) Verify `using System.Threading.Tasks;` is present (or covered by implicit usings). Add to the using block if missing.
       (b) Add a single new line immediately AFTER the closing brace of the existing `public void Load()` method (around line 65) and BEFORE the next member (`GetAll()` around line 67):
       ```csharp
       /// <summary>
       /// Phase 21 (PERF-03): async load wrapper. Dispatches sync Load() to the threadpool
       /// via Task.Run so file I/O + JSON parse run off the calling (UI) thread. Sync Load()
       /// remains the canonical implementation; this is purely a dispatch shim per D-06.
       /// </summary>
       public Task LoadAsync() => Task.Run(Load);
       ```
       Do NOT modify `Load()`, `_data`, `_filePath`, `Save`, `Save Group`, or any other member.

    2. **Modify `src/Deskbridge/App.xaml.cs`** — three changes:

       (a) Change `protected override void OnStartup(StartupEventArgs e)` to `protected override async void OnStartup(StartupEventArgs e)`. The single-keyword `async` insertion is the only signature change.

       (b) Locate the existing `IConnectionStore` registration (PATTERNS.md "App.xaml.cs" section, lines 201-206 in current file):
       ```csharp
       services.AddSingleton<IConnectionStore>(sp =>
       {
           var store = new JsonConnectionStore();
           store.Load();
           return store;
       });
       ```
       Replace with:
       ```csharp
       services.AddSingleton<IConnectionStore>(sp => new JsonConnectionStore());
       ```

       (c) Insert the awaited LoadAsync call between `BuildServiceProvider()` and the existing `MigrateFromTermsrv` block. Locate `_serviceProvider = services.BuildServiceProvider();` (around line 65) and the next existing block which is the `WindowsCredentialService.MigrateFromTermsrv` call (around line 67-76). Between those two, insert:
       ```csharp
       // Phase 21 (PERF-03): load connections off the UI thread before MainWindow is shown.
       // Sync Load() retained on JsonConnectionStore for benchmarks and tests; this awaited
       // path is the production startup load. Existing post-load steps (credential migration,
       // pipeline wiring) run AFTER this await — order preserved per D-04/D-05.
       try
       {
           var connectionStore = _serviceProvider.GetRequiredService<IConnectionStore>();
           await connectionStore.LoadAsync();
       }
       catch (Exception ex)
       {
           Log.Fatal(ex, "Failed to load connections during startup");
       }
       ```
       Do NOT reorder any subsequent code. The `MigrateFromTermsrv` block, pipeline stage wiring, eager singleton resolution, and `mainWindow.Show()` must remain in their existing relative order — they all simply move 1 try/catch block later.

    3. **Build**: `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo`. Must exit 0. (The `async void OnStartup` is a documented WPF pattern — no warning is expected, but if VS-style "async void" warnings fire, do not suppress; the existing `CrashHandler.InstallDispatcherHook` covers post-`await` exceptions outside the new try/catch per RESEARCH.md §5.2.)

    4. **Run targeted tests**: `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~JsonConnectionStoreAsyncLoadTests" --no-restore --nologo`. All three tests must pass.

    5. **Run full test suite**: `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --no-restore --nologo`. All must pass. If any existing test that uses `Substitute.For<IConnectionStore>()` fails because it awaits `LoadAsync()` (returns null Task by default), add `mock.LoadAsync().Returns(Task.CompletedTask);` to that test's setup. Per PATTERNS.md, only the new `JsonConnectionStoreAsyncLoadTests` should hit this path; sibling VM tests do not call `LoadAsync`.

    6. **Run benchmark project build** (no execution needed): `dotnet build tests/Deskbridge.Benchmarks/Deskbridge.Benchmarks.csproj -c Release --nologo`. Confirms `Load()` is still callable from `StoreBenchmarks` (D-06 invariant — sync Load retained).

    7. **Commit GREEN**: stage all modified files and commit with message `feat(21-03): implement LoadAsync + async OnStartup for off-thread JSON load (GREEN)`. Do NOT use `Co-Authored-By`.
  </action>
  <verify>
    <automated>cd /c/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~JsonConnectionStoreAsyncLoadTests" --no-restore --nologo 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'public Task LoadAsync()' src/Deskbridge.Core/Services/JsonConnectionStore.cs` returns 1 match.
    - `grep -n 'Task\.Run(Load)' src/Deskbridge.Core/Services/JsonConnectionStore.cs` returns 1 match.
    - `grep -n 'protected override async void OnStartup' src/Deskbridge/App.xaml.cs` returns 1 match.
    - `grep -n 'await connectionStore\.LoadAsync()' src/Deskbridge/App.xaml.cs` returns 1 match.
    - `grep -n 'AddSingleton<IConnectionStore>(sp => new JsonConnectionStore())' src/Deskbridge/App.xaml.cs` returns 1 match (factory simplified).
    - `grep -c 'store\.Load();' src/Deskbridge/App.xaml.cs` returns 0 (no sync Load() call remains in App.xaml.cs).
    - The `MigrateFromTermsrv` call must appear AFTER the `await connectionStore.LoadAsync()` line — verify by `grep -n -E 'await connectionStore\.LoadAsync|MigrateFromTermsrv' src/Deskbridge/App.xaml.cs` and confirming the line numbers are in that order.
    - `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~JsonConnectionStoreAsyncLoadTests" --no-restore --nologo` exits 0 with `Passed: 3`.
    - `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --no-restore --nologo` exits 0 (full suite green).
    - `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo` exits 0.
    - `dotnet build tests/Deskbridge.Benchmarks/Deskbridge.Benchmarks.csproj -c Release --nologo` exits 0 (benchmarks still reference sync Load).
    - Latest commit subject matches regex `^feat\(21-03\): .*GREEN.*$`.
  </acceptance_criteria>
  <done>
    LoadAsync implemented as Task.Run(Load); App.OnStartup is async void with awaited LoadAsync between BuildServiceProvider and MigrateFromTermsrv; DI factory no longer calls Load; all tests green; benchmarks still build; GREEN commit recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O (connections.json) | Same trust boundary as existing sync Load() — same path, same parser, same fallback to empty store on JsonException. No new boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | T (Tampering) | JsonConnectionStore.LoadAsync | accept | LOW severity — `Task.Run(Load)` does not change the read path or parser; identical attack surface to existing sync Load(). |
| T-21-05 | D (Denial of service) | async void OnStartup | accept | LOW severity — try/catch with Log.Fatal around the await is the documented WPF pattern; existing `CrashHandler.InstallDispatcherHook` (configured before the await per App.xaml.cs:41) catches anything outside the new try/catch. No new dispatcher-level DoS vector. |
| T-21-06 | I (Information disclosure) | Background-thread _data write | accept | LOW severity — single reference-type field assignment is atomic in .NET; await-continuation provides happens-before edge for UI-thread reads (RESEARCH.md §6). No torn read. |
</threat_model>

<verification>
- `dotnet test --filter "FullyQualifiedName~JsonConnectionStoreAsyncLoadTests"` reports 3 passing.
- `dotnet test` (full suite) reports 0 failures.
- `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug` produces 0 errors.
- `dotnet build tests/Deskbridge.Benchmarks/Deskbridge.Benchmarks.csproj -c Release` produces 0 errors (D-06: sync Load still callable).
- Both commits (RED + GREEN) present in `git log`.
</verification>

<success_criteria>
- `Task LoadAsync()` shipped on `IConnectionStore` and `JsonConnectionStore`.
- `App.OnStartup` is async with awaited LoadAsync after BuildServiceProvider, before MigrateFromTermsrv.
- DI factory does not call `Load()`.
- All existing tests green; three new tests pass.
- Sync `Load()` retained for benchmarks/tests.
</success_criteria>

<output>
After completion, create `.planning/phases/21-performance-optimizations/21-03-SUMMARY.md` recording: the LoadAsync wrapper shape, the OnStartup async refactor, the order invariant (BuildSP → await Load → Migrate → pipeline → Show), the test class structure, and the two TDD commits.
</output>
