---
phase: 21
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Deskbridge/Views/ConnectionTreeControl.xaml
autonomous: true
requirements:
  - PERF-01
  - PERF-05

must_haves:
  truths:
    - "TreeView scrolls by pixel rather than per item, producing smooth wheel and trackpad scrolling (PERF-01)"
    - "Group expand/collapse, multi-select, drag-drop, context menu, and selection visuals continue to work unchanged"
    - "Each non-empty group row displays an inline (N) badge between the group name and the key icon (PERF-05; D-10, D-11, D-12)"
    - "Empty groups (ConnectionCount == 0) hide the badge entirely (Collapsed) — no (0) artifact (UI-SPEC locked)"
    - "Badge uses 12px Regular weight, TextFillColorSecondaryBrush, 4px left margin, VerticalAlignment=Center inside the locked 28px row"
    - "Badge count is recursive across all descendants, reusing the existing GroupTreeItemViewModel.ConnectionCount property"
  artifacts:
    - path: "src/Deskbridge/Views/ConnectionTreeControl.xaml"
      provides: "VirtualizingPanel.ScrollUnit=Pixel attribute on the ConnectionTree + group count badge TextBlock in the HierarchicalDataTemplate StackPanel"
      contains: 'VirtualizingPanel.ScrollUnit="Pixel"'
  key_links:
    - from: "ConnectionTreeControl.xaml ConnectionTree element"
      to: "WPF VirtualizingStackPanel scroll math"
      via: "VirtualizingPanel.ScrollUnit attached property"
      pattern: 'VirtualizingPanel\\.ScrollUnit="Pixel"'
    - from: "ConnectionTreeControl.xaml group HierarchicalDataTemplate"
      to: "GroupTreeItemViewModel.ConnectionCount"
      via: "data binding with StringFormat='({0})'"
      pattern: "Binding ConnectionCount, StringFormat"
---

<objective>
Two pure-XAML polish changes to `ConnectionTreeControl.xaml` — bundled because they share the same file and have no logic dependency on each other:

1. **PERF-01** — Add `VirtualizingPanel.ScrollUnit="Pixel"` to the ConnectionTree TreeView so wheel/trackpad gestures advance by pixel (per D-08).
2. **PERF-05** — Insert an inline `(N)` count badge between the group name and the key icon, hidden when zero (per D-10/D-11/D-12 and UI-SPEC visual contract).

Purpose: Both are rendering-only changes consuming existing VM properties; no code-behind, no ViewModel, no resource, no converter changes. Reuses the existing recursive `GroupTreeItemViewModel.ConnectionCount` getter.
Output: Two surgical XAML edits in one file, ~20 lines added total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-performance-optimizations/21-CONTEXT.md
@.planning/phases/21-performance-optimizations/21-RESEARCH.md
@.planning/phases/21-performance-optimizations/21-PATTERNS.md
@.planning/phases/21-performance-optimizations/21-UI-SPEC.md

<interfaces>
<!-- Existing TreeView opening tag in ConnectionTreeControl.xaml lines 339-354 (verbatim from PATTERNS.md) -->

```xml
<TreeView x:Name="ConnectionTree"
          ItemsSource="{Binding RootItems}"
          ItemContainerStyle="{StaticResource FullRowTreeViewItemStyle}"
          HorizontalContentAlignment="Stretch"
          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
          VirtualizingPanel.IsVirtualizing="True"
          VirtualizingPanel.VirtualizationMode="Recycling"
          ScrollViewer.CanContentScroll="True"
          BorderThickness="0"
          Background="Transparent"
          Padding="0"
          behaviors:TreeViewMultiSelectBehavior.EnableMultiSelect="True"
          behaviors:TreeViewDragDropBehavior.EnableDragDrop="True"
          PreviewKeyDown="TreeView_PreviewKeyDown"
          PreviewMouseDoubleClick="TreeView_PreviewMouseDoubleClick"
          PreviewMouseRightButtonDown="TreeView_PreviewMouseRightButtonDown">
```

<!-- Existing group HierarchicalDataTemplate StackPanel (verbatim from PATTERNS.md, lines ~357-385) -->
<!-- Layout: [FolderIcon] [Name TextBlock] [INSERT BADGE HERE] [Key SymbolIcon] -->

<!-- Existing GroupTreeItemViewModel.ConnectionCount (RESEARCH.md §4.2): -->
<!--   public int ConnectionCount => CountConnections(Children);  // recursive -->
<!-- Plain getter (NOT [ObservableProperty]). Re-evaluates on tree rebuild via BuildTree(). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add VirtualizingPanel.ScrollUnit="Pixel" to ConnectionTree opening tag</name>
  <files>src/Deskbridge/Views/ConnectionTreeControl.xaml</files>
  <read_first>
    - src/Deskbridge/Views/ConnectionTreeControl.xaml (lines 339-360 — TreeView opening tag, verify existing virtualization attribute order)
    - .planning/phases/21-performance-optimizations/21-RESEARCH.md §1 — confirms behavior change is rendering-only
    - .planning/phases/21-performance-optimizations/21-UI-SPEC.md "Tree Virtualization" table — locks the attribute name and value
  </read_first>
  <action>
    Open `src/Deskbridge/Views/ConnectionTreeControl.xaml`. Locate the `<TreeView x:Name="ConnectionTree" ...>` opening tag (around line 339). Insert `VirtualizingPanel.ScrollUnit="Pixel"` as a sibling attribute directly after `VirtualizingPanel.VirtualizationMode="Recycling"` and BEFORE `ScrollViewer.CanContentScroll="True"`. This groups all three `VirtualizingPanel.*` attributes together.

    Final block (4 attribute lines):
    ```xml
              VirtualizingPanel.IsVirtualizing="True"
              VirtualizingPanel.VirtualizationMode="Recycling"
              VirtualizingPanel.ScrollUnit="Pixel"
              ScrollViewer.CanContentScroll="True"
    ```

    Indentation: 10 spaces (matches existing siblings). Do NOT modify any other attribute on the TreeView. Do NOT add a namespace.

    Add a single-line phase-tag comment directly above the attribute group (per repo convention from PATTERNS.md "Shared Patterns" / "Phase-tagged comment style"):
    ```xml
              <!-- Phase 21 (PERF-01): per-pixel scrolling for smoother wheel/trackpad gestures. -->
    ```
  </action>
  <verify>
    <automated>cd /c/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo /clp:ErrorsOnly 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'VirtualizingPanel.ScrollUnit="Pixel"' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns exactly 1 match.
    - The match line appears between `VirtualizingPanel.VirtualizationMode="Recycling"` and `ScrollViewer.CanContentScroll="True"` (verify by `grep -B1 -A1 'ScrollUnit="Pixel"' src/Deskbridge/Views/ConnectionTreeControl.xaml`).
    - `grep -n 'Phase 21 (PERF-01)' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns at least 1.
    - `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo` exits 0 with no XAML parse errors.
  </acceptance_criteria>
  <done>
    `ScrollUnit="Pixel"` attribute and phase-tag comment present on ConnectionTree; project builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert recursive count badge in group HierarchicalDataTemplate</name>
  <files>src/Deskbridge/Views/ConnectionTreeControl.xaml</files>
  <read_first>
    - src/Deskbridge/Views/ConnectionTreeControl.xaml (lines 354-385 — full group HierarchicalDataTemplate; identify the Name TextBlock and the Key SymbolIcon as the bracketing siblings)
    - .planning/phases/21-performance-optimizations/21-UI-SPEC.md §"Visibility Binding (locked)" lines 142-157 — VERBATIM badge XAML (locked)
    - .planning/phases/21-performance-optimizations/21-PATTERNS.md §"ConnectionTreeControl.xaml" — confirms 4px gap rhythm, VerticalAlignment=Center, secondary brush
    - src/Deskbridge/ViewModels/GroupTreeItemViewModel.cs (lines 1-50 — confirm `ConnectionCount` is the public property name and is recursive via `CountConnections(Children)`)
  </read_first>
  <action>
    Open `src/Deskbridge/Views/ConnectionTreeControl.xaml`. Locate the group `HierarchicalDataTemplate` block (DataType `{x:Type vm:GroupTreeItemViewModel}`) and within its `StackPanel`, find the `<TextBlock Text="{Binding Name}" ... />` element followed by the `<ui:SymbolIcon Symbol="Key24" ... />` element.

    Insert the following TextBlock VERBATIM between those two elements (do not modify — UI-SPEC locked). Use the same indentation as the surrounding StackPanel children:

    ```xml
            <!-- Phase 21 (PERF-05): recursive connection-count badge. Hidden when count == 0
                 per UI-SPEC. Binds to GroupTreeItemViewModel.ConnectionCount; binding refreshes
                 on tree rebuild (BuildTree replaces RootItems). -->
            <TextBlock Text="{Binding ConnectionCount, StringFormat='({0})'}"
                       FontSize="12"
                       FontWeight="Regular"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="4,0,0,0"
                       VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ConnectionCount}" Value="0">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </TextBlock.Style>
                </TextBlock>
            </TextBlock>
    ```

    Do NOT modify the surrounding Name TextBlock or Key SymbolIcon. Do NOT introduce a converter, namespace, or static resource. The inline `<DataTrigger>` handles hide-when-zero per UI-SPEC.

    Visual sanity check before saving:
    - Final left-to-right child order in the StackPanel: folder `ui:SymbolIcon` → Name `TextBlock` → **NEW badge `TextBlock`** → key `ui:SymbolIcon`.
    - All four children retain `VerticalAlignment="Center"`.
    - Badge `Margin="4,0,0,0"` matches the key icon's existing margin (preserves the 4px gap rhythm).
    - Badge `Foreground` uses `TextFillColorSecondaryBrush`; `FontSize="12"`, `FontWeight="Regular"` per UI-SPEC.
  </action>
  <verify>
    <automated>cd /c/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Binding ConnectionCount, StringFormat='(\{0\})'" src/Deskbridge/Views/ConnectionTreeControl.xaml` returns exactly 1 match.
    - `grep -c '<DataTrigger Binding="{Binding ConnectionCount}" Value="0">' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns at least 1.
    - `grep -n 'Phase 21 (PERF-05)' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns at least 1.
    - Visual ordering: `grep -n -E 'Text="\{Binding Name\}"|Binding ConnectionCount, StringFormat|Symbol="Key24"' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns three matches with line numbers in this order — Name TextBlock < Badge TextBlock < Key SymbolIcon.
    - Badge styling preserved: `grep -c 'Foreground="\{DynamicResource TextFillColorSecondaryBrush\}"' src/Deskbridge/Views/ConnectionTreeControl.xaml` returns at least 2 (key icon + badge).
    - `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug --nologo` exits 0.
    - Only one file modified in this task: `git diff --stat src/Deskbridge/Views/ConnectionTreeControl.xaml` shows non-zero changes; no other source file modified by this task.
  </acceptance_criteria>
  <done>
    Badge TextBlock inserted between Name and Key icon with locked styling; data-triggered Collapsed-when-zero rule in place; phase-tag comment present; project builds clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure XAML rendering — no input crosses any new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | I (Information disclosure) | TreeView scroll math | accept | LOW severity — pixel-based scroll math is documented WPF behavior consuming data already shown by the tree. No new data egress. |
| T-21-07 | I (Information disclosure) | Group count badge binding | accept | LOW severity — binding consumes a managed `int` from a VM-internal computed property; `StringFormat='({0})'` does not pass user-influenced text to a format string. No new attacker-influenced inputs reach the binding pipeline. |
</threat_model>

<verification>
- `dotnet build src/Deskbridge/Deskbridge.csproj -c Debug` exits 0 with no XAML parse errors.
- Manual smoke (deferred to Plan 21-04): badge visible on populated groups, Collapsed on empty groups, scroll smooth at 500+.
- No code, ViewModel, or test changes — entire plan confined to one XAML file.
</verification>

<success_criteria>
- `VirtualizingPanel.ScrollUnit="Pixel"` present on ConnectionTree.
- Badge TextBlock binds `ConnectionCount` with `StringFormat='({0})'`, hides when zero via `DataTrigger`.
- Badge sits between Name and Key icon in group row.
- Project builds clean.
</success_criteria>

<output>
After completion, create `.planning/phases/21-performance-optimizations/21-01-SUMMARY.md` recording: ScrollUnit attribute insertion (PERF-01), badge XAML insertion (PERF-05), reuse of `GroupTreeItemViewModel.ConnectionCount`, no-converter / no-VM-change scope.
</output>
