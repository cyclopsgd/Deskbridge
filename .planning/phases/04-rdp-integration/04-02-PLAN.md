---
phase: 04-rdp-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - src/Deskbridge.Core/Interfaces/IProtocolHostFactory.cs
  - src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs
  - src/Deskbridge.Core/Exceptions/RdpConnectFailedException.cs
  - src/Deskbridge.Core/Pipeline/Stages/ResolveCredentialsStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/CreateHostStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/ConnectStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/UpdateRecentsStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/DisconnectStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/DisposeStage.cs
  - src/Deskbridge.Core/Pipeline/Stages/PublishClosedEventStage.cs
  - src/Deskbridge.Core/Services/ConnectionCoordinator.cs
  - src/Deskbridge.Core/Services/DisconnectReasonClassifier.cs
  - src/Deskbridge.Protocols.Rdp/RdpHostControl.cs
  - src/Deskbridge.Protocols.Rdp/RdpConnectionConfigurator.cs
  - src/Deskbridge.Protocols.Rdp/RdpProtocolHostFactory.cs
  - src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs
  - src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj
  - src/Deskbridge/App.xaml.cs
  - src/Deskbridge/MainWindow.xaml
  - src/Deskbridge/MainWindow.xaml.cs
  - src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs
  - tests/Deskbridge.Tests/Pipeline/ResolveCredentialsStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/CreateHostStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/ConnectStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/UpdateRecentsStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/DisconnectStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/DisposeStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/PublishClosedEventStageTests.cs
  - tests/Deskbridge.Tests/Pipeline/ConnectionPipelineIntegrationTests.cs
  - tests/Deskbridge.Tests/Coordinator/ConnectionCoordinatorTests.cs
  - tests/Deskbridge.Tests/Rdp/RdpHostControlShapeTests.cs
  - tests/Deskbridge.Tests/Rdp/AirspaceSwapperTests.cs
  - tests/Deskbridge.Tests/Rdp/ErrorIsolationTests.cs
  - tests/Deskbridge.Tests/Security/PasswordLeakTests.cs
  - tests/Deskbridge.Tests/Security/ErrorSanitizationTests.cs
autonomous: false
requirements:
  - RDP-01
  - RDP-02
  - RDP-04
  - RDP-05
  - RDP-07
  - RDP-08
  - RDP-09

must_haves:
  truths:
    - "Double-clicking a connection in the tree establishes a live RDP session displayed in the viewport"
    - "ConnectionCoordinator is the only entry point into IConnectionPipeline from UI code (no other production class in src/Deskbridge/ references IConnectionPipeline directly)"
    - "A COM error in RdpHostControl.ConnectAsync raises ErrorOccurred and propagates to ConnectionFailedEvent on the bus without crashing the application"
    - "Closing MainWindow while a live session is active runs the full RdpHostControl.Dispose sequence (both reflection leak fixes + FinalReleaseComObject) before the window closes"
    - "Rapidly dragging the window edge during a live session shows a bitmap snapshot (no black flicker) via WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE hooks"
    - "All 4 connect-pipeline stages execute in Resolve->Create->Connect->Recents order and publish ConnectionEstablishedEvent exactly once on success"
    - "All 3 disconnect-pipeline stages execute in Disconnect->Dispose->PublishClosed order and publish ConnectionClosedEvent exactly once"
    - "Password never appears in Serilog output (structured or otherwise) and never appears in ConnectionModel JSON serialization output"
  artifacts:
    - path: "src/Deskbridge.Protocols.Rdp/RdpHostControl.cs"
      provides: "IProtocolHost implementation wrapping AxMsRdpClient9NotSafeForScripting in WindowsFormsHost"
      contains: "public sealed class RdpHostControl : IProtocolHost"
    - path: "src/Deskbridge.Core/Pipeline/Stages/ResolveCredentialsStage.cs"
      provides: "Stage Order=100; walks credential inheritance per CredentialMode (Own/Inherit/Prompt)"
    - path: "src/Deskbridge.Core/Pipeline/Stages/CreateHostStage.cs"
      provides: "Stage Order=200; resolves IProtocolHostFactory by Protocol enum, sets ctx.Host"
    - path: "src/Deskbridge.Core/Pipeline/Stages/ConnectStage.cs"
      provides: "Stage Order=300; awaits host.ConnectAsync with 30s timeout; publishes ConnectionEstablishedEvent or ConnectionFailedEvent"
    - path: "src/Deskbridge.Core/Pipeline/Stages/UpdateRecentsStage.cs"
      provides: "Stage Order=400; sets LastUsedAt on ConnectionModel and persists via IConnectionStore"
    - path: "src/Deskbridge.Core/Pipeline/Stages/DisconnectStage.cs"
      provides: "Disconnect stage Order=100; awaits host.DisconnectAsync with 30s timeout"
    - path: "src/Deskbridge.Core/Pipeline/Stages/DisposeStage.cs"
      provides: "Disconnect stage Order=200; runs strict disposal sequence on IProtocolHost"
    - path: "src/Deskbridge.Core/Pipeline/Stages/PublishClosedEventStage.cs"
      provides: "Disconnect stage Order=300; publishes ConnectionClosedEvent"
    - path: "src/Deskbridge.Core/Services/ConnectionCoordinator.cs"
      provides: "Event-bus subscriber for ConnectionRequestedEvent; dispatches to STA; runs IConnectionPipeline.ConnectAsync"
    - path: "src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs"
      provides: "Singleton managing WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE bitmap snapshot + HideWithoutSnapshot token"
    - path: "src/Deskbridge.Protocols.Rdp/RdpConnectionConfigurator.cs"
      provides: "Static Apply(rdp, ctx) that sets all default RDP properties + per-connection overrides"
    - path: "src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs"
      provides: "Connect command publishes ConnectionRequestedEvent (replaces snackbar stub at line 650)"
  key_links:
    - from: "ConnectionTreeViewModel.Connect"
      to: "IEventBus.Publish(ConnectionRequestedEvent)"
      via: "_eventBus field injected via DI"
      pattern: "ConnectionRequestedEvent\\("
    - from: "ConnectionCoordinator constructor"
      to: "IEventBus.Subscribe ConnectionRequestedEvent"
      via: "subscription in constructor"
      pattern: "Subscribe.ConnectionRequestedEvent"
    - from: "ConnectionCoordinator.OnConnectionRequested"
      to: "_dispatcher.InvokeAsync (STA marshal per D-11)"
      via: "if !CheckAccess dispatch"
      pattern: "_dispatcher\\.InvokeAsync"
    - from: "CreateHostStage"
      to: "IProtocolHostFactory.Create(Protocol.Rdp)"
      via: "factory resolved from context.Connection.Protocol"
      pattern: "IProtocolHostFactory"
    - from: "RdpHostControl.ConnectAsync"
      to: "runtime Handle==0 guard"
      via: "handle check before GetOcx"
      pattern: "Handle == IntPtr\\.Zero"
    - from: "RdpHostControl.Dispose"
      to: "HwndSourceKeyboardInputSite._sinkElement + HostContainerInternal"
      via: "reflection null-out and dispose (WFH leak fixes)"
      pattern: "_sinkElement|HostContainerInternal"
    - from: "MainWindow.OnClosing"
      to: "_activeRdpHost?.Dispose()"
      via: "explicit dispose before base.OnClosing"
      pattern: "OnClosing.*Dispose"
    - from: "AirspaceSwapper.AttachToWindow"
      to: "HwndSource.AddHook (WM_ENTERSIZEMOVE=0x0231, WM_EXITSIZEMOVE=0x0232)"
      via: "WndProc hook"
      pattern: "WM_ENTERSIZEMOVE|0x0231"
---

<objective>
Build the production RDP integration: `RdpHostControl` (IProtocolHost impl with full disposal + both reflection-based WFH leak fixes), the 7 protocol-agnostic pipeline stages (4 connect + 3 disconnect per D-08/D-09), `ConnectionCoordinator` (event-bus bridge per D-11), `AirspaceSwapper` (PrintWindow-based bitmap-swap for drag/resize per D-13), single-host viewport swap per D-12, DI composition updates, and the tree double-click -> ConnectionRequestedEvent wiring.

Purpose: Deliver RDP-01, RDP-04, RDP-05, RDP-07, RDP-08, RDP-09. RDP-02 is partly covered by AxSiting from Plan 04-01; it is re-consumed here by the production RdpHostControl runtime handle check.

Output:
- 3 Core interfaces/exceptions: `IProtocolHostFactory`, `IConnectionCoordinator`, `RdpConnectFailedException`
- 7 pipeline stage files under `Deskbridge.Core/Pipeline/Stages/`
- 2 Core services: `ConnectionCoordinator`, `DisconnectReasonClassifier` (minimal stub; full form in Plan 04-03)
- 3 RDP files: `RdpHostControl`, `RdpConnectionConfigurator`, `RdpProtocolHostFactory`
- 1 airspace helper: `AirspaceSwapper`
- App.xaml.cs DI updates (register new stages/factory/coordinator/swapper)
- MainWindow.xaml mount point + MainWindow.xaml.cs OnClosing dispose + AirspaceSwapper.AttachToWindow
- ConnectionTreeViewModel.Connect rewired (publish ConnectionRequestedEvent)
- 14 test files (stages + coordinator + shape + airspace + error-isolation + security)

First task of Wave 0 re-runs the 4 Plan 04-01 gates before proceeding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-rdp-integration/04-CONTEXT.md
@.planning/phases/04-rdp-integration/04-RESEARCH.md
@.planning/phases/04-rdp-integration/04-VALIDATION.md
@.planning/phases/04-rdp-integration/04-01-PLAN.md
@RDP-ACTIVEX-PITFALLS.md
@WINFORMS-HOST-AIRSPACE.md
@WPF-UI-PITFALLS.md
@CLAUDE.md
@REFERENCE.md
@DESIGN.md
@src/Deskbridge.Core/Interfaces/IProtocolHost.cs
@src/Deskbridge.Core/Interfaces/IConnectionPipeline.cs
@src/Deskbridge.Core/Interfaces/IDisconnectPipeline.cs
@src/Deskbridge.Core/Interfaces/ICredentialService.cs
@src/Deskbridge.Core/Interfaces/IConnectionStore.cs
@src/Deskbridge.Core/Interfaces/IEventBus.cs
@src/Deskbridge.Core/Pipeline/ConnectionContext.cs
@src/Deskbridge.Core/Pipeline/DisconnectContext.cs
@src/Deskbridge.Core/Pipeline/ConnectionPipeline.cs
@src/Deskbridge.Core/Pipeline/DisconnectPipeline.cs
@src/Deskbridge.Core/Pipeline/PipelineResult.cs
@src/Deskbridge.Core/Events/ConnectionEvents.cs
@src/Deskbridge.Core/Models/ConnectionModel.cs
@src/Deskbridge.Core/Models/Enums.cs
@src/Deskbridge.Protocols.Rdp/AxSiting.cs
@src/Deskbridge/App.xaml.cs
@src/Deskbridge/MainWindow.xaml
@src/Deskbridge/MainWindow.xaml.cs
@src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs

<interfaces>
Contracts this plan CREATES for 04-03 and Phase 5+ to consume:

```csharp
// src/Deskbridge.Core/Interfaces/IProtocolHostFactory.cs
namespace Deskbridge.Core.Interfaces;
public interface IProtocolHostFactory
{
    IProtocolHost Create(Protocol protocol);
}

// src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs
namespace Deskbridge.Core.Interfaces;
public interface IConnectionCoordinator
{
    IProtocolHost? ActiveHost { get; }   // Phase 4: 0..1 host; Phase 5 replaces with collection
    event EventHandler<IProtocolHost>? HostMounted;
    event EventHandler<IProtocolHost>? HostUnmounted;
}

// src/Deskbridge.Core/Exceptions/RdpConnectFailedException.cs
namespace Deskbridge.Core.Exceptions;
public sealed class RdpConnectFailedException : Exception
{
    public int DiscReason { get; }
    public string HumanReason { get; }
    public RdpConnectFailedException(int discReason, string humanReason) : base(humanReason)
    { DiscReason = discReason; HumanReason = humanReason; }
}
```

Contracts this plan CONSUMES:
- `IProtocolHost` from src/Deskbridge.Core/Interfaces/IProtocolHost.cs
- `ConnectionContext` from src/Deskbridge.Core/Pipeline/ConnectionContext.cs
- Events from src/Deskbridge.Core/Events/ConnectionEvents.cs
- `AxSiting.SiteAndConfigure` from src/Deskbridge.Protocols.Rdp/AxSiting.cs (Plan 04-01)
</interfaces>
</context>

<tasks>

<task type="auto" wave="0">
  <name>Task 0.1: Re-run Plan 04-01 gates to confirm prototype still green</name>
  <files>(no files modified; verification task)</files>
  <read_first>
    - .planning/phases/04-rdp-integration/04-01-SUMMARY.md (gate outcomes + observed GDI delta)
    - .planning/phases/04-rdp-integration/04-VALIDATION.md lines 41-44 (gate IDs 4-01-G1..G4)
    - tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs (the 4 gate test methods)
  </read_first>
  <action>
    1. Confirm Plan 04-01 SUMMARY exists and gate 1 GDI delta was less than 50. If not, STOP.
    2. Set env vars `DESKBRIDGE_SMOKE_RDP_HOST` / `_USER` / `_PASS` (same values as Plan 04-01 validation).
    3. Run `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~Smoke"` — all 4 gates must pass.
    4. If any gate fails now that previously passed, do NOT proceed — something in the prototype regressed.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~Smoke"</automated>
  </verify>
  <acceptance_criteria>
    - Test runner output contains `Passed: 4` (or all Skip if env var not set — but user must have re-verified manually) for the Smoke filter
    - Zero test Failures
    - If any gate Failed, halt and escalate; do NOT proceed to Wave 1
  </acceptance_criteria>
  <done>All 4 smoke gates green OR user-verified after env-var setup.</done>
</task>

<task type="auto" tdd="true" wave="0">
  <name>Task 0.2: Test scaffolds for 7 stages + coordinator + shape + airspace + error-isolation + security (14 files)</name>
  <files>tests/Deskbridge.Tests/Pipeline/ResolveCredentialsStageTests.cs, tests/Deskbridge.Tests/Pipeline/CreateHostStageTests.cs, tests/Deskbridge.Tests/Pipeline/ConnectStageTests.cs, tests/Deskbridge.Tests/Pipeline/UpdateRecentsStageTests.cs, tests/Deskbridge.Tests/Pipeline/DisconnectStageTests.cs, tests/Deskbridge.Tests/Pipeline/DisposeStageTests.cs, tests/Deskbridge.Tests/Pipeline/PublishClosedEventStageTests.cs, tests/Deskbridge.Tests/Pipeline/ConnectionPipelineIntegrationTests.cs, tests/Deskbridge.Tests/Coordinator/ConnectionCoordinatorTests.cs, tests/Deskbridge.Tests/Rdp/RdpHostControlShapeTests.cs, tests/Deskbridge.Tests/Rdp/AirspaceSwapperTests.cs, tests/Deskbridge.Tests/Rdp/ErrorIsolationTests.cs, tests/Deskbridge.Tests/Security/PasswordLeakTests.cs, tests/Deskbridge.Tests/Security/ErrorSanitizationTests.cs</files>
  <read_first>
    - .planning/phases/04-rdp-integration/04-VALIDATION.md lines 74-96 (Wave 0 requirements list)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Validation Architecture lines 1071-1117
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Security Domain lines 1119-1157 (password leak + error sanitization threats)
    - tests/Deskbridge.Tests/ConnectionPipelineTests.cs (existing Phase 1 pipeline test — follow shape)
    - tests/Deskbridge.Tests/DisconnectPipelineTests.cs (existing Phase 1 disconnect test)
    - src/Deskbridge.Core/Interfaces/IConnectionPipeline.cs + IDisconnectPipeline.cs (stage contracts)
  </read_first>
  <behavior>
    Write tests for contracts that do not yet exist. Expected: TEST PROJECT FAILS TO COMPILE until Wave 1/2/3 tasks implement the referenced types. This is TDD RED. Tests mock `IProtocolHost`, `ICredentialService`, `IConnectionStore`, `IEventBus` via NSubstitute.

    **ResolveCredentialsStageTests** (6 tests):
    - Own_WithStoredCredential_AppliesToContext — mock `ICredentialService.GetForConnection` returns NetworkCredential, assert `ctx.ResolvedPassword` equals password, `ctx.Connection.Username` equals cred.UserName.
    - Own_WithoutStoredCredential_PublishesCredentialRequestedEvent_AndReturnsFailure.
    - Inherit_ResolvesViaCredentialService_ResolveInherited — mock returns cred; assert applied.
    - Inherit_NoMatch_PublishesRequestedEvent_AndReturnsFailure.
    - Prompt_PublishesRequestedEvent_AndReturnsFailure.
    - StageOrderIs100 — assert `stage.Order == 100` and `stage.Name == "ResolveCredentials"`.

    **CreateHostStageTests** (3 tests):
    - CreatesHost_ViaFactory_ForConnectionProtocol — mock `IProtocolHostFactory.Create(Protocol.Rdp)` returns mocked IProtocolHost; assert `ctx.Host` is the same reference.
    - SetsHostOnContext_BeforeReturningSuccess.
    - StageOrderIs200_AndName_IsCreateHost.

    **ConnectStageTests** (5 tests):
    - AwaitsConnectAsync_AndPublishesEstablishedEvent_OnSuccess — mock `IProtocolHost.ConnectAsync` returns completed Task; assert `bus.Received().Publish(Arg.Any ConnectionEstablishedEvent)`.
    - TimesOutAfter_Timeout_AndPublishesFailedEvent — stage must accept timeout via ctor (default 30s; test passes 100ms); mock ConnectAsync never completes.
    - RdpConnectFailedException_IsPublishedAsFailedEvent_WithHumanReason.
    - COMException_IsCaught_AndPublishedAsFailedEvent.
    - HostNotCreated_ReturnsFailure_WithoutPublishingEvent — ctx.Host == null.

    **UpdateRecentsStageTests** (2 tests):
    - SetsLastUsedAt_ToNowUtc_AndCallsStoreSave. NOTE: ConnectionModel currently has no LastUsedAt field. This stage should either (a) add a `LastUsedAt` nullable DateTime property to ConnectionModel in this task's scope, or (b) use `UpdatedAt`. Per 04-RESEARCH.md §Pattern lines 170-176 and D-08, add `LastUsedAt` to ConnectionModel (backward-compat: nullable). Test asserts LastUsedAt is non-null and recent.
    - StageOrderIs400.

    **DisconnectStageTests** (3 tests):
    - CallsHostDisconnectAsync_AndReturnsSuccess.
    - TimesOutAfter30Seconds_StillReturnsSuccess_WithWarning (disconnect failure is non-fatal — DisposeStage runs anyway).
    - StageOrderIs100.

    **DisposeStageTests** (3 tests):
    - CallsHostDispose.
    - ExceptionInDispose_IsCaughtAndLogged_DoesNotAbortPipeline.
    - StageOrderIs200.

    **PublishClosedEventStageTests** (2 tests):
    - PublishesConnectionClosedEvent_WithContextReason.
    - StageOrderIs300.

    **ConnectionPipelineIntegrationTests** (2 tests):
    - ConnectPipeline_RunsStages_InResolve100_Create200_Connect300_Recents400_Order — register all 4 stages in reverse, assert execution order via side effects on a `List string trace`.
    - DisconnectPipeline_RunsStages_InDisconnect100_Dispose200_Publish300_Order.

    **ConnectionCoordinatorTests** (4 tests):
    - Subscribes_ToConnectionRequestedEvent_OnConstruction — verify `_bus.Received().Subscribe(...)`.
    - Marshals_ToDispatcher_WhenPublished_FromWorkerThread — per 04-RESEARCH.md Open Question #3, ConnectionCoordinator accepts `Dispatcher` as ctor param (default: `Dispatcher.CurrentDispatcher`) so tests can inject. Test: publish from `Task.Run`, verify pipeline call marshaled via dispatcher.
    - CallsConnectPipeline_ConnectAsync_WithTheEventConnection.
    - DisposesAndReplaces_ActiveHost_OnSecondConnectionRequestedEvent — Phase 4 single-host replacement policy (04-RESEARCH.md Open Question #2).

    **RdpHostControlShapeTests** (4 tests — `[Collection("RDP-STA")]`):
    - Implements_IProtocolHost — reflection check.
    - Constructor_ThrowsInvalidOperationException_OnMtaThread — use `Thread` with `SetApartmentState(ApartmentState.MTA)` to run ctor, assert throws.
    - IsConnected_ReturnsFalse_BeforeConnect.
    - Host_ThrowsObjectDisposedException_AfterDispose.
    NOTE: These tests must NOT call ConnectAsync against a live RDP target; only shape checks.

    **AirspaceSwapperTests** (3 tests — `[Collection("RDP-STA")]`):
    - AttachToWindow_HooksWndProc — create a hidden Window, attach, send `WM_ENTERSIZEMOVE` (0x0231) via SendMessage, assert observable (expose `IsInSizeMove` internal property or raise `SizeMoveStarted` event).
    - RegisterHost_OnWM_ENTERSIZEMOVE_HidesHost_ShowsImage — WFH Visibility becomes Collapsed; Image Visibility becomes Visible. Snapshot Image.Source may be null in test env without a real ActiveX Handle — it is acceptable for the test to assert that `HideWithoutSnapshot` behavior in isolation and mark the PrintWindow path as `[SkippableFact]` with a live-RDP requirement.
    - HideWithoutSnapshot_Returns_DisposableToken_ThatRestoresVisibility — call, verify Visibility = Collapsed; dispose token; verify Visibility = Visible.

    **ErrorIsolationTests** (3 tests):
    - RdpHostControl_CatchesCOMException_InConnect_AndRaisesErrorOccurred_WithoutThrowing — RdpHostControl should expose an `internal virtual` cast point (or take a `Func GetOcx factory` for test override) so the COM layer can be stubbed. Stubbed cast throws COMException; verify `ErrorOccurred` fires and the test does not throw out of the call.
    - ConnectStage_DoesNotPropagate_COMException_AsStageFailure — returns `PipelineResult(false, ...)`, publishes ConnectionFailedEvent.
    - MainWindow_OnClosing_DoesNotThrow_EvenIfRdpHostControl_DisposeThrows.

    **PasswordLeakTests** (2 tests):
    - NotInLogs — configure Serilog with an in-memory sink (write a small test helper `InMemorySink : ILogEventSink`); invoke pipeline stages with `ctx.ResolvedPassword = "SecretSquirrel123!"`; assert the string "SecretSquirrel123!" never appears in any captured log event text.
    - NotInJson — serialize `ConnectionModel` via `System.Text.Json.JsonSerializer.Serialize`; assert output contains no case-insensitive match for `password` or `credential`. Also serialize a `ConnectionContext { Connection = model, ResolvedPassword = "SecretSquirrel123!" }` and assert `SecretSquirrel123!` is not present. If it IS, the stage author must apply `[JsonIgnore]` to `ResolvedPassword` in ConnectionContext.

    **ErrorSanitizationTests** (3 tests):
    - ConnectStage_LogsCOMException_As_TypeAndHResult_NotMessage — in-memory Serilog sink; cause a COMException with message containing `"password=Hunter2"`; assert log contains `"COMException"` and `HResult` but NOT `"Hunter2"` or `"password="`.
    - RdpHostControl_LogsCOMException_Similarly (same pattern applied to RdpHostControl's catch blocks).
    - Classifier_Describe_DoesNotLeakPassword — placeholder test for full classifier in Plan 04-03; trivial assertion that `DisconnectReasonClassifier.Describe(516, 0, null)` does not throw (in Plan 04-02 it can be a fallback `$"Disconnect reason {x}"`).

    Commit: `test(04-02): add pipeline stage + coordinator + shape + airspace + error isolation + security test scaffolds (14 files)`.
  </behavior>
  <action>
    For each file in `files`:
    1. Create the file at the exact path.
    2. Top of file: `namespace Deskbridge.Tests.{Subfolder};`
    3. For tests needing STA (AirspaceSwapperTests, RdpHostControlShapeTests): decorate with `[Collection("RDP-STA")]`.
    4. Use NSubstitute for mocks; FluentAssertions (`result.Should().Be(...)`) consistent with existing Phase 1/3 tests.
    5. Tests reference types that don't exist yet — compile will FAIL until Wave 1+ tasks create them. DO NOT stub implementations in this task.
    6. Test method names: `Given_When_Then` or `X_DoesY_WhenZ` consistent with existing tests.
    7. For security tests, create a small helper `tests/Deskbridge.Tests/Security/InMemorySink.cs` implementing `ILogEventSink` with a `List LogEvent Events { get; }` for capture.
    8. Commit: `test(04-02): add stage+coordinator+shape+security test scaffolds`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; ls tests/Deskbridge.Tests/Pipeline/ tests/Deskbridge.Tests/Coordinator/ tests/Deskbridge.Tests/Rdp/ tests/Deskbridge.Tests/Security/ | sort</automated>
  </verify>
  <acceptance_criteria>
    - All 14 scaffold files exist at documented paths
    - grep across files shows combined count of `[Fact]` + `[Theory]` + `[SkippableFact]` is greater than or equal to 42
    - Security test file `PasswordLeakTests.cs` contains literal strings: `SecretSquirrel123!`, `NotInLogs`, `NotInJson`
    - Error sanitization file contains literal `HResult` assertion and an explicit NOT-contains check for `password=`
    - `dotnet build tests/Deskbridge.Tests` fails with "type or namespace ... does not exist" errors (TDD RED — expected)
  </acceptance_criteria>
  <done>14 scaffold files exist with test bodies; test project compile-fails against missing types (TDD RED state).</done>
</task>

<task type="auto" wave="1">
  <name>Task 1.1: Core interfaces + exception (IProtocolHostFactory, IConnectionCoordinator, RdpConnectFailedException)</name>
  <files>src/Deskbridge.Core/Interfaces/IProtocolHostFactory.cs, src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs, src/Deskbridge.Core/Exceptions/RdpConnectFailedException.cs, src/Deskbridge.Core/Models/ConnectionModel.cs</files>
  <read_first>
    - src/Deskbridge.Core/Interfaces/IProtocolHost.cs (pattern for interface files)
    - src/Deskbridge.Core/Models/Enums.cs (Protocol enum)
    - src/Deskbridge.Core/Models/ConnectionModel.cs (to add LastUsedAt nullable DateTime)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 3 lines 400-446 (ConnectionCoordinator shape)
  </read_first>
  <action>
    1. Create `src/Deskbridge.Core/Interfaces/IProtocolHostFactory.cs`:
       ```csharp
       using Deskbridge.Core.Models;
       namespace Deskbridge.Core.Interfaces;
       public interface IProtocolHostFactory { IProtocolHost Create(Protocol protocol); }
       ```
    2. Create `src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs`:
       ```csharp
       namespace Deskbridge.Core.Interfaces;
       public interface IConnectionCoordinator
       {
           IProtocolHost? ActiveHost { get; }
           event EventHandler<IProtocolHost>? HostMounted;
           event EventHandler<IProtocolHost>? HostUnmounted;
       }
       ```
    3. Create `src/Deskbridge.Core/Exceptions/RdpConnectFailedException.cs`:
       ```csharp
       namespace Deskbridge.Core.Exceptions;
       public sealed class RdpConnectFailedException : Exception
       {
           public int DiscReason { get; }
           public string HumanReason { get; }
           public RdpConnectFailedException(int discReason, string humanReason) : base(humanReason)
           { DiscReason = discReason; HumanReason = humanReason; }
       }
       ```
    4. Add nullable `LastUsedAt` to `ConnectionModel`:
       ```csharp
       public DateTime? LastUsedAt { get; set; }
       ```
       Place after `UpdatedAt`. This unblocks `UpdateRecentsStage` tests.
    5. Commit: `feat(04-02): add core interfaces + RdpConnectFailedException + ConnectionModel.LastUsedAt`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge.Core/Deskbridge.Core.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - All 3 new files exist at exact paths
    - `IProtocolHostFactory.cs` contains `Create(Protocol protocol)` signature
    - `IConnectionCoordinator.cs` contains `IProtocolHost? ActiveHost`, `HostMounted`, `HostUnmounted`
    - `RdpConnectFailedException.cs` contains `DiscReason` and `HumanReason` properties
    - `ConnectionModel.cs` contains `public DateTime? LastUsedAt { get; set; }`
    - `Deskbridge.Core` builds with zero warnings
  </acceptance_criteria>
  <done>Interfaces + exception + model update compile; downstream tasks can reference them.</done>
</task>

<task type="auto" wave="2">
  <name>Task 2.1: RdpConnectionConfigurator + RdpHostControl (production IProtocolHost with WFH leak fixes) + minimal DisconnectReasonClassifier stub</name>
  <files>src/Deskbridge.Protocols.Rdp/RdpConnectionConfigurator.cs, src/Deskbridge.Protocols.Rdp/RdpHostControl.cs, src/Deskbridge.Protocols.Rdp/RdpProtocolHostFactory.cs, src/Deskbridge.Core/Services/DisconnectReasonClassifier.cs, src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj</files>
  <read_first>
    - src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs (reference for disposal + connect pattern)
    - src/Deskbridge.Protocols.Rdp/AxSiting.cs (reused for siting guard)
    - src/Deskbridge.Core/Interfaces/IProtocolHost.cs (the contract)
    - src/Deskbridge.Core/Pipeline/ConnectionContext.cs (ResolvedPassword note — do not log)
    - src/Deskbridge.Core/Exceptions/RdpConnectFailedException.cs (from Task 1.1)
    - RDP-ACTIVEX-PITFALLS.md §1 (siting), §3 (disposal), §4 (IMsTscNonScriptable), §6 (STA), §7 (event firing)
    - WINFORMS-HOST-AIRSPACE.md §leaks (both reflection fixes: HwndSourceKeyboardInputSite._sinkElement AND HostContainerInternal)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 1 lines 210-313 (RdpHostControl shape)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 4 lines 464-514 (configurator)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Example 1 lines 782-887 (disposal verbatim)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §Specifics (default property values)
  </read_first>
  <action>
    **A. Update `src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj`** — add PackageReference for logging abstractions:
    ```xml
    <ItemGroup>
      <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    </ItemGroup>
    ```
    Ensure `Directory.Packages.props` has a matching `PackageVersion` (current .NET 10 aligned version, typically `10.0.*`).

    **B. Create `src/Deskbridge.Protocols.Rdp/RdpConnectionConfigurator.cs`** — verbatim from 04-RESEARCH.md lines 488-514:
    - `namespace Deskbridge.Protocols.Rdp;`
    - `public static class RdpConnectionConfigurator`
    - `public static void Apply(AxMsRdpClient9NotSafeForScripting rdp, ConnectionContext ctx)` sets exactly these properties (values per 04-RESEARCH.md Pattern 4):
      - `rdp.Server = c.Hostname;`
      - `rdp.AdvancedSettings9.RDPPort = c.Port;`
      - `rdp.UserName = c.Username ?? "";`
      - `rdp.Domain = c.Domain ?? "";`
      - `rdp.AdvancedSettings9.SmartSizing = c.DisplaySettings?.SmartSizing ?? true;`
      - `if (c.DisplaySettings?.Width is > 0) rdp.DesktopWidth = c.DisplaySettings.Width.Value;`
      - `if (c.DisplaySettings?.Height is > 0) rdp.DesktopHeight = c.DisplaySettings.Height.Value;`
      - `rdp.ColorDepth = 32;`
      - `rdp.AdvancedSettings9.EnableCredSspSupport = true;`
      - `rdp.AdvancedSettings9.CachePersistenceActive = 0;`
      - `rdp.AdvancedSettings9.BitmapPeristence = 0;` (keep COM typelib misspelling)
      - `rdp.SecuredSettings3.KeyboardHookMode = 0;`
      - `rdp.AdvancedSettings9.GrabFocusOnConnect = false;`
      - `rdp.AdvancedSettings9.EnableAutoReconnect = false;` (D-04)
      - `rdp.AdvancedSettings9.ContainerHandledFullScreen = 0;`

    **C. Create `src/Deskbridge.Core/Services/DisconnectReasonClassifier.cs`** — minimal stub (Plan 04-03 fills it out):
    - `namespace Deskbridge.Core.Services;`
    - `public static class DisconnectReasonClassifier`
    - `public static string Describe(int discReason, int extendedReason, Func<uint, uint, string>? getErrorDescription = null)` — calls `getErrorDescription` if provided (wrapped in try/catch), else returns `$"Disconnect reason {discReason} (extended {extendedReason})"`. This keeps Core free of direct dependency on `AxMsRdpClient9` (protocol-agnostic per D-10).
    - Plan 04-03 adds the `DisconnectCategory` enum + `Classify` + `ShouldAutoRetry` to this same file.

    **D. Create `src/Deskbridge.Protocols.Rdp/RdpHostControl.cs`** — production `IProtocolHost`:
    1. `namespace Deskbridge.Protocols.Rdp;`
    2. `public sealed class RdpHostControl : IProtocolHost`
    3. Public surface:
       - `Guid ConnectionId { get; private set; }`
       - `bool IsConnected => _rdp?.Connected != 0;`
       - `event EventHandler<string>? ErrorOccurred;`
       - `Task ConnectAsync(ConnectionContext context);`
       - `Task DisconnectAsync();`
       - `void Dispose();`
       - `public WindowsFormsHost Host => _host ?? throw new ObjectDisposedException(nameof(RdpHostControl));`
       - `public event EventHandler<int>? DisconnectedAfterConnect;` — Plan 04-03 reconnect coordinator subscribes here
    4. Constructor `RdpHostControl(ILogger<RdpHostControl> logger)`: STA assertion, create `_host = new WindowsFormsHost { Background = Brushes.Black }`, `_rdp = new AxMsRdpClient9NotSafeForScripting()`.
    5. **ConnectAsync(ConnectionContext context)** body per 04-RESEARCH.md Pattern 1 lines 247-296:
       - ObjectDisposedException.ThrowIf + ConnectionId = context.Connection.Id
       - Runtime guard: `if (_rdp!.Handle == IntPtr.Zero) throw new InvalidOperationException("RdpHostControl.ConnectAsync called before host was added to the visual tree. See RDP-ACTIVEX-PITFALLS §1.");`
       - Create `_loginTcs = new TaskCompletionSource<bool>(TaskCreationOptions.RunContinuationsAsynchronously);`
       - Wire: `_rdp.OnLoginComplete += OnLoginComplete; _rdp.OnDisconnected += OnDisconnectedDuringConnect; _rdp.OnLogonError += OnLogonError;`
       - Call `RdpConnectionConfigurator.Apply(_rdp, context);`
       - Password set block (internal virtual cast point for test-stubbing per ErrorIsolationTests):
         ```csharp
         if (!string.IsNullOrEmpty(context.ResolvedPassword))
         {
             try
             {
                 var ocx = GetOcxForPasswordSet(_rdp) as IMsTscNonScriptable
                     ?? throw new InvalidOperationException("GetOcx() did not return IMsTscNonScriptable.");
                 ocx.ClearTextPassword = context.ResolvedPassword;
                 context.ResolvedPassword = null;  // Defense in depth (Security Checklist item 3)
             }
             catch (Exception ex) when (ex is COMException or InvalidCastException or NullReferenceException)
             {
                 ErrorOccurred?.Invoke(this, $"Password set failed: {ex.GetType().Name}");
                 throw;
             }
         }
         ```
       - Add `internal virtual object? GetOcxForPasswordSet(AxMsRdpClient9NotSafeForScripting rdp) => rdp.GetOcx();` so ErrorIsolationTests can override.
       - `try { _rdp.Connect(); } catch (Exception ex) when (ex is COMException or AxHost.InvalidActiveXStateException) { ErrorOccurred?.Invoke(this, $"Connect threw: {ex.GetType().Name} HResult=0x{(ex is COMException ce ? ce.HResult : ex.HResult):X8}"); _loginTcs.TrySetException(ex); }`
       - `return _loginTcs.Task;`
    6. **Event handlers**:
       - `OnLoginComplete(object? s, EventArgs e)` → unsubscribe `OnDisconnectedDuringConnect`, subscribe `OnDisconnectedAfterConnect`, `_loginTcs?.TrySetResult(true);`
       - `OnDisconnectedDuringConnect(object? s, IMsTscAxEvents_OnDisconnectedEvent e)` → `var human = DisconnectReasonClassifier.Describe(e.discReason, _rdp!.ExtendedDisconnectReason, (d, x) => _rdp.GetErrorDescription(d, x)); _loginTcs?.TrySetException(new RdpConnectFailedException(e.discReason, human));`
       - `OnDisconnectedAfterConnect(object? s, IMsTscAxEvents_OnDisconnectedEvent e)` → raise `DisconnectedAfterConnect?.Invoke(this, e.discReason);` (Plan 04-03 consumer handles reconnect logic)
       - `OnLogonError(object? s, IMsTscAxEvents_OnLogonErrorEvent e)` → `ErrorOccurred?.Invoke(this, $"LogonError: {e.lError}");`
    7. **DisconnectAsync**: per 04-RESEARCH.md §Example 1 lines 787-801 (poll Connected for 30s with `await Task.Delay(100)`; plain await — stays STA per D-11).
    8. **Dispose**: verbatim from 04-RESEARCH.md §Example 1 lines 803-880:
       - `DisconnectAsync().GetAwaiter().GetResult();`
       - Unsubscribe ALL wired events (OnLoginComplete, OnDisconnectedDuringConnect, OnDisconnectedAfterConnect, OnLogonError) inside try/catch.
       - **WFH leak fix #1** (lines 824-841): `HwndSourceKeyboardInputSite._sinkElement` + `_sink` reflection null-out, after `site.Unregister()`.
       - **WFH leak fix #2** (lines 844-853): `WindowsFormsHost.HostContainerInternal` Dispose via reflection.
       - `_host.Child = null;` + `Marshal.FinalReleaseComObject(_rdp.GetOcx())` (try/catch) + `_rdp.Dispose()` (try/catch for AccessViolationException, InvalidComObjectException, COMException).
       - `_rdp = null; _host.Dispose(); _host = null;`
    9. **Logging discipline** — NEVER:
       - Pass `context` or `ConnectionContext` as structured parameter with `{@...}`.
       - Log `context.ResolvedPassword`.
       - Log `ex.Message` or `ex.ToString()` from a `COMException`.
       Always:
       - `_logger.LogInformation("Connecting to {Hostname}:{Port} as {Username}", c.Hostname, c.Port, c.Username);`
       - `_logger.LogWarning("COM exception during {Phase}: {ExceptionType} HResult={HResult:X8}", phase, ex.GetType().Name, ex.HResult);`

    **E. Create `src/Deskbridge.Protocols.Rdp/RdpProtocolHostFactory.cs`**:
    - `namespace Deskbridge.Protocols.Rdp;`
    - `public sealed class RdpProtocolHostFactory : IProtocolHostFactory`
    - Constructor takes `IServiceProvider services` (for resolving `ILogger<RdpHostControl>`) or `ILoggerFactory factory`.
    - `public IProtocolHost Create(Protocol protocol)` throws `NotSupportedException` if protocol != Protocol.Rdp; otherwise returns `new RdpHostControl(_loggerFactory.CreateLogger<RdpHostControl>())`.

    Commit: `feat(04-02): add RdpHostControl + RdpConnectionConfigurator + RdpProtocolHostFactory + DisconnectReasonClassifier stub (RDP-01, RDP-04, RDP-07)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj -c Debug &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~RdpHostControlShapeTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Deskbridge.Protocols.Rdp/RdpHostControl.cs` contains literal strings: `public sealed class RdpHostControl : IProtocolHost`, `Handle == IntPtr.Zero`, `IMsTscNonScriptable`, `ClearTextPassword`, `context.ResolvedPassword = null`, `_sinkElement`, `HostContainerInternal`, `Marshal.FinalReleaseComObject`, `DisconnectedAfterConnect`
    - File `RdpConnectionConfigurator.cs` contains: `EnableAutoReconnect = false`, `CachePersistenceActive = 0`, `KeyboardHookMode = 0`, `BitmapPeristence = 0`, `GrabFocusOnConnect = false`, `EnableCredSspSupport = true`, `ColorDepth = 32`
    - File `RdpProtocolHostFactory.cs` contains: `public IProtocolHost Create(Protocol protocol)`
    - File `DisconnectReasonClassifier.cs` contains: `public static string Describe(int discReason, int extendedReason`
    - grep: NO occurrence of `ex.Message` or `ex.ToString()` as an argument to any `_logger.Log*` call in `src/Deskbridge.Protocols.Rdp/RdpHostControl.cs` (verify: `grep -E "_logger\\.(LogInformation|LogWarning|LogError).*ex\\.(Message|ToString)" src/Deskbridge.Protocols.Rdp/RdpHostControl.cs` returns zero lines)
    - grep: NO occurrence of `{@context}` or `{@connection}` (case-insensitive) in `src/Deskbridge.Protocols.Rdp/RdpHostControl.cs`
    - `dotnet build src/Deskbridge.Protocols.Rdp` succeeds with zero warnings
    - `dotnet test --filter "FullyQualifiedName~RdpHostControlShapeTests"` passes all 4 tests
  </acceptance_criteria>
  <done>RdpHostControl production implementation compiles, passes shape tests, security grep checks pass, no warnings.</done>
</task>

<task type="auto" wave="2">
  <name>Task 2.2: AirspaceSwapper (PrintWindow bitmap snapshot + HideWithoutSnapshot token)</name>
  <files>src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs</files>
  <read_first>
    - WINFORMS-HOST-AIRSPACE.md FULL DOCUMENT — especially §ResizeSnapshotManager reference implementation (copy verbatim)
    - WINFORMS-HOST-AIRSPACE.md §PerMonitorV2 DPI — why SmartSizing is the answer (informs no manual DPI scaling)
    - REFERENCE.md §UI Design §Drag/Resize Smoothness — pattern spec
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 5 lines 516-563 (API shape)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-13, §D-14 (locked decisions)
  </read_first>
  <action>
    1. Create `src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs`:
       - `namespace Deskbridge.Protocols.Rdp;`
       - `public sealed class AirspaceSwapper : IDisposable`
       - Public API (per 04-RESEARCH.md Pattern 5 lines 522-540):
         ```csharp
         public void AttachToWindow(Window window);
         public void RegisterHost(WindowsFormsHost host, Image overlay);
         public void UnregisterHost(WindowsFormsHost host);
         public IDisposable HideWithoutSnapshot(WindowsFormsHost host);
         public void Dispose();
         ```
    2. Internal implementation — copy verbatim from WINFORMS-HOST-AIRSPACE.md §ResizeSnapshotManager section:
       - P/Invoke `PrintWindow(IntPtr hwnd, IntPtr hdcBlt, uint flags)` with `PW_CLIENTONLY = 0x1`
       - P/Invoke `GetDC`, `CreateCompatibleDC`, `CreateCompatibleBitmap`, `SelectObject`, `DeleteDC`, `DeleteObject`, `ReleaseDC`
       - WM_ENTERSIZEMOVE = 0x0231, WM_EXITSIZEMOVE = 0x0232 constants
       - `AttachToWindow`: get `HwndSource.FromHwnd(new WindowInteropHelper(window).EnsureHandle())` and call `AddHook(WndProc)`
       - `WndProc(IntPtr hwnd, int msg, IntPtr wParam, IntPtr lParam, ref bool handled)`:
         - On WM_ENTERSIZEMOVE: for each registered host, capture PrintWindow bitmap, set `overlay.Source = bitmap`, `overlay.Visibility = Visible`, `host.Visibility = Collapsed`.
         - On WM_EXITSIZEMOVE: for each registered host, `overlay.Visibility = Collapsed`, `host.Visibility = Visible`, `overlay.Source = null` (release handle).
       - `HideWithoutSnapshot(host)`: set `host.Visibility = Collapsed`; return an `IDisposable` whose `Dispose` sets `host.Visibility = Visible` (Plan 04-03 reconnect overlay uses this).
    3. Dispose releases any held HwndSource hooks; unregister all hosts; null out bitmaps.
    4. Thread-safety: all calls must happen on UI thread (STA). Assert dispatcher access in `RegisterHost` / `UnregisterHost` / `HideWithoutSnapshot`.
    5. Commit: `feat(04-02): add AirspaceSwapper (PrintWindow bitmap snapshot + hide-only mode) (RDP-09)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj -c Debug &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~AirspaceSwapperTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs` exists
    - File contains literal strings: `public sealed class AirspaceSwapper`, `AttachToWindow`, `RegisterHost`, `UnregisterHost`, `HideWithoutSnapshot`, `PrintWindow`, `WM_ENTERSIZEMOVE`, `0x0231`, `WM_EXITSIZEMOVE`, `0x0232`, `PW_CLIENTONLY`
    - `dotnet test --filter "FullyQualifiedName~AirspaceSwapperTests"` passes (HideWithoutSnapshot + WndProc hook tests green; PrintWindow snapshot test may SkipIf without a live Handle — acceptable)
    - Zero build warnings
  </acceptance_criteria>
  <done>AirspaceSwapper compiles and passes available unit tests; PrintWindow path is at least reachable and documented.</done>
</task>

<task type="auto" wave="3">
  <name>Task 3.1: Seven pipeline stages (Resolve, CreateHost, Connect, UpdateRecents, Disconnect, Dispose, PublishClosed)</name>
  <files>src/Deskbridge.Core/Pipeline/Stages/ResolveCredentialsStage.cs, src/Deskbridge.Core/Pipeline/Stages/CreateHostStage.cs, src/Deskbridge.Core/Pipeline/Stages/ConnectStage.cs, src/Deskbridge.Core/Pipeline/Stages/UpdateRecentsStage.cs, src/Deskbridge.Core/Pipeline/Stages/DisconnectStage.cs, src/Deskbridge.Core/Pipeline/Stages/DisposeStage.cs, src/Deskbridge.Core/Pipeline/Stages/PublishClosedEventStage.cs</files>
  <read_first>
    - src/Deskbridge.Core/Interfaces/IConnectionPipeline.cs + IDisconnectPipeline.cs (IConnectionPipelineStage / IDisconnectPipelineStage contracts)
    - src/Deskbridge.Core/Pipeline/PipelineResult.cs
    - src/Deskbridge.Core/Pipeline/ConnectionContext.cs + DisconnectContext.cs
    - src/Deskbridge.Core/Events/ConnectionEvents.cs
    - src/Deskbridge.Core/Interfaces/ICredentialService.cs + IConnectionStore.cs
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 2 lines 346-398 (ConnectStage shape verbatim)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Example 3 lines 957-1013 (ResolveCredentialsStage shape verbatim)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-08 (4 connect stages), §D-09 (3 disconnect stages), §D-10 (protocol-agnostic), §D-11 (plain await)
    - Existing Phase 1 tests: tests/Deskbridge.Tests/ConnectionPipelineTests.cs, DisconnectPipelineTests.cs
  </read_first>
  <action>
    All stages: `namespace Deskbridge.Core.Pipeline.Stages;`. Protocol-agnostic — no reference to AxMSTSCLib/RdpHostControl (D-10).

    **A. ResolveCredentialsStage.cs** — verbatim from 04-RESEARCH.md §Example 3 lines 957-1013:
    - `public sealed class ResolveCredentialsStage(ICredentialService creds, IConnectionStore store, IEventBus bus, ILogger<ResolveCredentialsStage> log) : IConnectionPipelineStage`
    - `Name => "ResolveCredentials"`, `Order => 100`
    - `ExecuteAsync` switches on `c.CredentialMode`:
      - `Own`: calls `creds.GetForConnection(c)`; if null → publish CredentialRequestedEvent, return `PipelineResult(false, "Credentials not found (own)")`; else `ApplyCredential(ctx, cred)` → success.
      - `Inherit`: calls `creds.ResolveInherited(c, store)`; null → publish + fail; else apply.
      - `Prompt` / default: publish + fail.
    - `ApplyCredential(ConnectionContext ctx, NetworkCredential cred)`: `ctx.Connection.Username = cred.UserName; ctx.Connection.Domain = cred.Domain; ctx.ResolvedPassword = cred.Password;`
    - Logging: `log.LogInformation("Credentials resolved for {Hostname}", c.Hostname);` — NEVER log password.

    **B. CreateHostStage.cs**:
    - `public sealed class CreateHostStage(IProtocolHostFactory factory) : IConnectionPipelineStage`
    - `Name => "CreateHost"`, `Order => 200`
    - `ExecuteAsync(ctx)`: `ctx.Host = factory.Create(ctx.Connection.Protocol); return Task.FromResult(new PipelineResult(true));`

    **C. ConnectStage.cs** — verbatim from 04-RESEARCH.md §Pattern 2 lines 353-393, with configurable timeout:
    - `public sealed class ConnectStage(IEventBus bus, ILogger<ConnectStage> logger, TimeSpan? connectTimeout = null) : IConnectionPipelineStage`
    - `Name => "Connect"`, `Order => 300`
    - `_timeout = connectTimeout ?? TimeSpan.FromSeconds(30);`
    - Body: check `ctx.Host is null` → fail without publishing; linked CTS with `_timeout.CancelAfter`; `var connectTask = ctx.Host.ConnectAsync(ctx);` + `Task.WhenAny` against `Task.Delay(Timeout.Infinite, cts.Token)`; if delay wins → TimeoutException; else `await connectTask;` to propagate.
    - Success → `bus.Publish(new ConnectionEstablishedEvent(ctx.Connection, ctx.Host));` → return `PipelineResult(true)`.
    - Catch `RdpConnectFailedException` → publish `ConnectionFailedEvent(ctx.Connection, ex.HumanReason, ex)`, return `PipelineResult(false, ex.HumanReason)`.
    - Catch `TimeoutException | COMException | OperationCanceledException` → `logger.LogWarning(ex, "Connect failed for {Hostname}", ctx.Connection.Hostname);` — note: this is the ONE place `ex` is passed to logger as the exception parameter (Serilog prints just the type + message, NOT the whole object). But to be extra safe, change to: `logger.LogWarning("Connect failed for {Hostname}: {ExceptionType} HResult={HResult:X8}", ctx.Connection.Hostname, ex.GetType().Name, ex.HResult);` — this is safer per the project's ErrorSanitization test.

    **D. UpdateRecentsStage.cs**:
    - `public sealed class UpdateRecentsStage(IConnectionStore store) : IConnectionPipelineStage`
    - `Name => "UpdateRecents"`, `Order => 400`
    - `ExecuteAsync(ctx)`: `ctx.Connection.LastUsedAt = DateTime.UtcNow; store.Save(ctx.Connection); return Task.FromResult(new PipelineResult(true));`

    **E. DisconnectStage.cs**:
    - `public sealed class DisconnectStage(ILogger<DisconnectStage> logger) : IDisconnectPipelineStage`
    - `Name => "Disconnect"`, `Order => 100`
    - `ExecuteAsync(ctx)`: if `ctx.Host is null` return success; else try to `await ctx.Host.DisconnectAsync()` with 30s linked CTS; on timeout → `logger.LogWarning("Disconnect timed out for {Hostname}", ctx.Connection.Hostname);` — still return `PipelineResult(true)` so DisposeStage runs.

    **F. DisposeStage.cs**:
    - `public sealed class DisposeStage(ILogger<DisposeStage> logger) : IDisconnectPipelineStage`
    - `Name => "Dispose"`, `Order => 200`
    - `ExecuteAsync(ctx)`: `try { ctx.Host?.Dispose(); } catch (Exception ex) { logger.LogError("Dispose threw: {ExceptionType} HResult={HResult:X8}", ex.GetType().Name, ex.HResult); } return Task.FromResult(new PipelineResult(true));`

    **G. PublishClosedEventStage.cs**:
    - `public sealed class PublishClosedEventStage(IEventBus bus) : IDisconnectPipelineStage`
    - `Name => "PublishClosed"`, `Order => 300`
    - `ExecuteAsync(ctx)`: `bus.Publish(new ConnectionClosedEvent(ctx.Connection, ctx.Reason)); return Task.FromResult(new PipelineResult(true));`

    Commit: `feat(04-02): add 4 connect stages and 3 disconnect stages (RDP-05, RDP-08)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~Stage|FullyQualifiedName~ConnectionPipelineIntegrationTests"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 stage files exist at documented paths
    - Each file contains `public int Order => NNN;` matching the expected value (100, 200, 300, 400, 100, 200, 300)
    - ConnectStage file contains literal string `Task.WhenAny` and does NOT contain `ex.Message` as a log argument
    - ResolveCredentialsStage file does NOT contain literal `ResolvedPassword` in any `log.Log*` call (grep-verifiable)
    - All stage test filters pass (Resolve, CreateHost, Connect, UpdateRecents, Disconnect, Dispose, PublishClosed, ConnectionPipelineIntegration)
    - Zero build warnings in `Deskbridge.Core`
  </acceptance_criteria>
  <done>All 7 stages compile, all stage unit + integration tests pass, logging discipline enforced by grep.</done>
</task>

<task type="auto" wave="3">
  <name>Task 3.2: ConnectionCoordinator (event-bus bridge, STA dispatcher marshal, single-host policy)</name>
  <files>src/Deskbridge.Core/Services/ConnectionCoordinator.cs</files>
  <read_first>
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 3 lines 400-446 (verbatim shape)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md Open Question #2 lines 1055-1057 (single-host replacement policy)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md Open Question #3 lines 1059-1061 (Dispatcher ctor param for testability)
    - src/Deskbridge.Core/Interfaces/IConnectionPipeline.cs + IDisconnectPipeline.cs
    - src/Deskbridge.Core/Interfaces/IEventBus.cs
    - src/Deskbridge.Core/Events/ConnectionEvents.cs (ConnectionRequestedEvent, ConnectionEstablishedEvent, ConnectionClosedEvent)
    - RDP-ACTIVEX-PITFALLS.md §6 (STA thread affinity)
  </read_first>
  <action>
    Create `src/Deskbridge.Core/Services/ConnectionCoordinator.cs`:
    - `namespace Deskbridge.Core.Services;`
    - `public sealed class ConnectionCoordinator : IConnectionCoordinator, IDisposable`
    - Fields: `IEventBus _bus, IConnectionPipeline _connect, IDisconnectPipeline _disconnect, Dispatcher _dispatcher, ILogger<ConnectionCoordinator> _logger`, `IProtocolHost? _active`.
    - Constructor `ConnectionCoordinator(IEventBus bus, IConnectionPipeline connect, IDisconnectPipeline disconnect, ILogger<ConnectionCoordinator> logger, Dispatcher? dispatcher = null)`:
      - `_dispatcher = dispatcher ?? Dispatcher.CurrentDispatcher;`
      - `_bus.Subscribe<ConnectionRequestedEvent>(this, OnConnectionRequested);`
      - `_bus.Subscribe<ConnectionEstablishedEvent>(this, OnConnectionEstablished);`
      - `_bus.Subscribe<ConnectionClosedEvent>(this, OnConnectionClosed);`
    - `public IProtocolHost? ActiveHost => _active;`
    - `public event EventHandler<IProtocolHost>? HostMounted;`
    - `public event EventHandler<IProtocolHost>? HostUnmounted;`
    - `private void OnConnectionRequested(ConnectionRequestedEvent evt)`:
      - Marshal to STA: `if (!_dispatcher.CheckAccess()) { _dispatcher.InvokeAsync(() => OnConnectionRequested(evt)); return; }`
      - Single-host replacement policy (Open Question #2): if `_active != null` → dispatch disconnect first: `_ = _disconnect.DisconnectAsync(new DisconnectContext { Connection = <model from _active>, Host = _active, Reason = DisconnectReason.UserInitiated });` — note: _active's ConnectionId gives us the model via _connect? Simpler: store the ConnectionModel alongside the host. Update field to `(IProtocolHost host, ConnectionModel model)? _active;`
      - `_ = _connect.ConnectAsync(evt.Connection);` — fire and forget; failures go to ConnectionFailedEvent via ConnectStage.
    - `private void OnConnectionEstablished(ConnectionEstablishedEvent evt)`:
      - `_active = (evt.Host, evt.Connection);`
      - `HostMounted?.Invoke(this, evt.Host);`
    - `private void OnConnectionClosed(ConnectionClosedEvent evt)`:
      - if `_active?.model.Id == evt.Connection.Id` → raise `HostUnmounted?.Invoke(this, _active.Value.host); _active = null;`
    - `public void Dispose()`:
      - Unsubscribe all three events.
      - If `_active != null` → synchronously run disconnect pipeline (guarded by try/catch).

    Update `IConnectionCoordinator` interface (src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs) already has `ActiveHost { get; }` — no change needed.

    Commit: `feat(04-02): add ConnectionCoordinator (event-bus bridge, STA marshal, single-host policy) (RDP-05)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~ConnectionCoordinatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Deskbridge.Core/Services/ConnectionCoordinator.cs` exists
    - Contains literal strings: `public sealed class ConnectionCoordinator`, `Subscribe<ConnectionRequestedEvent>`, `_dispatcher.InvokeAsync`, `CheckAccess`, `DisposesAndReplaces_ActiveHost` (or equivalent replacement logic)
    - Constructor accepts optional `Dispatcher? dispatcher` parameter
    - `ConnectionCoordinatorTests` all 4 tests pass
    - Zero build warnings
  </acceptance_criteria>
  <done>ConnectionCoordinator compiles, subscribes on construction, marshals to STA, enforces single-host replacement policy, all tests pass.</done>
</task>

<task type="auto" wave="4">
  <name>Task 4.1: DI composition (App.xaml.cs) — register factory, stages, coordinator, airspace swapper</name>
  <files>src/Deskbridge/App.xaml.cs</files>
  <read_first>
    - src/Deskbridge/App.xaml.cs (existing DI registrations)
    - src/Deskbridge.Core/Services/ConnectionCoordinator.cs (from Task 3.2)
    - src/Deskbridge.Protocols.Rdp/RdpProtocolHostFactory.cs (from Task 2.1)
    - src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs (from Task 2.2)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 3 lines 444-445 (coordinator is Singleton)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-10 (IProtocolHostFactory registered)
  </read_first>
  <action>
    Modify `src/Deskbridge/App.xaml.cs` `ConfigureServices` method. Add AFTER the existing `IConnectionPipeline`/`IDisconnectPipeline` registrations:

    ```csharp
    // ---- Phase 4: RDP integration ----
    // Pipeline stages (connect)
    services.AddSingleton<IConnectionPipelineStage, ResolveCredentialsStage>();
    services.AddSingleton<IConnectionPipelineStage, CreateHostStage>();
    services.AddSingleton<IConnectionPipelineStage, ConnectStage>();
    services.AddSingleton<IConnectionPipelineStage, UpdateRecentsStage>();

    // Pipeline stages (disconnect)
    services.AddSingleton<IDisconnectPipelineStage, DisconnectStage>();
    services.AddSingleton<IDisconnectPipelineStage, DisposeStage>();
    services.AddSingleton<IDisconnectPipelineStage, PublishClosedEventStage>();

    // Protocol host factory + RDP impl
    services.AddSingleton<IProtocolHostFactory, RdpProtocolHostFactory>();

    // Connection coordinator (event-bus bridge)
    services.AddSingleton<IConnectionCoordinator, ConnectionCoordinator>();

    // Airspace swapper
    services.AddSingleton<AirspaceSwapper>();

    // Microsoft.Extensions.Logging backing (Serilog adapter)
    services.AddLogging(b => b.AddSerilog(dispose: false));
    ```

    After BuildServiceProvider, ALSO register stages into the pipelines:
    ```csharp
    // Wire stages into pipelines (must happen after container build)
    var connectPipeline = _serviceProvider.GetRequiredService<IConnectionPipeline>();
    foreach (var stage in _serviceProvider.GetServices<IConnectionPipelineStage>())
        connectPipeline.AddStage(stage);

    var disconnectPipeline = _serviceProvider.GetRequiredService<IDisconnectPipeline>();
    foreach (var stage in _serviceProvider.GetServices<IDisconnectPipelineStage>())
        disconnectPipeline.AddStage(stage);

    // Resolve ConnectionCoordinator eagerly so it subscribes to the event bus
    _ = _serviceProvider.GetRequiredService<IConnectionCoordinator>();
    ```

    Add `using` directives: `Deskbridge.Core.Pipeline.Stages;`, `Deskbridge.Protocols.Rdp;`, `Serilog;`, `Microsoft.Extensions.Logging;`.

    **Serilog setup** — Phase 3 added Serilog to Deskbridge.Core but may not have configured it in the host. Add at the top of `OnStartup` (before `ConfigureServices`):
    ```csharp
    var logPath = System.IO.Path.Combine(
        Environment.GetFolderPath(Environment.SpecialFolder.ApplicationData),
        "Deskbridge", "logs", "deskbridge-.log");
    Log.Logger = new LoggerConfiguration()
        .MinimumLevel.Information()
        .WriteTo.File(logPath, rollingInterval: RollingInterval.Day, retainedFileCountLimit: 5)
        .CreateLogger();
    ```
    (Phase 6's LOG-01 will refine this; Phase 4 just needs a baseline so RdpHostControl logging works.)

    Add Serilog.Extensions.Logging package to Deskbridge.csproj + Directory.Packages.props if not present: `<PackageVersion Include="Serilog.Extensions.Logging" />`, `<PackageReference Include="Serilog.Extensions.Logging" />`.

    Commit: `feat(04-02): wire RDP pipeline stages, factory, coordinator, airspace into DI`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build Deskbridge.sln -c Debug &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~DiCompositionTests"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Deskbridge/App.xaml.cs` contains literal strings: `ResolveCredentialsStage`, `CreateHostStage`, `ConnectStage`, `UpdateRecentsStage`, `DisconnectStage`, `DisposeStage`, `PublishClosedEventStage`, `IProtocolHostFactory`, `RdpProtocolHostFactory`, `IConnectionCoordinator`, `ConnectionCoordinator`, `AirspaceSwapper`, `connectPipeline.AddStage`, `disconnectPipeline.AddStage`, `GetRequiredService<IConnectionCoordinator>`
    - Build succeeds with zero warnings
    - Existing `DiCompositionTests` still pass (update test if it asserts registered service count; should include the new singletons)
  </acceptance_criteria>
  <done>DI composition wired; all services resolvable; coordinator subscribes at startup; existing DI tests still green.</done>
</task>

<task type="auto" wave="4">
  <name>Task 4.2: MainWindow viewport integration (mount host, OnClosing dispose, AirspaceSwapper.AttachToWindow)</name>
  <files>src/Deskbridge/MainWindow.xaml, src/Deskbridge/MainWindow.xaml.cs</files>
  <read_first>
    - src/Deskbridge/MainWindow.xaml (current viewport region: `<Border Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}">` — line 251-262)
    - src/Deskbridge/MainWindow.xaml.cs (current constructor — DI already injects services)
    - src/Deskbridge.Core/Interfaces/IConnectionCoordinator.cs (HostMounted / HostUnmounted events)
    - src/Deskbridge.Protocols.Rdp/AirspaceSwapper.cs (API: AttachToWindow, RegisterHost, UnregisterHost)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-12 (single live host swap in viewport)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pitfall 2 lines 710-730 (dotnet/wpf #10171 — explicit WFH dispose in OnClosing)
    - WINFORMS-HOST-AIRSPACE.md (airspace invariants; WPF-UI overlays must NOT overlap viewport)
  </read_first>
  <action>
    **A. MainWindow.xaml** — name the viewport Grid and add a sibling Image for the snapshot:
    1. Find `<Border Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}">` (line 251). Replace its content with a `Grid x:Name="ViewportGrid"`:
       ```xml
       <Border Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}">
         <Grid x:Name="ViewportGrid">
           <!-- Empty-state placeholder (kept) -->
           <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
               Visibility="{Binding HasNoTabs, Converter={StaticResource BoolToVisibility}}">
             <TextBlock Text="Deskbridge" FontSize="20" FontWeight="SemiBold"
                 Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                 HorizontalAlignment="Center" />
             <TextBlock Text="Ctrl+N to create a connection" FontSize="14"
                 Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                 HorizontalAlignment="Center" Margin="0,8,0,0" />
           </StackPanel>
           <!-- Snapshot Image for drag/resize (RDP-09, D-13). Collapsed by default; AirspaceSwapper toggles. -->
           <Image x:Name="ViewportSnapshot" Stretch="Fill" Visibility="Collapsed" />
           <!-- RDP host mounts here via code-behind Children.Add -->
         </Grid>
       </Border>
       ```
    2. Confirm no WPF element is z-order above `ViewportGrid` within the viewport cell (airspace invariant). `SnackbarPresenter` / `ContentDialogHost` already span RowSpan=3 — they can cover viewport BUT they are collapsed by default. This is acceptable; Phase 6 will add AirspaceSwapper.HideWithoutSnapshot when a ContentDialog opens over a live session.

    **B. MainWindow.xaml.cs**:
    1. Inject `IConnectionCoordinator coordinator` and `AirspaceSwapper airspace` via the constructor; store as private fields.
    2. Add `private IProtocolHost? _activeRdpHost;`
    3. Subscribe to coordinator events in constructor:
       ```csharp
       coordinator.HostMounted += OnHostMounted;
       coordinator.HostUnmounted += OnHostUnmounted;
       ```
    4. `protected override void OnSourceInitialized(EventArgs e)`:
       ```csharp
       base.OnSourceInitialized(e);
       _airspace.AttachToWindow(this);
       ```
    5. `private void OnHostMounted(object? sender, IProtocolHost host)`:
       ```csharp
       if (host is not Deskbridge.Protocols.Rdp.RdpHostControl rdp) return;
       Dispatcher.Invoke(() =>
       {
           ViewportGrid.Children.Add(rdp.Host);
           _airspace.RegisterHost(rdp.Host, ViewportSnapshot);
           _activeRdpHost = rdp;
       });
       ```
    6. `private void OnHostUnmounted(object? sender, IProtocolHost host)`:
       ```csharp
       if (host is not Deskbridge.Protocols.Rdp.RdpHostControl rdp) return;
       Dispatcher.Invoke(() =>
       {
           _airspace.UnregisterHost(rdp.Host);
           ViewportGrid.Children.Remove(rdp.Host);
           if (ReferenceEquals(_activeRdpHost, rdp)) _activeRdpHost = null;
       });
       ```
    7. `protected override void OnClosing(CancelEventArgs e)` (dotnet/wpf #10171 mitigation per 04-RESEARCH.md Pitfall 2):
       ```csharp
       try { _activeRdpHost?.Dispose(); } catch { /* log via Serilog; must not throw */ }
       _activeRdpHost = null;
       base.OnClosing(e);
       ```
    8. Ensure `using System.ComponentModel;` for CancelEventArgs.

    Commit: `feat(04-02): wire MainWindow viewport to ConnectionCoordinator + AirspaceSwapper (RDP-09, D-12)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build Deskbridge.sln -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `src/Deskbridge/MainWindow.xaml` contains literal strings `x:Name="ViewportGrid"` and `x:Name="ViewportSnapshot"`
    - `src/Deskbridge/MainWindow.xaml.cs` contains: `_airspace.AttachToWindow(this)`, `HostMounted`, `HostUnmounted`, `OnClosing`, `_activeRdpHost?.Dispose`, `ViewportGrid.Children.Add(rdp.Host)`, `RegisterHost`, `UnregisterHost`
    - Build succeeds with zero warnings
    - Application starts without crashing (manual: `dotnet run --project src/Deskbridge`). NOT part of automated gate; user verifies in visual checkpoint 4.3.
  </acceptance_criteria>
  <done>MainWindow wires coordinator events to viewport mount/unmount; AirspaceSwapper attached; OnClosing dispose path honored.</done>
</task>

<task type="auto" wave="4">
  <name>Task 4.3: Connect trigger wiring (ConnectionTreeViewModel.Connect publishes ConnectionRequestedEvent)</name>
  <files>src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs</files>
  <read_first>
    - src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs lines 1-70 + 650-666 (current stub — snackbar "will be wired in Phase 4")
    - src/Deskbridge.Core/Interfaces/IEventBus.cs
    - src/Deskbridge.Core/Events/ConnectionEvents.cs (ConnectionRequestedEvent shape)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Pattern 3 lines 447-462 (5-line replacement snippet)
  </read_first>
  <action>
    1. Add `IEventBus` as a constructor parameter to `ConnectionTreeViewModel` (after `IServiceProvider serviceProvider`):
       ```csharp
       private readonly IEventBus _eventBus;
       public ConnectionTreeViewModel(
           IConnectionStore connectionStore,
           IConnectionQuery connectionQuery,
           ICredentialService credentialService,
           IContentDialogService contentDialogService,
           ISnackbarService snackbarService,
           IServiceProvider serviceProvider,
           IEventBus eventBus)   // NEW
       {
           // ... existing assignments ...
           _eventBus = eventBus;
       }
       ```
    2. Add using directive: `using Deskbridge.Core.Events;`
    3. Replace the stub at lines 651-665. New body per 04-RESEARCH.md Pattern 3:
       ```csharp
       [RelayCommand]
       private void Connect(TreeItemViewModel? item = null)
       {
           var target = item as ConnectionTreeItemViewModel
                        ?? PrimarySelectedItem as ConnectionTreeItemViewModel;
           if (target is null) return;
           var model = _connectionStore.GetById(target.Id);
           if (model is null) return;
           _eventBus.Publish(new ConnectionRequestedEvent(model));
           // ConnectionCoordinator handles the rest (STA marshal + pipeline run + ConnectionFailedEvent on failure).
       }
       ```
    4. DI already registers `IEventBus` as singleton (from Phase 1). No App.xaml.cs change needed unless constructor injection validation catches the new parameter — if so, ensure `ConnectionTreeViewModel` is registered as singleton (it already is per STATE.md Phase 03 decision).

    Commit: `feat(04-02): wire tree Connect command to ConnectionRequestedEvent (RDP-05)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build Deskbridge.sln -c Debug &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~ViewModels"</automated>
  </verify>
  <acceptance_criteria>
    - `src/Deskbridge/ViewModels/ConnectionTreeViewModel.cs` contains literal strings: `IEventBus eventBus`, `_eventBus = eventBus;`, `_eventBus.Publish(new ConnectionRequestedEvent`
    - Does NOT contain literal string `_snackbarService.Show("Not yet connected"` (old stub removed)
    - grep across `src/Deskbridge/` shows NO production class other than `ConnectionCoordinator` (which lives in Deskbridge.Core) references `IConnectionPipeline` directly — run `grep -r "IConnectionPipeline" src/Deskbridge/ --include="*.cs" | grep -v "App.xaml.cs"` returns empty (App.xaml.cs only registers, does not call `.ConnectAsync`)
    - Build zero warnings
    - Existing ViewModel tests still pass
  </acceptance_criteria>
  <done>Connect command publishes event; Coordinator is the only connect path from UI; invariant test (grep) green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" wave="5">
  <name>Task 5.1: Live RDP end-to-end + drag/resize visual checkpoint</name>
  <files>(human verification — no files modified)</files>
  <action>Pause execution. Present the what-built / how-to-verify content below to the user and wait for the resume-signal. On approval, record the outcome in the plan SUMMARY.</action>
  <verify>User confirms via resume-signal; outcome recorded in SUMMARY.md.</verify>
  <done>User approved or the failure is documented and escalated.</done>
  <what-built>
    Full pipeline wired: double-click a connection in the tree → ConnectionRequestedEvent published → ConnectionCoordinator marshals to STA → IConnectionPipeline runs Resolve → CreateHost → Connect → UpdateRecents → RdpHostControl mounts in ViewportGrid via HostMounted event → AirspaceSwapper attached to main window handles WM_ENTERSIZEMOVE/WM_EXITSIZEMOVE → closing MainWindow disposes the active host cleanly.
  </what-built>
  <how-to-verify>
    1. Ensure at least one ConnectionModel exists with credentials stored: open the app, create a connection with hostname = a real reachable RDP target (e.g. localhost or a throwaway VM), set CredentialMode = Own, enter username + password (Phase 3 stores them via Windows Credential Manager).
    2. Restart the app (`dotnet run --project src/Deskbridge`).
    3. Double-click the connection in the tree.
    4. **Verify RDP-01, RDP-04, RDP-05, RDP-08**: A live RDP session renders in the viewport within 10s. No black rectangle flicker. Sign in succeeds (you see the remote desktop).
    5. **Verify RDP-09**: Rapidly drag the window edge (resize) and move the window around the screen. Instead of a black rectangle where the RDP would be, you see a smooth bitmap snapshot. On release, the live RDP view restores.
    6. **Verify dispose path**: Close the MainWindow while connected. The process should exit cleanly (no crash, no hung process in Task Manager).
    7. **Verify per-connection error isolation (RDP-07)**: Edit the connection to use an invalid hostname. Double-click it. A `ConnectionFailedEvent` should fire (visible as a Serilog log line in `%AppData%/Deskbridge/logs/`). The app stays alive; you can double-click a valid connection afterward.
    8. **Verify ErrorOccurred sanitization**: Check the Serilog log for the failed attempt — it should contain the `COMException` type name and HResult, but NEVER contain your password.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 verifications pass, or describe the specific failure (e.g. "fail - drag causes black flicker").
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI thread ↔ RDP COM control | STA thread affinity required. Background event delivery from IEventBus must marshal to Dispatcher (D-11 / Pitfall 8). |
| RdpHostControl ↔ IMsTscNonScriptable | Plain-text password crosses here. Must be wiped immediately after (defense in depth). |
| Serilog sinks ↔ file system | Structured log output — any `{@...}` parameter serializes entire objects, must never include ConnectionContext. |
| System.Text.Json ↔ connections.json | ConnectionModel serialized here — must not carry a password field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-LOG | Information Disclosure | ResolveCredentialsStage, ConnectStage, RdpHostControl, ConnectionCoordinator — any `log.Log*` call site | mitigate | Stages log only individual safe fields (`{Hostname}`, `{Username}`, `{Port}`). NEVER pass ConnectionContext as structured `{@Context}`. Grep-verifiable in acceptance criteria. PasswordLeakTests.NotInLogs validates via in-memory Serilog sink. |
| T-04-EXC | Information Disclosure | RdpHostControl.ConnectAsync, ConnectStage.ExecuteAsync, DisposeStage | mitigate | Every COM catch logs `ex.GetType().Name` and `ex.HResult:X8` ONLY. Never `ex.Message` or `ex.ToString()`. ErrorSanitizationTests enforces via regression test with a COMException whose message contains `"password=Hunter2"`. |
| T-04-CRED | Information Disclosure | RdpHostControl.ConnectAsync | mitigate | `context.ResolvedPassword = null;` immediately after `IMsTscNonScriptable.ClearTextPassword` assignment succeeds (Security Checklist item 3). Reduces crash-dump exposure window. |
| T-04-JSN | Tampering / Disclosure | ConnectionModel serialization to connections.json | mitigate | ConnectionModel has NO Password field by design (verified in Phase 3). PasswordLeakTests.NotInJson regression test asserts no `password`/`credential` substring in Serialize output. If ConnectionContext is ever serialized, `[JsonIgnore]` on ResolvedPassword is enforced by the same test. |
| T-04-GDI | Denial of Service | RdpHostControl.Dispose | mitigate | Plan 04-01 Gate 1 (20-cycle baseline) remains authoritative. RdpHostControl.Dispose includes BOTH reflection-based WFH leak fixes verbatim from WINFORMS-HOST-AIRSPACE.md. Task 0.1 of this plan re-runs the gate to confirm no regression. |
| T-04-ISO | Denial of Service | RdpHostControl — every COM call site, ConnectStage catch blocks, MainWindow.OnClosing | mitigate | Every COM call in RdpHostControl wrapped in try/catch. `ErrorOccurred` event raised with sanitized typed info. ConnectStage catches `COMException`, publishes `ConnectionFailedEvent`, returns `PipelineResult(false, …)` — pipeline continues for other connections. MainWindow.OnClosing catches Dispose exceptions. ErrorIsolationTests enforces. |

</threat_model>

<verification>
- Build: `dotnet build Deskbridge.sln -c Debug` — zero warnings
- Unit tests (all, excluding live-RDP smoke): `dotnet test --filter "FullyQualifiedName!~Smoke"` — all pass
- Security gate: PasswordLeakTests.NotInLogs + NotInJson + ErrorSanitizationTests all green
- Architectural invariant: `grep -r "IConnectionPipeline" src/Deskbridge/ --include="*.cs" | grep -v "App.xaml.cs" | grep -v "//"` returns empty (coordinator is only connect path)
- Grep discipline: no `ex.Message` / `ex.ToString()` in logger calls within `src/Deskbridge.Protocols.Rdp/` and `src/Deskbridge.Core/Pipeline/Stages/` (except ConnectStage's single bounded use which is the structured-logging-safe variant)
- Live E2E visual checkpoint 5.1 verifies RDP-01, RDP-04, RDP-05, RDP-07, RDP-08, RDP-09 simultaneously against a real target
</verification>

<success_criteria>
- RDP-01: RdpHostControl implements IProtocolHost correctly (RdpHostControlShapeTests green)
- RDP-02: AxSiting from Plan 04-01 + RdpHostControl runtime Handle==0 guard both honor siting order (SitingGuardTests + Gate 3 already passed)
- RDP-04: Strict dispose sequence including both reflection-based WFH leak fixes (DisposeStageTests + Plan 04-01 Gate 1)
- RDP-05: Connect/disconnect lifecycle goes through IConnectionPipeline/IDisconnectPipeline; ConnectionCoordinator is the only UI-side invoker (grep invariant + ConnectionCoordinatorTests green)
- RDP-07: COM errors caught, ErrorOccurred raised, process alive (ErrorIsolationTests + checkpoint 5.1 step 7)
- RDP-08: Pipeline stages publish correct events (ConnectStageTests, PublishClosedEventStageTests, ConnectionPipelineIntegrationTests green)
- RDP-09: AirspaceSwapper hooked to MainWindow; snapshot swap on drag/resize (AirspaceSwapperTests + visual checkpoint 5.1 step 5)
- Security: T-04-LOG, T-04-EXC, T-04-CRED, T-04-JSN, T-04-GDI, T-04-ISO all mitigated with regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-rdp-integration/04-02-SUMMARY.md` documenting:
- All files created (19 production + 14 test files)
- Decision coverage: D-01 already done in 04-01; this plan covers D-04 (partial — configurator only; loop in 04-03), D-08, D-09, D-10, D-11, D-12, D-13, D-14
- Observed outcome of live-RDP visual checkpoint 5.1
- Any deviations from plan (e.g. if logger signatures differ, if test mocking approach was adjusted)
- Open items handed off to Plan 04-03: `DisconnectedAfterConnect` event consumer (reconnect coordinator), full `DisconnectReasonClassifier` table, reconnect overlay ViewModel + XAML
</output>
