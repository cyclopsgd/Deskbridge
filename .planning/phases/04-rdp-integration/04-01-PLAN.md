---
phase: 04-rdp-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs
  - tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs
  - tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs
  - tests/Deskbridge.Tests/Deskbridge.Tests.csproj
  - src/Deskbridge.Protocols.Rdp/AxSiting.cs
  - src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs
autonomous: false
requirements:
  - RDP-02
  - RDP-03
  - RDP-04
  - RDP-07
user_setup:
  - service: live-rdp-target
    why: "Plan 4-01 gates 1, 2, 4 require a real RDP endpoint (enable Remote Desktop on localhost, or spin up a throwaway VM)"
    env_vars:
      - name: DESKBRIDGE_SMOKE_RDP_HOST
        source: "Hostname or IP of a reachable RDP target (e.g. 127.0.0.1 with Remote Desktop enabled)"
      - name: DESKBRIDGE_SMOKE_RDP_USER
        source: "Valid username on that target"
      - name: DESKBRIDGE_SMOKE_RDP_PASS
        source: "Valid password for that username"
    dashboard_config:
      - task: "Enable Remote Desktop on the target machine"
        location: "Settings > System > Remote Desktop (or VM equivalent)"

must_haves:
  truths:
    - "Running the 20-cycle GDI gate against a live RDP target shows GDI handle delta < 50 vs baseline"
    - "IMsTscNonScriptable cast + ClearTextPassword succeeds against a real RDP target and OnLoginComplete fires"
    - "AxSiting.SiteAndConfigure throws InvalidOperationException with the text 'not sited' when Handle == 0 after presumed siting"
    - "An intentional bad hostname triggers OnDisconnected with a discReason code and does NOT crash the test host process"
  artifacts:
    - path: "tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs"
      provides: "xUnit v3 collection fixture that forces ApartmentState.STA on the worker thread"
      contains: "[CollectionDefinition(\"RDP-STA\")]"
    - path: "tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs"
      provides: "Four gate tests (Gate1..Gate4) each SkippableFact when DESKBRIDGE_SMOKE_RDP_HOST unset"
      contains: "[Collection(\"RDP-STA\")]"
    - path: "tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs"
      provides: "Pure-unit test for AxSiting helper (no live RDP needed)"
    - path: "src/Deskbridge.Protocols.Rdp/AxSiting.cs"
      provides: "SiteAndConfigure<T>(Panel viewport, WindowsFormsHost host, T rdp, Action<T> configure) where T : AxHost"
    - path: "src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs"
      provides: "Bare-minimum connect/dispose prototype used by smoke tests only. Namespaced under Prototype to signal throwaway scope."
  key_links:
    - from: "RdpHostControlSmokeTests.Gate1_20CycleGdiBaseline"
      to: "GetGuiResources P/Invoke (user32.dll)"
      via: "GdiHandleCount helper method"
      pattern: "GetGuiResources.*GR_GDIOBJECTS"
    - from: "RdpHostControlSmokeTests.Gate3_SitingOrderGuard"
      to: "src/Deskbridge.Protocols.Rdp/AxSiting.cs SiteAndConfigure"
      via: "direct call, no visual tree"
      pattern: "AxSiting\\.SiteAndConfigure"
    - from: "RdpSmokeHost.ConnectAsync"
      to: "AxMsRdpClient9NotSafeForScripting.GetOcx() as IMsTscNonScriptable"
      via: "ClearTextPassword setter"
      pattern: "GetOcx\\(\\) as IMsTscNonScriptable"
---

<objective>
Prove the four dangerous primitives of RDP ActiveX lifecycle work correctly in isolation before building the production pipeline integration. This plan is a smoke-test gate, not production code. Its only artifacts are two throwaway C# files (AxSiting helper, RdpSmokeHost prototype) and a test scaffold. Gates 1-4 from 04-CONTEXT.md §D-02 MUST all pass before Plan 04-02 begins.

Purpose: Resolve the phase-level blocker logged in STATE.md ("RDP ActiveX integration concentrates highest risk -- research recommends a minimal connect/dispose prototype before full implementation") and de-risk the .NET 10 framework leak uncertainty (dotnet/winforms #13499 PR #13532 status) by measuring against the actual shipped SDK.

Output:
- `tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs` — STA collection fixture
- `tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs` — Gate1..Gate4 tests
- `tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs` — pure unit for AxSiting
- `src/Deskbridge.Protocols.Rdp/AxSiting.cs` — public siting-order guard helper (kept beyond this plan; 04-02 consumes it)
- `src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs` — throwaway host; 04-02 will NOT depend on it
- Xunit.SkippableFact package reference added to `Deskbridge.Tests.csproj` (xUnit v3 does not ship Skip attribute out of the box)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-rdp-integration/04-CONTEXT.md
@.planning/phases/04-rdp-integration/04-RESEARCH.md
@.planning/phases/04-rdp-integration/04-VALIDATION.md
@RDP-ACTIVEX-PITFALLS.md
@WINFORMS-HOST-AIRSPACE.md
@CLAUDE.md
@REFERENCE.md
@src/Deskbridge.Core/Interfaces/IProtocolHost.cs
@src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj
@tests/Deskbridge.Tests/Deskbridge.Tests.csproj
@tests/Deskbridge.Tests/GlobalUsings.cs

<interfaces>
<!-- IProtocolHost (existing, src/Deskbridge.Core/Interfaces/IProtocolHost.cs) — prototype does NOT need to implement this; 04-02 does. -->
<!-- Prototype exposes a minimal surface used only by smoke tests. -->

From the interop DLLs in src/Deskbridge.Protocols.Rdp/Interop/:
- `AxMSTSCLib.AxMsRdpClient9NotSafeForScripting` — AxHost-derived ActiveX wrapper (classic aximp)
- `MSTSCLib.IMsTscNonScriptable` — COM interface exposed via `rdp.GetOcx()`; has `ClearTextPassword { set; }`
- `MSTSCLib.IMsTscAxEvents_OnDisconnectedEvent` — event args with `int discReason`
- `AxHost.InvalidActiveXStateException` — thrown when property set before siting

Expected RdpSmokeHost shape (prototype only — 04-02 replaces with real RdpHostControl):
```csharp
namespace Deskbridge.Protocols.Rdp.Prototype;
public sealed class RdpSmokeHost : IDisposable
{
    public WindowsFormsHost Host { get; }
    public event EventHandler<string>? ErrorOccurred;
    public Task ConnectAsync(string hostname, int port, string? username, string? domain, string? password, CancellationToken ct);
    public Task DisconnectAsync();
    public void Dispose();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true" wave="0">
  <name>Task 0.1: Add Xunit.SkippableFact package + STA collection fixture + AxSiting unit test scaffold</name>
  <files>tests/Deskbridge.Tests/Deskbridge.Tests.csproj, tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs, tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs, Directory.Packages.props</files>
  <read_first>
    - tests/Deskbridge.Tests/Deskbridge.Tests.csproj (current package refs, no Skip support)
    - tests/Deskbridge.Tests/GlobalUsings.cs (existing globals)
    - Directory.Packages.props (Central Package Management — all version pins live here)
    - .planning/phases/04-rdp-integration/04-VALIDATION.md (Wave 0 list, lines 74-96)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Wave 0 Gaps (last bullet: "A `[SkippableFact]` attribute... xunit v3 doesn't ship one out of the box; Xunit.SkippableFact does")
    - RDP-ACTIVEX-PITFALLS.md §6 (STA thread affinity — why fixture must force apartment state)
  </read_first>
  <behavior>
    - Test 1: SitingGuardTests.Throws_When_HandleIsZero_AfterAddingToUnrootedPanel — creates a `System.Windows.Controls.Grid` (not added to any Window), a `WindowsFormsHost`, a fake/stubbed `AxHost` subclass returning `Handle == IntPtr.Zero`; calls `AxSiting.SiteAndConfigure` and asserts `InvalidOperationException` whose Message contains "not sited".
    - Test 2: SitingGuardTests.DoesNotThrow_When_HandleIsNonZero — fake `AxHost` returning `Handle == (IntPtr)0x1234`; call succeeds and `configure` delegate is invoked exactly once.
    - Tests must execute on STA (use `[Collection("RDP-STA")]`).
  </behavior>
  <action>
    1. Add `Xunit.SkippableFact` to `Directory.Packages.props` `ItemGroup`: `<PackageVersion Include="Xunit.SkippableFact" Version="1.5.23" />`. Add `<PackageReference Include="Xunit.SkippableFact" />` to `tests/Deskbridge.Tests/Deskbridge.Tests.csproj`.
    2. Create `tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs` with:
       ```csharp
       namespace Deskbridge.Tests.Fixtures;
       [CollectionDefinition("RDP-STA")]
       public class RdpStaCollection : ICollectionFixture<StaCollectionFixture> { }
       public sealed class StaCollectionFixture
       {
           public StaCollectionFixture()
           {
               // xUnit v3 workers default to MTA on Windows; RDP tests need STA (RDP-ACTIVEX-PITFALLS §6).
               // Asserted at entry — real STA enforcement happens via xUnit's [STAThread] fixture pattern.
               // Note: xUnit v3 requires `[Fact(... )] [STAThreadAttribute]`-equivalent via Xunit.Sdk.
               // For this scaffold we assert and document; if a test detects MTA, it skips via Skip.If.
           }
           public bool IsSta() => Thread.CurrentThread.GetApartmentState() == ApartmentState.STA;
       }
       ```
       CRITICAL: In the `[Fact]` or `[SkippableFact]` body, call `Skip.IfNot(fixture.IsSta(), "STA required")` at the start. Do NOT attempt to change the existing thread's apartment state at runtime (not allowed after first call).
    3. Create `tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs` with two tests described in Behavior. Use a private test-only `FakeAxHost : AxHost` stub that overrides `Handle` return via a constructor-injected IntPtr. If `AxHost`'s `Handle` is non-overridable, use reflection to set `System.Windows.Forms.Control.Handle`'s backing `_window` (acceptable — this is test-only scaffolding).
    4. Include `global using Xunit.SkippableFact;` or full namespace at test usage sites. Keep `GlobalUsings.cs` minimal — add `global using Skip = Xunit.Skip;` only if multiple test files need it.
    5. Commit: `test(04-01): add STA fixture and AxSiting unit test scaffold`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build tests/Deskbridge.Tests/Deskbridge.Tests.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - `Directory.Packages.props` contains `<PackageVersion Include="Xunit.SkippableFact"`
    - `tests/Deskbridge.Tests/Deskbridge.Tests.csproj` contains `<PackageReference Include="Xunit.SkippableFact" />`
    - File `tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs` exists and contains the literal string `[CollectionDefinition("RDP-STA")]`
    - File `tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs` exists and contains the literal string `SitingGuardTests`, `Throws_When_HandleIsZero`, `[Collection("RDP-STA")]`
    - `dotnet build tests/Deskbridge.Tests` exits with code 0
    - Running `dotnet test tests/Deskbridge.Tests --filter "FullyQualifiedName~SitingGuardTests"` fails with a COMPILE error about AxSiting not existing (correct — Task 1.1 creates it). Alternatively the tests can be disabled with `[Fact(Skip="AxSiting not yet implemented")]` in this commit and re-enabled in Task 1.1.
  </acceptance_criteria>
  <done>Test project builds; scaffold files exist at the documented paths; AxSiting references compile-fail as expected (Task 1.1 implements it).</done>
</task>

<task type="auto" tdd="true" wave="0">
  <name>Task 0.2: Smoke test scaffold (RdpHostControlSmokeTests with 4 Gate tests)</name>
  <files>tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs</files>
  <read_first>
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Example 2 (lines 889-953, verbatim gate test shape)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-02 (the four gate criteria)
    - .planning/phases/04-rdp-integration/04-VALIDATION.md lines 41-44 (per-task Gate1..Gate4 map)
    - RDP-ACTIVEX-PITFALLS.md §2 (GDI handle thresholds + GetGuiResources pattern)
    - tests/Deskbridge.Tests/Fixtures/StaCollectionFixture.cs (from Task 0.1)
  </read_first>
  <behavior>
    - Gate1_20CycleGdiBaseline_HandleCountReturnsToBaseline — Skip.IfNot(env var DESKBRIDGE_SMOKE_RDP_HOST set), GC before + after, loop 20 iterations of `RunOneCycleAsync()` (create RdpSmokeHost, connect, wait OnLoginComplete, disconnect, wait OnDisconnected, dispose), assert `GetGuiResources(proc, 0) - baseline &lt; 50`.
    - Gate2_IMsTscNonScriptable_PasswordSetSucceeds — Skip.IfNot env, create RdpSmokeHost, connect with valid creds from env, assert `OnLoginComplete` fires within 30s.
    - Gate3_SitingOrderGuard_ThrowsWhenConfigureBeforeSite — no env required, pure test: create unrooted Grid + WFH + new `AxMsRdpClient9NotSafeForScripting()`, call `AxSiting.SiteAndConfigure(grid, host, rdp, r =&gt; r.Server = "ignored")`, assert `InvalidOperationException` with "not sited".
    - Gate4_ComError_DoesNotTearDownApp — Skip.IfNot env, connect with DESKBRIDGE_SMOKE_RDP_HOST but set Server = "definitely-not-a-host.invalid", assert OnDisconnected fires (discReason != 0), `ErrorOccurred` event raised, then Dispose() runs cleanly, `Assert.True(Process.GetCurrentProcess().HasExited == false)`.
  </behavior>
  <action>
    Create `tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs` per the shape in 04-RESEARCH.md §Example 2 (lines 889-953). Literal requirements:
    1. File starts with `namespace Deskbridge.Tests.Smoke;`
    2. Class decorated `[Collection("RDP-STA")]` (ties to fixture from Task 0.1).
    3. Class name `RdpHostControlSmokeTests`.
    4. Four test methods named exactly: `Gate1_20CycleGdiBaseline_HandleCountReturnsToBaseline`, `Gate2_IMsTscNonScriptable_PasswordSetSucceeds`, `Gate3_SitingOrderGuard_ThrowsWhenConfigureBeforeSite`, `Gate4_ComError_DoesNotTearDownApp`.
    5. P/Invoke for `GetGuiResources` (see 04-RESEARCH.md line 946-949 verbatim):
       ```csharp
       [DllImport("user32.dll")] private static extern int GetGuiResources(IntPtr hProcess, int uiFlags);
       private static int GetGdi() => GetGuiResources(Process.GetCurrentProcess().Handle, 0 /*GR_GDIOBJECTS*/);
       ```
    6. Env-var helpers: `private static readonly string? Host = Environment.GetEnvironmentVariable("DESKBRIDGE_SMOKE_RDP_HOST");` plus `_User`, `_Pass`, `_Domain` (optional).
    7. `RunOneCycleAsync()` private helper: instantiates `RdpSmokeHost`, adds it to a hidden `Window` (required for siting), calls `ConnectAsync`, awaits a `TaskCompletionSource` wired to the smoke host's `OnLoginComplete`-equivalent, then `DisconnectAsync`, then `Dispose`. The hidden Window must `Show()` then `Close()` each cycle (closing the window triggers the full WFH dispose path — this is exactly what we want to measure).
    8. Leave Gate1..Gate4 bodies referencing `RdpSmokeHost` type that Task 1.2 will create. Expect COMPILE ERROR at this point — that is OK. Mark tests `[SkippableFact(Skip = "Prototype not implemented")]` temporarily, or use `#if` guard; OR defer the full body until Task 1.2. **Chosen approach**: write the full bodies now with `[SkippableFact]` but REFERENCE `Deskbridge.Protocols.Rdp.Prototype.RdpSmokeHost`; compile fails until Task 1.2 lands. Tests are written once and do not need editing after prototype is in place.
    9. Reference `AxSiting` from `Deskbridge.Protocols.Rdp` project (from Task 1.1).
    10. Add `[SkippableFact]` from `Xunit.SkippableFact`; use `Skip.If(Host is null, "Set DESKBRIDGE_SMOKE_RDP_HOST to run")` as the very first line of each live-RDP gate.
    11. Commit: `test(04-01): add smoke gate test scaffold (4 gates)` — note: will not compile until Task 1.1 + 1.2 complete.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; grep -c "Gate[1-4]_" tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/Deskbridge.Tests/Smoke/RdpHostControlSmokeTests.cs` exists
    - grep shows exactly 4 occurrences of `Gate` followed by `_` (one per test method)
    - File contains literal string `[Collection("RDP-STA")]`
    - File contains literal string `GetGuiResources`
    - File contains literal string `DESKBRIDGE_SMOKE_RDP_HOST`
    - File contains literal string `Deskbridge.Protocols.Rdp.Prototype.RdpSmokeHost` (fully-qualified reference)
    - File contains literal string `AxSiting.SiteAndConfigure`
    - Solution compiles only after Task 1.1 and 1.2 complete (test-project build will fail at end of Wave 0 — that is expected; the Wave 1 tasks close the loop).
  </acceptance_criteria>
  <done>Smoke test file exists at the correct path with all four gates scaffolded; compile failure against missing RdpSmokeHost + AxSiting is the expected state at end of Wave 0.</done>
</task>

<task type="auto" tdd="true" wave="1">
  <name>Task 1.1: Implement AxSiting.SiteAndConfigure helper (Gate 3 implementation)</name>
  <files>src/Deskbridge.Protocols.Rdp/AxSiting.cs</files>
  <read_first>
    - src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj (UseWindowsForms=true, AxMSTSCLib + MSTSCLib refs)
    - RDP-ACTIVEX-PITFALLS.md §1 (siting-order state machine — the entire section)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md lines 314-343 (canonical SiteAndConfigure<T> code, use verbatim)
    - tests/Deskbridge.Tests/Rdp/SitingGuardTests.cs (Task 0.1 — the test this implements against)
  </read_first>
  <behavior>
    - SiteAndConfigure: first sets `host.Child = rdp` (step 1 — triggers CreateControl inside WFH), then `viewport.Children.Add(host)` (step 2 — adds to visual tree, triggers handle creation), then asserts `rdp.Handle != IntPtr.Zero` (throws if siting failed), then calls `configure(rdp)` (step 3 — safe to set properties now).
    - Throws `InvalidOperationException` with message containing literal substring "not sited" when Handle == 0 after add — matches test expectation.
  </behavior>
  <action>
    Create `src/Deskbridge.Protocols.Rdp/AxSiting.cs` verbatim from 04-RESEARCH.md lines 314-343 Pattern 1 helper. Literal requirements:
    1. `namespace Deskbridge.Protocols.Rdp;`
    2. `public static class AxSiting`
    3. Method signature: `public static void SiteAndConfigure&lt;T&gt;(Panel viewport, WindowsFormsHost host, T rdp, Action&lt;T&gt; configure) where T : AxHost`
    4. Body steps 1-3 exactly as in RESEARCH.md:
       ```csharp
       host.Child = rdp;
       viewport.Children.Add(host);
       if (rdp.Handle == IntPtr.Zero)
           throw new InvalidOperationException(
               "AxHost not sited after adding to visual tree. " +
               "Parent container may be collapsed or have no layout. See RDP-ACTIVEX-PITFALLS §1.");
       configure(rdp);
       ```
    5. XML doc comment referencing RDP-ACTIVEX-PITFALLS §1.
    6. Using directives: `System.Windows.Controls` (for Panel), `System.Windows.Forms.Integration` (for WindowsFormsHost), `System.Windows.Forms` (for AxHost).
    7. Commit: `feat(04-01): add AxSiting.SiteAndConfigure helper (RDP-02)`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~SitingGuardTests"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Deskbridge.Protocols.Rdp/AxSiting.cs` exists
    - File contains literal strings: `public static class AxSiting`, `SiteAndConfigure&lt;T&gt;`, `where T : AxHost`, `host.Child = rdp`, `viewport.Children.Add(host)`, `not sited`
    - `dotnet test --filter "FullyQualifiedName~SitingGuardTests"` passes with 2 passing tests (Throws_When_HandleIsZero + DoesNotThrow_When_HandleIsNonZero)
    - No warnings in the Deskbridge.Protocols.Rdp project build (Directory.Build.props TreatWarningsAsErrors inherited from foundation)
  </acceptance_criteria>
  <done>AxSiting compiles, SitingGuardTests pass, no warnings.</done>
</task>

<task type="auto" wave="1">
  <name>Task 1.2: Implement RdpSmokeHost prototype (connect/dispose primitives for gates)</name>
  <files>src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs</files>
  <read_first>
    - src/Deskbridge.Core/Interfaces/IProtocolHost.cs (does NOT implement this — 04-02 does; read for shape reference only)
    - RDP-ACTIVEX-PITFALLS.md §1, §3, §4, §6, §7 — the five sections this code must honor
    - WINFORMS-HOST-AIRSPACE.md §leaks / §RdpHostWrapper reference implementation (full-cleanup sequence; prototype USES this, not a reduced version, because gate 1 measures GDI and the reflection-based leak fixes are mandatory for accurate measurement)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Example 1 (lines 782-887 — verbatim Dispose sequence)
    - .planning/phases/04-rdp-integration/04-RESEARCH.md §Common Pitfalls 1 (lines 695-709 — reflection leak fixes are mandatory)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §D-02 (all four gate criteria this code supports)
    - .planning/phases/04-rdp-integration/04-CONTEXT.md §Specifics (default property values: SmartSizing=true, EnableCredSspSupport=true, CachePersistenceActive=0, BitmapPeristence=0 [COM-typelib misspelled], KeyboardHookMode=0, GrabFocusOnConnect=false, EnableAutoReconnect=false)
  </read_first>
  <action>
    Create `src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs`. This is a **throwaway prototype** — 04-02 will NOT depend on it. Namespace `Deskbridge.Protocols.Rdp.Prototype` signals this intent. Literal requirements:

    1. `namespace Deskbridge.Protocols.Rdp.Prototype;`
    2. `public sealed class RdpSmokeHost : IDisposable`
    3. Fields (private): `WindowsFormsHost? _host`, `AxMsRdpClient9NotSafeForScripting? _rdp`, `TaskCompletionSource&lt;bool&gt;? _loginTcs`, `TaskCompletionSource&lt;bool&gt;? _disconnectTcs`, `bool _disposed`, `int? _lastDiscReason`.
    4. Public surface:
       ```csharp
       public WindowsFormsHost Host => _host ?? throw new ObjectDisposedException(nameof(RdpSmokeHost));
       public int? LastDiscReason => _lastDiscReason;
       public event EventHandler&lt;string&gt;? ErrorOccurred;
       public Task ConnectAsync(System.Windows.Controls.Panel viewport, string hostname, int port, string? username, string? domain, string? password, CancellationToken ct);
       public Task DisconnectAsync();
       public void Dispose();
       ```
    5. **Constructor** asserts STA per RDP-ACTIVEX-PITFALLS §6:
       ```csharp
       public RdpSmokeHost()
       {
           if (Thread.CurrentThread.GetApartmentState() != ApartmentState.STA)
               throw new InvalidOperationException("RdpSmokeHost must be constructed on STA thread (RDP-ACTIVEX-PITFALLS §6).");
           _host = new WindowsFormsHost { Background = System.Windows.Media.Brushes.Black };
           _rdp = new AxMsRdpClient9NotSafeForScripting();
       }
       ```
    6. **ConnectAsync** — wire events BEFORE siting, use `AxSiting.SiteAndConfigure` to site, set properties per defaults, set password via `GetOcx() as IMsTscNonScriptable` (throws if null), call `_rdp.Connect()`:
       - Wire handlers: OnLoginComplete → `_loginTcs.TrySetResult(true)`, OnDisconnected-during-connect → `_loginTcs.TrySetException(new Exception($"Disconnected during connect: discReason={e.discReason}"))` AND set `_lastDiscReason`, OnLogonError → raise `ErrorOccurred` with `$"LogonError: {e.lError}"`.
       - Use `AxSiting.SiteAndConfigure(viewport, _host, _rdp, rdp =&gt; { ...set all properties... })` — this is the code path gate 3 validates.
       - After siting, set password: `var ocx = _rdp.GetOcx() as IMsTscNonScriptable ?? throw new InvalidOperationException("GetOcx returned non-IMsTscNonScriptable"); ocx.ClearTextPassword = password ?? "";` wrapped in try/catch for `COMException`, `InvalidCastException`, `NullReferenceException`.
       - Do NOT log `password` anywhere. Do NOT interpolate `password` into exception messages.
       - Default properties to set (from 04-CONTEXT.md §Specifics + 04-RESEARCH.md Pattern 4):
         - `rdp.Server = hostname`
         - `rdp.AdvancedSettings9.RDPPort = port`
         - `rdp.UserName = username ?? ""`
         - `rdp.Domain = domain ?? ""`
         - `rdp.ColorDepth = 32`
         - `rdp.AdvancedSettings9.SmartSizing = true`
         - `rdp.AdvancedSettings9.EnableCredSspSupport = true`
         - `rdp.AdvancedSettings9.CachePersistenceActive = 0`
         - `rdp.AdvancedSettings9.BitmapPeristence = 0` (note: property name has no 's' — COM typelib misspelling, do NOT "fix" it)
         - `rdp.SecuredSettings3.KeyboardHookMode = 0`
         - `rdp.AdvancedSettings9.GrabFocusOnConnect = false`
         - `rdp.AdvancedSettings9.EnableAutoReconnect = false`
         - `rdp.AdvancedSettings9.ContainerHandledFullScreen = 0`
       - Call `_rdp.Connect();` wrapped in try/catch for `COMException` and `AxHost.InvalidActiveXStateException`.
       - Return `_loginTcs.Task`.
       - Honor `CancellationToken`: use `ct.Register(() =&gt; _loginTcs.TrySetCanceled(ct))`.
    7. **DisconnectAsync** — if `_rdp.Connected != 0`, call `_rdp.Disconnect()` (catch swallow), poll `_rdp.Connected` every 100ms for up to 30s, plain `await Task.Delay(100)` (no ConfigureAwait — stays STA per D-11).
    8. **Dispose** — copy verbatim from 04-RESEARCH.md lines 802-880 (the full cleanup sequence including both reflection-based leak fixes). Specifically:
       - Run `DisconnectAsync().GetAwaiter().GetResult()` first.
       - Unsubscribe all wired events (OnLoginComplete, OnDisconnected, OnLogonError) inside try/catch.
       - WFH leak fix #1: `HwndSourceKeyboardInputSite._sinkElement` null-out via reflection (see RESEARCH lines 826-841).
       - WFH leak fix #2: `WindowsFormsHost.HostContainerInternal` Dispose via reflection (see RESEARCH lines 844-853).
       - `_host.Child = null;`
       - `Marshal.FinalReleaseComObject(_rdp.GetOcx())` (try/catch).
       - `_rdp.Dispose()` (try/catch for `AccessViolationException`, `InvalidComObjectException`, `COMException`).
       - `_rdp = null;`
       - `_host?.Dispose(); _host = null;`
    9. Log errors (but NEVER password) via a plain `System.Diagnostics.Debug.WriteLine` — prototype does not need Serilog. 04-02 uses `ILogger<RdpHostControl>`.
    10. Commit: `feat(04-01): add RdpSmokeHost prototype for gate tests`.
  </action>
  <verify>
    <automated>cd C:/Users/cyclo/projects/Deskbridge &amp;&amp; dotnet build src/Deskbridge.Protocols.Rdp/Deskbridge.Protocols.Rdp.csproj -c Debug</automated>
  </verify>
  <acceptance_criteria>
    - File `src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs` exists
    - File contains literal strings: `public sealed class RdpSmokeHost`, `AxMsRdpClient9NotSafeForScripting`, `IMsTscNonScriptable`, `ClearTextPassword`, `AxSiting.SiteAndConfigure`, `_sinkElement`, `HostContainerInternal`, `Marshal.FinalReleaseComObject`, `ApartmentState.STA`, `EnableAutoReconnect = false`, `CachePersistenceActive = 0`, `KeyboardHookMode = 0`, `GrabFocusOnConnect = false`, `BitmapPeristence = 0`
    - File does NOT contain literal strings `password` or `ResolvedPassword` inside any `Log.*(...)`, `Debug.WriteLine(...)`, or interpolated string that reaches an output sink (grep: `grep -n "WriteLine.*password" src/Deskbridge.Protocols.Rdp/Prototype/RdpSmokeHost.cs` returns zero lines)
    - Project `Deskbridge.Protocols.Rdp` builds with zero warnings
  </acceptance_criteria>
  <done>RdpSmokeHost compiles; references AxSiting; implements full disposal sequence with both reflection leak fixes; password never appears in any logging sink.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" wave="2">
  <name>Task 2.1: Live-RDP gate verification (D-02 criteria #1, #2, #4)</name>
  <files>(human verification — no files modified)</files>
  <action>Pause execution. Present the what-built / how-to-verify content below to the user and wait for the resume-signal. On approval, record the outcome in the plan SUMMARY.</action>
  <verify>User confirms via resume-signal; outcome recorded in SUMMARY.md.</verify>
  <done>User approved or the failure is documented and escalated.</done>
  <what-built>
    Prototype `RdpSmokeHost` with full disposal sequence (including two WFH reflection leak fixes), `AxSiting.SiteAndConfigure` helper, and 4 gate tests wired to a live RDP host via environment variables. Gate 3 is already green from Task 1.1; Gates 1, 2, 4 need a real RDP target.
  </what-built>
  <how-to-verify>
    1. Enable Remote Desktop on the target machine (for localhost: Settings > System > Remote Desktop > On). Note a valid local user + password.
    2. In a PowerShell / bash terminal in `C:/Users/cyclo/projects/Deskbridge`, set env vars:
       ```
       $env:DESKBRIDGE_SMOKE_RDP_HOST = "127.0.0.1"
       $env:DESKBRIDGE_SMOKE_RDP_USER = "<your-local-user>"
       $env:DESKBRIDGE_SMOKE_RDP_PASS = "<that-password>"
       ```
       (or export equivalents in bash)
    3. Run the full smoke gate: `dotnet test tests/Deskbridge.Tests/Deskbridge.Tests.csproj --filter "FullyQualifiedName~Smoke"`
    4. Verify in the test output:
       - **Gate 1** passes with a reported GDI delta < 50 handles (captured in the Assert message). If delta ≥ 50 but < 200, record the delta; Phase 5's 15+ connection warning covers this class. If delta ≥ 200, STOP — escalate.
       - **Gate 2** passes — `OnLoginComplete` fired within 30s.
       - **Gate 4** passes — `OnDisconnected` fires with a discReason ≠ 0 on a bad hostname; process does not exit; `ErrorOccurred` is raised with a sanitized message.
    5. Record the observed GDI delta in Plan 04-01 SUMMARY for STATE.md.
    6. If any gate fails, do NOT proceed to Plan 04-02. Open an issue / escalate. If gate 1 delta is between 50 and 200, note it but proceeding is acceptable.
  </how-to-verify>
  <resume-signal>
    Type "approved &lt;GDI-delta&gt;" (e.g. "approved 12") where the number is the measured delta, OR "fail &lt;reason&gt;" to describe what broke.
  </resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RDP COM ↔ managed AxHost | Untrusted COM layer; exceptions and lifecycle state cross here. Must wrap every COM call in try/catch and assert apartment state. |
| Test process ↔ live RDP target | Real network I/O. Test env vars carry a real password; test runner output must never capture it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-GDI | Denial of Service | `RdpSmokeHost.Dispose` + WFH leak fixes | mitigate | The 20-cycle Gate 1 test measures GDI delta directly. Both reflection-based WFH leak fixes (HwndSourceKeyboardInputSite._sinkElement, HostContainerInternal) are mandatory per WINFORMS-HOST-AIRSPACE.md. If delta ≥ 50, escalate before Plan 04-02. |
| T-04-CRED | Information Disclosure | `RdpSmokeHost.ConnectAsync` (ClearTextPassword setter) | mitigate | Password never logged. No `Debug.WriteLine`, no string interpolation, no exception message includes the password. Unit test in Plan 04-02 (PasswordLeakTests) enforces this over time; Plan 04-01 enforces via acceptance_criteria grep. |
| T-04-ISO | Denial of Service | `RdpSmokeHost.ConnectAsync` + Gate 4 test | mitigate | Every COM call wrapped in try/catch. `ErrorOccurred` event raised with sanitized typed info. Gate 4 asserts process stays alive after a COM failure during connect. |

</threat_model>

<verification>
- Build: `dotnet build Deskbridge.sln -c Debug` succeeds with zero warnings
- Unit tests (pure): `dotnet test --filter "FullyQualifiedName~SitingGuardTests"` passes (2 tests)
- Gate tests (manual, requires live RDP): `dotnet test --filter "FullyQualifiedName~Smoke"` with `DESKBRIDGE_SMOKE_RDP_HOST` set — all 4 gates pass
- Security grep: no occurrence of `password` in any `Debug.WriteLine` / `Console.WriteLine` / `Log.*` in `src/Deskbridge.Protocols.Rdp/Prototype/`
- File artifacts exist: `AxSiting.cs`, `RdpSmokeHost.cs`, `StaCollectionFixture.cs`, `RdpHostControlSmokeTests.cs`, `SitingGuardTests.cs`
- GDI delta recorded in Plan SUMMARY (updates STATE.md Blockers/Concerns section: the Phase 4 blocker becomes resolvable)
</verification>

<success_criteria>
- AxSiting helper lives at `src/Deskbridge.Protocols.Rdp/AxSiting.cs` and is used by both Plan 04-01 tests and Plan 04-02 (production `RdpHostControl`)
- RdpSmokeHost prototype lives under `Prototype/` namespace signaling throwaway status
- All 4 gate tests (Gate1-Gate4) exist and pass against a live RDP target
- GDI delta over 20 connect/disconnect cycles is < 50 handles (per D-02 criterion #1)
- IMsTscNonScriptable + ClearTextPassword succeeds (per D-02 criterion #2)
- Siting-order guard throws when violated (per D-02 criterion #3)
- COM error on bad hostname does not tear down process; surfaces cleanly via `ErrorOccurred` event (per D-02 criterion #4)
- STATE.md Blockers/Concerns Phase 4 entry is cleared (done via SUMMARY.md write)
</success_criteria>

<output>
After completion, create `.planning/phases/04-rdp-integration/04-01-SUMMARY.md` documenting:
- Files created (5 paths listed under `files_modified`)
- Observed GDI delta across 20 cycles (single integer)
- Any deviations from plan (e.g. if SkippableFact pattern adjusted)
- STATE.md Phase 4 blocker: resolved / still open (based on gate outcomes)
</output>
