<ui:ContentDialog x:Class="Deskbridge.Dialogs.CommandPaletteDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:local="clr-namespace:Deskbridge.Dialogs"
    xmlns:vm="clr-namespace:Deskbridge.ViewModels"
    d:DataContext="{d:DesignInstance vm:CommandPaletteViewModel}"
    mc:Ignorable="d"
    IsFooterVisible="False"
    DialogMaxWidth="480"
    DialogMaxHeight="480">

    <!-- BasedOn style for WPF-UI theming (Pitfall 1: ContentDialog subclasses need this). -->
    <ui:ContentDialog.Resources>
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}"
               TargetType="{x:Type local:CommandPaletteDialog}" />
    </ui:ContentDialog.Resources>

    <!-- UI-SPEC §Command Palette Internal Layout: 480 wide, top-anchored via body Margin. -->
    <DockPanel Width="480" Margin="0,0,0,0">

        <!-- Search row (48px height, Search icon + TextBox + Esc hint) -->
        <Border DockPanel.Dock="Top"
                Height="48"
                Padding="12,0"
                BorderThickness="0,0,0,1"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}">
            <Grid VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="24" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:SymbolIcon Grid.Column="0"
                               Symbol="Search24"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <ui:TextBox Grid.Column="1"
                            x:Name="SearchBox"
                            FontSize="14"
                            BorderThickness="0"
                            Background="Transparent"
                            Margin="8,0,0,0"
                            VerticalAlignment="Center"
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                            ClearButtonEnabled="False" />

                <Border Grid.Column="2"
                        Padding="6,2"
                        CornerRadius="4"
                        VerticalAlignment="Center"
                        Margin="8,0,0,0"
                        Background="{DynamicResource ControlFillColorSecondaryBrush}">
                    <TextBlock Text="Esc"
                               FontSize="11"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </Border>
            </Grid>
        </Border>

        <!-- Result list (bound to CommandPaletteViewModel.Items) -->
        <ListBox x:Name="ResultList"
                 ItemsSource="{Binding Items}"
                 SelectedItem="{Binding SelectedItem}"
                 BorderThickness="0"
                 Background="Transparent"
                 MaxHeight="400"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:CommandPaletteRowViewModel}">
                    <Border Padding="12,0" Height="40">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="24" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ui:SymbolIcon Grid.Column="0"
                                           Symbol="{Binding Icon}"
                                           FontSize="16"
                                           VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1"
                                        VerticalAlignment="Center"
                                        Margin="8,0,0,0">
                                <TextBlock Text="{Binding Title}"
                                           Style="{StaticResource BodyStyle}" />
                                <TextBlock Text="{Binding Subtitle}"
                                           Style="{StaticResource CaptionStyle}"
                                           Visibility="{Binding Subtitle, Converter={StaticResource NullToCollapsed}}" />
                            </StackPanel>

                            <Border Grid.Column="2"
                                    Padding="6,2"
                                    CornerRadius="4"
                                    VerticalAlignment="Center"
                                    Margin="8,0,0,0"
                                    Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                    Visibility="{Binding Shortcut, Converter={StaticResource NullToCollapsed}}">
                                <TextBlock Text="{Binding Shortcut}"
                                           FontSize="11"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            </Border>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</ui:ContentDialog>
