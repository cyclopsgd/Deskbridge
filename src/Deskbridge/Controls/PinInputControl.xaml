<UserControl x:Class="Deskbridge.Controls.PinInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Focusable="False">

    <UserControl.Resources>
        <!-- Standard WPF TextBox (not ui:TextBox) to avoid WPF-UI template overhead
             on tiny single-char cells. Pitfall 8a does NOT apply to stock TextBox. -->
        <Style x:Key="PinCellStyle" TargetType="TextBox">
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="44" />
            <Setter Property="MaxLength" Value="1" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="FontSize" Value="22" />
            <!-- Audit U16: no FontFamily — inherit the app default like every other
                 control (design rule: FontFamily is never hardcoded). -->
            <Setter Property="CaretBrush" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="4,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="CellBorder"
                                Background="{DynamicResource ControlFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Focusable="False" />
                                <!-- Audit U6: 2px accent underline marks the focused cell,
                                     matching WPF-UI's Fluent text-input focus treatment
                                     used by every other input in the app. -->
                                <Border x:Name="FocusUnderline"
                                        Height="2"
                                        VerticalAlignment="Bottom"
                                        CornerRadius="0,0,3,3"
                                        Background="{DynamicResource SystemAccentColorPrimaryBrush}"
                                        Visibility="Collapsed"
                                        IsHitTestVisible="False" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="FocusUnderline" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CellBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <StackPanel x:Name="CellPanel" Orientation="Horizontal" HorizontalAlignment="Center">
        <TextBox x:Name="Cell0" Style="{StaticResource PinCellStyle}" />
        <TextBox x:Name="Cell1" Style="{StaticResource PinCellStyle}" />
        <TextBox x:Name="Cell2" Style="{StaticResource PinCellStyle}" />
        <TextBox x:Name="Cell3" Style="{StaticResource PinCellStyle}" />
        <TextBox x:Name="Cell4" Style="{StaticResource PinCellStyle}" />
        <TextBox x:Name="Cell5" Style="{StaticResource PinCellStyle}" />
    </StackPanel>
</UserControl>
